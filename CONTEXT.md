## 2026-09-22 手动运行按默认电脑路由（本地源码已改，正式服务未重启）

任务中心手动“运行”现在由后端检查任务绑定的目标：绑定已批准 B 时，沿远程运行服务冻结版本并在 B 排队；历史未绑定任务保持 A 本机队列。本机手动队列自身也拒绝绑定远端的任务，避免并发修改导致 A 代跑。远程运行仍限管理员，重复请求标识返回原运行；前端显示“默认运行电脑（手动和计划）”，按返回类型打开本机或远程记录。隔离副本全后端 439 项通过、18 项条件跳过；前端构建成功并同步 `frontend/dist/`。这不等于正式 A 主控已加载，也不等于 B 现场验收；本次未操作正式数据库、未重启或推送。待补：完整浏览器交互、A/B 实机离线恢复矩阵和旧本机执行器统一 Agent 协议。

## 2026-09-22 手动运行目标一致性（需求确认与规划记录）

用户确认：任务选择 B 电脑后，任务中心点击“运行”也必须在 B 运行；B 退出 Agent 时只在 B 排队，不能由 A 本机执行并显示成功。已在[需求 8.1 节](docs/多宿主机调度需求.md)、[任务拆分 10 节](docs/多宿主机调度任务拆分.md)和[参考实现对照 8 节](docs/多宿主机调度参考实现对照验证_2026-09-20.md)补充语义、实施项和证据。参考 `kk-rpa-dashboard` 的手动与计划触发共用 `task.robot_id`；SpiderFly 当时计划触发会读 `target_host_id`，宿主机页可独立向 B 分发，但任务中心手动入口仍直接入 A 本机队列。此段为随后修改代码之前的规划记录。

## 2026-09-22 B 电脑接入误报维护（本地源码已修，运行中的 B Agent 尚未升级）

真实 B 电脑已安装 Python 和 Agent 依赖，完成注册、获批并持续心跳；向导却报“未完成”。根因是 `manage.ps1` 用虚拟环境启动器路径比对 Agent PID，但 Windows 运行进程显示基础 Python 的可执行文件路径，导致 30 秒内始终不认进程。现按 `.venv/pyvenv.cfg` 中的基础解释器路径及精确模块、数据目录核对，保留对不相关 PID 的拒绝。首次依赖准备等待窗口延至 10 分钟，已就绪依赖为 1 分钟；子进程提前退出时提取实际错误。后台启动参数改用编码命令，避免含空格的目录或电脑名称被拆分。安装向导优先显示可识别的具体错误；人工升级清单补全向导和 Python 安装脚本。

隔离合成进程已验证启动、状态、安全停止、带空格路径、不相关 PID 拒绝以及人工升级文件完整性。A 的现有主控和 B 的现有 Agent 未停止、未覆盖；修复需以后从主控重新下载 Agent 包、在 B 空闲时人工升级才能生效。A 数据库当前只有一条成功的本机手动执行，`remote_runs` 为空；任务中心“运行”不受计划宿主机绑定影响，真正 B 电脑任务须从“宿主机”页指定 B 分发，不能把本机成功写成双机任务通过。

## 2026-09-22 Agent 向导自动准备 Python（A 电脑主控已运行，B 电脑待验收）

Agent ZIP 新增 `install-python.ps1`；B 电脑向导在缺少 64 位 Python 3.12 时可点击“自动安装”，从 python.org 下载 3.12.10、校验数字签名、为当前用户安装并设置 PATH，结束后自动复检。已安装时直接跳过；`start.ps1` 也能识别标准安装目录，避免当前进程 PATH 尚未刷新的误报。主控下载接口把新脚本纳入完整性白名单。用户步骤、失败日志及系统软件边界见 README、Agent README 和宿主机功能地图。

Windows PowerShell 5.1 解析、已有 Python 的重复安装跳过、旧 PATH 探测与宿主机接口 59 项隔离测试曾通过；B 电脑从零下载、签名校验、安装、接入和真实双机任务尚未现场验证。主控本机配置为 `0.0.0.0:8383`，配置与运行资料保留本地，不进 Git。此次文档与源码发布后仍需从 A 页面重新下载 Agent ZIP 到 B 电脑实测。

## 2026-09-21 阶段 7 发布准备（源码和隔离验证已完成，未部署、未做真实双机验收）

GitHub README 已按真实多宿主机能力重排：首屏解释主控与 Agent 的关系，优先给出两台电脑的小白安装、审批和双端调度步骤，再展示真实 AI 案例、运行机制、验证证据与使用边界；Hero 更新为主控 → Windows Agent → 进度/日志/产物回传的纯 SVG 系统图。明确第二台电脑只下载 Agent ZIP、不克隆完整仓库，不再把当前源码误写成“不支持多机调度”。

主控新增 `scripts/spiderfly-controller.ps1`，提供启动、停止、状态、日志、备份、空目录恢复和人工快进升级入口；停止/备份先检查运行占用和 SQLite，升级要求干净发布目录、备份成功且不自动重启。根目录新增 `安装并启动主控.bat` 图形向导，检查 Python 3.12、配置端口并按需申请局域网防火墙权限。Agent 升至 v0.3.0，新增 `manage.ps1` 的启动、文件式安全停止、状态、日志、同一主控地址切换、备份恢复和人工 ZIP 升级；下载包新增 `安装并接入Agent.bat` 图形向导，检查主控连通、接入码、电脑名称、调度与登录后自动启动。程序、DPAPI 身份和 journal 分开处理，不盲目自更新。

协议继续为 v1，主控同时公布并强制最低 Agent 0.3.0。旧版本或协议不兼容时在注册/心跳阶段返回明确升级提示，不消费接入码、不更新宿主机状态、不下发任务。主控/Agent 备份均带 SHA-256 清单，恢复拒绝覆盖非空目录且不自动启动；现有单机数据迁移、Agent 换机重新注册、源码与数据库配套回退及 16 项双机现场矩阵已写入发布验收指南。

宿主机 API 专项 59 项通过；Agent 48 项中 47 通过、1 条因当前账号不能创建符号链接而跳过；全后端隔离回归 435 项中 417 通过、18 条件跳过，pip check 通过。Windows PowerShell 5.1 下实际完成合成 Agent 托管启动/状态/安全停止、备份恢复、程序升级、地址变更，以及合成主控数据库/任务文件/工作目录备份恢复，数据库 quick_check 为 ok。小白向导接入后前端重新构建为 `index-Bwb5GE1H.js` / `index-LOW5d5qc.css`；Node 20.15.0 低于 Vite 建议版本但构建成功。本阶段源码随发布提交推送至 `main`；未停止或重启正式服务、未操作正式数据/计划、未安装第二台电脑。真实浏览器/Excel、锁屏/RDP、断网/崩溃、主控与 Agent 关停恢复及数据库恢复后真实运行仍待现场。详见[验证记录](docs/多宿主机阶段7发布准备验证_2026-09-21.md)和[发布验收指南](docs/多宿主机阶段7发布与双机验收.md)。

## 2026-09-21 阶段 6 收尾：Agent 托盘、状态条与人工接管（源码和隔离验证已完成，未部署）

Windows Agent 升至 v0.2.0。`start.ps1` 默认启动当前用户托盘，显示等待批准、非调度、调度空闲、运行中、桌面不可用和连接异常；本机可切换调度模式。任务运行时显示紧凑顶部状态条，包含任务、结构化进度或旧脚本原始状态、耗时、停止和紧急接管。运行中不能直接关闭调度；紧急接管会先停止当前进程树，确认 worker 结束后再切为非调度。

阶段 6 按局域网首版范围收尾。输入限制保持关闭，没有新增键盘鼠标钩子，并明确延期到真实验收证明有必要后再单独立项；所有远程任务首版统一单 Agent 串行并显示同一状态条，不识别后台任务类型，也不承诺任务脚本不使用桌面。当前连接、托盘和状态条仍在同一个用户进程，后台服务与当前用户会话组件拆分不纳入首版。Agent 隔离回归 47 项：46 通过、1 条因当前账号不能创建符号链接而跳过；宿主机接口临时副本回归 57 项通过，覆盖 Agent 0.2.0 下载包对白名单文件的完整性检查。桌面预览进程可启动。当前 computer-use 环境没有原生窗口接口，未完成像素级视觉验收；真实浏览器/Excel、锁屏、RDP、断网和崩溃矩阵移交阶段 7 双机验收。未部署到正式 Agent、未提交或推送。

## 2026-09-21 远程定时分发（源码、构建与专项隔离验证已完成，正式后端未加载）

任务现在可绑定一台已批准的计划运行宿主机。每天或每周计划到点时，调度器冻结当时的版本、目标机器和计划时间，直接创建远程运行，不进入旧本机队列；目标机离线、非调度或忙碌时保留在该机器队列，不自动改投。未绑定宿主机的任务继续使用旧本机队列。任务名称、说明、计划和目标机器编辑不创建代码版本。

同一计划发生时间使用稳定请求 ID，重复提交只保留一条远程运行。停用任务会取消尚未领取的远程排队记录；撤销一台宿主机时会同时停用绑定到它的未来计划并取消其排队记录，已运行或状态待确认的记录仍遵循原租约规则。可通过 `SPIDERFLY_SERVER_URL` 配置局域网主控地址，供计划失败通知生成详情链接；未配置时沿用该宿主机最近一次网页分发记录的主控地址。

宿主机、调度与任务 API 专项隔离回归 78 项通过；最终全后端隔离回归 433 项：415 通过、18 条件跳过。前端构建成功，资源为 `index-DZOg8rPE.js` / `index-BYooWMSp.css`。Node 20.15.0 仍低于 Vite 建议的 20.19.0，因此保留构建警告。已在隔离服务中完成任务列表、编辑宿主机选择、清空绑定、390px 窄屏无横向溢出及浏览器控制台检查。未做正式服务重启或两台真实 Windows 验收；未提交或推送。

## 2026-09-21 远程结构化进度与失败通知（源码、构建与隔离验证已完成，正式后端未加载）

多宿主机远程运行现复用现有 `SPIDERFLY_PROGRESS` JSON 行协议：Agent 的 stdout 事件可跨分片拼接，主控保存最近进度、有限历史和累计事件数，宿主机运行详情显示阶段、页码和采集数量；这些观察值不改变进程终态。远程失败或超时一经 Agent 终态确认，立即进入管理员“运行提醒”，错误摘要与原宿主机运行记录相连，已读状态按管理员账号独立保存。普通成员仍只读取原有维护结果，不会请求管理员专用接口。

远程失败飞书通知现沿用派发时冻结的任务失败通知开关。终态提交后由 worker 做一次发送尝试，消息包含宿主机、运行号、版本、脱敏错误和详情直达链接；未配置、失败或服务重启后的不确定发送均留痕但不自动重发，也不覆盖原运行结果。链接使用管理员派发时实际访问的主控地址，并由前端 `remote_run` 深链打开对应宿主机运行详情。

宿主机专项隔离回归 55 项通过，连同 worker 接线共 59 项；全后端隔离回归 430 项：412 通过、18 条件跳过。前端构建成功，资源为 `index-C2_3oV2g.js` / `index-BYooWMSp.css`；Node 20.15.0 仍低于 Vite 建议的 20.19.0，因此保留构建警告。未做页面浏览器验收、真实飞书送达、全局运行看板、正式服务重启或两台真实 Windows 验收。未提交或推送。

## 2026-09-21 远程失败维护候选（源码、构建与隔离验证已完成，正式后端未加载）

多宿主机链路新增独立远程维护队列：Agent 回传失败或超时终态后，主控用冻结的版本、脱敏后的失败摘要和远程事件生成 AI 参考候选；证据不足时可只保留建议。候选保持未运行、未启用，管理员在宿主机运行详情确认后，才会在同一事务内启用候选并向原宿主机创建一条新的远程运行。宿主机已撤销等无法重跑时，版本切换整体回滚。

该链路不进入旧单机“验证通过后自动启用”流程，也不会自动批准或自动重跑。宿主机页新增候选状态、源码下载、暂不使用和“确认并重新运行”入口；按 Kocotree UI 规范保持单一主操作及 720px 以下重排。专项隔离回归 50 项通过；最终全后端隔离回归 425 项：407 通过、18 条件跳过。页面用真实隔离 API 和合成失败候选检查了宽屏、720px、390px 和 320px，无页面横向溢出，候选操作区仅一个主按钮，控制台无错误或警告。检查中修正了 320px 根容器最小宽度导致的滚动条，并删除两处“远程 AI 候选尚未接入”的过时说明。

最终前端构建为 `index-Dnx-CWiQ.js` / `index-CLDuoCC1.css`；Node 20.15.0 低于 Vite 建议的 20.19.0，构建虽成功仍保留警告。未提交、推送、重启正式服务或操作正式数据；两台真实 Windows 仍未验收。

## 2026-09-20 多宿主机首增量（源码和构建已更新，正式后端未加载）

新增宿主机管理与 Agent 安装包下载、一次性接入与审批、Ed25519/DPAPI 身份、双端调度、指定机器普通 Python 任务手动运行、冻结快照、事件补传与产物下载。前端按用户指定 Kocotree 技能实现。现有前端未提交修改保留并一起参与隔离构建。

继续实施后，最终全后端隔离回归 422 项：404 通过、18 跳过，含 47 项宿主机 API 专项。Agent 43 项：42 通过、1 个符号链接权限跳过。新增 offer→持久化→claim 两步领取、首次响应丢失恢复、新建即 v1、远程运行固定版本 ID/序号，以及上传不做语法预检。手工更新和 AI 对话提交现在直接保存为未运行候选 vN，管理员确认前不会执行；前端统一小写 vN。真实 HTTP + Windows 子进程的同机双身份闭环与此前 19 项页面检查通过；最终前端已重新构建。此结果不等于两台物理电脑验收。

当前生产 Agent CLI 通过共享机器互斥锁与旧主控互斥，需在另一台 Windows 电脑运行。历史回退已改为复制内容生成新的后续 vN 并启用。远程失败生成 AI 维护候选、确认后重跑、远程定时分发、通知、托盘和状态条仍待实施；旧本机兼容环境仍会在任务保存后后台构建。授权后 journal 被人为删除/损坏时仍安全保留 uncertain。未提交、推送、重启正式服务或操作正式数据。前端 dist 已同步并保留旧资源，但新入口需后端配套加载；安装更新后端依赖 cryptography 后按空闲备份流程重启，不能把源码和构建更新视为完整上线。

入口：[实际能力](docs/功能地图/宿主机与远程运行.md)、[Agent 安装](agent/README.md)、[协议](docs/Agent协议_v1.md)、[验证记录](docs/多宿主机首增量验证_2026-09-20.md)。

## 2026-09-20 多宿主机调度目标（此前规划记录）

用户确认 SpiderFly 是唯一主产品，相邻 `kk-rpa-*` 项目只作参考。目标是在一台 Windows 主控中集中保存网页、数据库、AI 和知识库，由局域网内多台 Windows Agent 经一次性接入码申请、管理员批准并进入调度模式后接收任务。当前发布版仍是单机串行平台，本轮仅更新需求与任务拆分，没有修改运行代码、数据库、正式服务或现有任务。

第一阶段不建设 Linux 主控或公网入口。管理员上传一个 `.py` 和可选 `requirements.txt`，上传阶段不试跑；系统软件由宿主机使用者准备。AI保留创建任务能力，运行失败后只提供维护建议与不可变的下一候选版本，管理员确认后才将候选设为当前版本并重新运行；再次失败时继续生成下一个候选编号。版本统一为 `v1、v2、v3……`，内容不同就新增，历史不覆盖。

需求基线见 [多宿主机调度需求](docs/多宿主机调度需求.md)，实施与两机验收顺序见 [多宿主机调度任务拆分](docs/多宿主机调度任务拆分.md)。现有未提交前端改动保持原样，未纳入本次规划文档。

## 2026-09-15 发布内容与当前约定

本次提交基于 9 月 11 日 d3c8334，包含采集进度展示、自动重跑摘要简化、上传失败与修复版本来源提示，以及使用说明、AGENTS 和功能文档。9 月 14 日已丢弃的修改未恢复。

- 本机后端和最终前端均已加载；最终构建为 index-B1z2r-E4.js / index-Y-74q0Iz.css。
- 后端全套隔离回归 374 项：356 通过、18 条件跳过；最终前端构建、重跑摘要及版本待确认/确认后页面检查通过。未运行真实业务采集验证。
- 保留结构化进度；业务数据校验继续写在普通脚本中，未迁入通用步骤框架、多机器人或新的参数表单。
- 下方“未提交”等记录描述当时阶段，当前功能与使用边界以本节、使用说明和对应代码为准。

## 2026-09-15 上传失败与修复版本提示（前端已生效，未提交）

版本区顶部常驻最新更新经过和当前使用版本；上传失败生成修复版时明确显示来源关系、原版本验证信息和待确认操作。版本卡区分手动上传、自动修复和验证未通过。轮询刷新不再清除上传/操作错误。沿用现有后端验证与确认启用规则，不自动切换版本。前端构建、状态映射检查、隔离页面待确认/确认后提示与 390px 宽度检查通过；确认前无激活请求，点击后仅一次请求。静态资源已更新，无需重启。

## 2026-09-15 自动重跑摘要简化（前端已生效，未提交）

执行详情中的维护提示默认只显示一行结果与版本、重跑日志入口、折叠的“修复详情”。修改说明与完整事件移入详情，其他页面保持原展示。前端构建通过；隔离页面检查默认折叠、展开、重跑日志事件和 390px 宽度无溢出。仅前端变更，静态资源已更新，无需重启服务。

## 2026-09-15 采集进度与结果（本机已加载，未提交）

- 基于 9 月 11 日 d3c8334 增加采集进度，没有恢复 9 月 14 日已丢弃的修改。
- 普通 Windows、DP、受控采集输出支持进度事件；执行详情展示阶段、页码、累计数量、失败信息与完整性。未知总量不估算百分比，进度不能覆盖运行失败或平台验收。
- 原始日志继续保留；最近 40 条事件与最新数量独立持久化。旧脚本不受影响，需通过正常任务更新接入；新 AI 生成脚本已增加上报要求。协议见 [采集进度](docs/采集进度.md)。
- 隔离回归 374 项：356 通过、18 条件跳过；前端构建、桌面与手机组件页面验证、git diff --check 通过。DP/受限实时通道使用合成子进程验证，没有运行真实网站或用户业务任务。
- 服务空闲后备份 data 与 .env 至 ../SpiderFly-backups/before-collection-progress-20260915-085216，隐藏启动单 worker（PID 21900），9356 健康检查四个后台 worker 均正常。数据库 quick_check 为 ok，首页引用 index-mrC6ADQm.js。
- 详细证据见 [验证记录](docs/采集进度验证_2026-09-15.md)。未提交、未推送。

# SpiderFly 当前状态

## 正式服务已更新（2026-09-11）

源码提交 `9fed9d4` 已推送 GitHub main，随后按用户要求重启正式服务，加载本次代码与前端。服务地址为 `http://127.0.0.1:9356`；调度、执行队列、环境安装与 AI worker 健康检查均为 running。

重启前确认无待执行或运行中的任务，停止原启动器，并在仓库外完整备份 data、.env 和依赖清单（备份标识 pre-restart-20260911-171031）。正式平台 Python 已安装 backend/requirements-dp.txt，DrissionPage 4.1.1.4 导入成功，pip check 通过。新 DP 观察表已建立，数据库 quick_check 为 ok，首页加载当前构建 index-CX5Oz8Ju.js。

本次验证正式服务启动与依赖就绪，没有触发真实业务任务。下文“未重启”“仅测试环境安装”等描述保留历史阶段含义，当前部署状态以本节为准。

## 本次源码提交（2026-09-11）

用户授权将当前工作区全部项目改动一起提交到 GitHub main。本次包括模块拆分、Scrapling/DP 知识库、DP 原生观察与试跑、AI 对话与设置、候选版本确认、配套前端构建、文档与 Python 对照、AGENTS 维护规则及 README 美化。

发布前隔离副本回归 368 项：350 通过、18 条件跳过；前端 npm ci/build 通过。README 的本地链接、SVG、桌面/手机/深色预览已核对。此前 DP 原生专项和真实 AI 验证见下文记录。

本次提交不安装正式环境依赖、不重启正式服务、不变更已上传任务或真实数据。DP 在宿主机生效仍需在平台 Python 环境安装 backend/requirements-dp.txt，并加载新代码。下文与各验证记录中的“未发布”“未提交”描述当时验证状态；本次源码发布范围以本节及 Git 提交为准。

## 当前架构（2026-09-11）

- 单台 Windows 宿主机串行运行普通 Python 任务；平台位于 backend/app 与 frontend/src，业务只在上传脚本中。
- 指令框架、衍生运行库、兼容 wheel、本地包安装分支和对应示例、手册全部移除。原始历史可从 Git 提交追溯。
- 新脚本按实际需要声明 PyPI 依赖；标准库模板见 examples/task_template.py。旧包名在依赖入口明确拒绝，避免公网同名包误装。
- 失败或超时默认触发 AI 维护，受限验证通过后启用修复并自动重跑一次；原失败与版本保留，重跑失败不循环。
- AI 需要可用模型配置与已准备的 WSL readonly-v1 / collection-v1；不承诺任意 Windows 桌面程序或未知依赖自动修复。
- 仓库修改不自动替换已上传脚本，也不卸载现有任务环境；正式服务已重启，见顶部部署记录。
- 本轮验证结果见 [清理与自动修复验证](docs/架构精简验证_2026-09-11.md)。

## 本轮功能与历史验证

- 2026-09-11 已接入可选 drissionpage-v1：AI 通过 dp_probe 排队观察页面，生成原生 Python 并 test_collection 自动试跑；保存后执行、版本验证和维护使用同一执行器、普通任务配置端口（默认 9123）。真实 AI 六次调用约 33 秒采集知乎周榜前五条成功，页面观察和试跑均为 9123；保存后执行及停止清理另有真实专项验证。全套 368 项，350 通过、18 跳过；DP 专项 9 项真实运行通过，最后端口构造检查补跑 6 通过、3 条件跳过。原生 Windows 权限，不是 WSL 沙箱。仅测试环境安装 DP，正式环境依赖未安装、服务未重启、未提交推送。见 [DP 任务](docs/功能地图/DP任务.md) 与 [验证记录](docs/DP创建与试跑验证_2026-09-11.md)。

- 2026-09-11 用户明确要求查知识并用 DP，[知乎周榜隔离测试成功](docs/知乎周榜_DP验证_2026-09-11.md)：临时 DP 工具、实际 DrissionPage 4.1.1.4，主动 read_knowledge 两次后提取五条。没有主动 search_knowledge；未接入正式平台、未重启服务。

- 2026-09-11 用户指定 knowledge-plan 日榜地址后[重新测试成功](docs/知乎指定日榜_Agent验证_2026-09-11.md)：约 19 秒提取五条，来源和标题顺序一致；程序自动附带知识，Agent 仍未主动搜索或补读。未修改 Agent 或正式服务。

- 2026-09-11 完成[知乎日榜真实 Agent 测试](docs/知乎日榜_Agent验证_2026-09-11.md)：两轮均未主动查知识；首轮登录页后提前询问，追加轮误采微博并复用旧响应，预算不足结束。未取得有效知乎日榜前五条，本轮只测试记录。

- 2026-09-11 完成[微博 Top5 真实 Agent 测试](docs/微博Top5_Agent验证_2026-09-11.md)：获取 5 条响应数据，但模型没有主动调用知识工具，回答与导出字段不一致，知识调用验收尚未通过。正式服务未重启，本轮未修改 Agent 行为。

- 2026-09-11 吸收用户提供的 DrissionPage 4.1.1.2 本地 Markdown：64 篇清点，59 篇技术文档进入章节检索，新增完整目录、来源哈希和端口接管规则；版本与之前官网 4.1.1.4 概要分开。未接入 DP 执行，未重启正式服务。

- 2026-09-11 增补 DrissionPage 4 个参考专题与采集总索引；检索按工具优先并传递接入状态。仅知识参考，未安装或接入 DrissionPage，未重启正式服务。

- 2026-09-11 新增 13 个 Scrapling 主题知识文档、章节检索/读取和采集轮次自动参考；来源固定到 0.4.15。见 [采集知识库](docs/功能地图/采集知识库.md) 与 [验证记录](docs/采集知识库验证_2026-09-11.md)。本轮未升级采集依赖、未重启正式服务。

- AI 对话布局、历史对话切换与删除、规则摘要标题在本机另有改动；不包含在本次架构提交中。
- 模型设置与单轮时长相关代码另有本机改动，不以本段描述认定此次 GitHub 发布版已具备同样界面或限制。
- 已实现累计维护预算“不限”，实际用量继续记录，单次超时仍生效。历史记录包含正式服务重启及验证证据，不代表本轮代码已经加载。
- 人工候选版本确认、失败截图和前端变更按各自工作单独交付。本次发布指令相关功能清理、自动修复回归和配套文档。正式服务加载状态须以实际进程核实。

## 接手入口

2026-09-11 新增 [功能地图](docs/功能地图/README.md)，供项目负责人按六个功能模块阅读和提问。用户看过方案后授权实施，主要页面与接口已在工作区拆分；[代码对照与模块结构](docs/开发/代码对照与拆分方案.md) 维护实际位置，[模块拆分验证](docs/模块拆分验证_2026-09-11.md) 记录验证。本轮未提交、推送或重启正式服务，原有未提交改动保留。

[README](README.md) · [项目架构](docs/项目架构.md) · [开发与验证](docs/开发与验证.md) · [协作约定](AGENTS.md)

完整的原 CONTEXT 内容已迁入 [截至 2026-09-11 的历史记录](docs/history/CONTEXT_2026-09-11.md)，包括每轮变更、测试、重启和未交付说明。
