# SpiderFly 当前状态

## 当前架构（2026-09-11）

- 单台 Windows 宿主机串行执行 Python 任务，浏览器提供团队入口；平台与多机 Agent 调度项目独立。
- `backend/app` 负责平台；`backend/spiderfly_runtime` 提供可选输入输出和文件辅助；`flows` 集中维护单文件业务规则。完整边界见 [项目架构](docs/项目架构.md)。
- 新流程直接调用 Python 函数，不再使用指令注册框架。新包为 `spiderfly-runtime==0.1.0`，只包含通用辅助。
- 旧指令框架和内置业务示例源码已移除。原 0.1.0–0.1.4 wheel 及旧示例入口保留兼容，已上传任务、环境和版本不自动迁移。
- 架构工作区回归共 500 项；随后从实际待提交内容导出隔离副本，发布范围回归 488 项，473 通过、15 项环境相关跳过。wheel 与源码一致，五个旧 wheel 未变。正式服务未重启。验证记录见 [架构精简验证](docs/架构精简验证_2026-09-11.md)。

## 现有能力与限制

- 持久队列、计划、账号权限、日志、产物下载、任务代码版本及回退已经实现。
- DeepSeek 对话生成草稿；正常运行保存好的脚本不调用模型。人工候选验证后确认使用，故障自动修复按既有授权启用并重跑一次。
- 自动维护、草稿试跑与任务共用串行队列。受限环境仅支持已准备的 readonly-v1 / collection-v1 依赖；新运行库未安装到 WSL，不能扩大已支持范围。
- AI 支持公开网页探索、数据提取和 CSV 下载；浏览器会话与定时脚本的登录状态分开。
- 普通 Windows Python 任务与 WSL 受限任务使用不同环境，不能承诺任意 GUI 或未知依赖的自动修复。

## 本机另有待发布改动

- AI 对话布局、历史对话切换与删除、规则摘要标题在本机另有改动；不包含在本次架构提交中。
- 模型设置与单轮时长相关代码另有本机改动，不以本段描述认定此次 GitHub 发布版已具备同样界面或限制。
- 已实现累计维护预算“不限”，实际用量继续记录，单次超时仍生效。历史记录包含正式服务重启及验证证据，不代表本轮代码已经加载。
- 人工候选版本确认、失败截图和前端变更按各自工作单独交付。本次只发布架构精简与配套文档。正式服务加载状态须以实际进程核实。

## 接手入口

[README](README.md) · [项目架构](docs/项目架构.md) · [开发与验证](docs/开发与验证.md) · [协作约定](AGENTS.md)

完整的原 CONTEXT 内容已迁入 [截至 2026-09-11 的历史记录](docs/history/CONTEXT_2026-09-11.md)，包括每轮变更、测试、重启和未交付说明。
