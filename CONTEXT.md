# SpiderFly 调度中心

## GitHub 交付（2026-09-09）

本次提交包含 AI 采集环境、Scrapling 核心、任务需求与版本流程及对应文档。暂存内容导出到隔离副本后，全套 435 项检查通过 432 项，3 项 Windows 符号链接权限跳过；未使用真实模型或群通知。前端按暂存源码和锁文件重新构建，发布资源为 index-DttujKoG.js / index-9w2GKuq9.css。

独立的失败截图时序改动保留在本地，不包含在本次提交。正式服务未重启；提交代码不代表新后端已加载。下文“未提交/推送”是各阶段当时的记录，交付结果以 Git 记录为准。


## 需求与版本实现（2026-09-09，正式服务未加载）

已接入 task_versions：需求来源、原任务上传与 AI 提交、历史 PY 下载、验证/修复/启用、完整回退、待执行快照衔接和最终失败群通知。编号递增，错误上传与修复分别留版；维护失败不自动停用。前端入口收在“维护与版本”中。

当前需求推断只提取明确静态事实；自动验证支持已准备的 readonly-v1 / collection-v1，未知依赖与 Windows GUI 不支持。群发送用模拟接口验证，未向真实群发送。

本轮使用隔离副本和合成数据。正式服务未重启，未运行真实任务，未提交或推送；前轮正式重启被自动审批拒绝，本轮不绕过该限制。验证证据见 [版本更新验证](docs/版本更新验证_2026-09-09.md)。


## 需求与版本规则确认（2026-09-09，方案已记录）

用户逐项确认需求保存、代码推断来源、需求修改与修错分层、人工下载及原任务上传更新、版本编号递增、环境匹配、排队衔接及完整回退。修不好保留失败、通过已配置群通知交由团队人工处理，计划保留；不增加自动停用，页面保持一句结果加折叠详情。

本轮将规则保存为 [任务需求与版本方案](docs/任务需求与版本方案.md)，同步 AGENTS、README、使用说明和 AI 指南。仅修改说明文档，未实现上述新入口或改变代码、数据库、通知和服务；前轮测试不能作为这些待开发功能的验收。按方案的五步顺序继续开发。

## 对话配置说明补充（2026-09-09）

同步 AGENTS、README、使用说明、AI 指南、采集环境和 Agent 采集知识：对话要求需落实为源码及参数；单任务 HTTP 并发 1–4、crawl 默认 2，区别于任务队列和浏览器并行；说明逐页依赖与同会话串行、保存后定时沿用脚本、新对话不自动改正式任务。本次仅文档更新，核对接口、链接和差异，不新增运行测试、不重启服务，沿用下方发布状态。

## Scrapling 核心扩展（2026-09-09，当前）

用户接受先不加 DP、完善 Scrapling 核心能力。隔离副本已实现 FetcherSession 原生请求、DynamicSession 动态页面、匿名会话、1–4 并发取页、限速、按页断点。仅静态内容缺失时自动切动态；定时和维护仍由平台管理。新依赖安装在 WSL spiderfly-collection-v2，旧脚本标记兼容。

隔离全套 420 项检查通过 417 项，3 项 Windows 符号链接权限跳过。实际请求、动态页面、Cookie 隔离、并发和跨运行断点验证通过；未调用真实 DeepSeek。正式服务未重启，代码未提交/推送。详见 [核心验证记录](docs/Scrapling核心验证_2026-09-09.md)。

## AI 采集环境（2026-09-09，上一阶段）

用户确认接入 Scrapling，保留单文件 Python 入口。新增 collection-v1：受控公开 GET、Scrapling 解析、Playwright Chromium、同站翻页及页面内 JS；Agent 保存草稿后可通过 test_collection 排队实际试跑。页面展示状态、日志和结果文件，独立检查文件、字段、数量。带运行标记的新任务使用相同环境进行首次、定时、维护和重跑；原 readonly-v1 保留。

不接管 Windows Excel 或个人浏览器，不增加 Node.js/DP 任务入口。仅用户指定并写入验收的域名允许请求，登录、验证码及写入请求不支持。安装和具体上限见 [AI 采集环境](docs/AI采集环境.md)。

隔离全套 415 项检查通过 412 项、3 项符号链接权限跳过，真实 Scrapling 采集、Playwright 翻页、定时执行及页面权限验证通过。WSL 采集环境与前端构建已准备。自动审批拒绝了正式服务更新命令，后端未重启，新工具尚未在正式服务生效；未完成本轮正式备份，代码未提交/推送。详见 [本轮验证记录](docs/AI采集验证_2026-09-09.md)。

## 清理未采用方案（2026-09-08）

用户决定不增加平台 Excel/浏览器资源接管。删除本轮未采用的可选收尾模板、专用测试、接入说明及完整模板知识，清除各入口引用。Excel 保存及资源关闭仍由脚本按业务需要完成，上传的 Python 原样执行。

保留已完成的 DeepSeek 助手、知识工具、默认自动维护、简洁日志和修复后自动重跑。原公共指令、兼容示例及本机运行资料不清理。当前独立的失败截图时序改动留在本地，不纳入这次 AI 功能提交。本轮只清理源码与说明并提交 GitHub，不重启正式服务。

提交前从 Git 暂存区导出独立副本验证：403 项检查通过 400 项，3 项 Windows 符号链接权限跳过；启用实际 WSL 检查，pip check 通过。前端使用锁文件安装并构建，规范化 Git 源码换行后生成的 JS/CSS 与本次发布资源一致。未调用真实模型，未运行正式任务；本轮未重复此前已完成的页面验收。

## 修复后自动重跑（2026-09-08，当日较早阶段）

用户补充要求修复后重新执行程序。修复发布、重跑入队及维护关联已改为同一事务：沿用最早一条源码、依赖及输入兼容的排队记录，或新建“修复重跑”执行，保留冻结参数及原定时计划。每次修复自动重跑一次，失败不循环修复；停用任务不重跑，升级不补跑历史修复。

维护日志显示重跑实际状态，支持从原失败日志、AI 助手、通知直接查看重跑的输出和文件；完成后才形成维护通知。自动重跑占用剩余维护预算，最长 120 秒；重启和取消会收尾已关联执行，不重复入队。原失败记录保持不变。

隔离全套 403 项检查，400 通过、3 项 Windows 符号链接权限跳过；真实 WSL 串行队列自动重跑并生成两行 CSV，前端构建、pip check、管理员及成员页面验证通过。使用说明、README、AGENTS 和 AI 指南已同步。部署核对见 [修复后自动重跑验证](docs/修复后自动重跑验证_2026-09-08.md)。

13:21 已通过原 Windows 自启任务更新正式服务。完整备份 `%LOCALAPPDATA%/SpiderFly/Backups/before-repair-rerun-20260908-132134`；四个 worker 健康，重跑字段及队列索引已迁移，线上资源与构建逐字节一致。原 tasks/rpa_apps/users/executions 内容摘要未变，SQLite 完整性正常，没有补跑历史任务。代码与构建尚未提交或推送。

## 直接修复与页面精简（2026-09-08，当日较早阶段）

维护日志再次精简：维护记录、执行日志和通知共用一句修改及效果说明，去掉预览中的重复验收和启用套话；旧记录无需模型重新总结。完整事件与已确认用量在“技术详情”展开，历史版本在“代码版本”展开。模型提交修复说明时要求按“把 A 改为 B，解决 C”写一句简短中文，验证通过后才显示运行结果。

本次日志精简已验证并上线：隔离全套 394 项（391 通过、3 项符号链接权限跳过），构建及管理员/成员页面核对通过；正式任务及维护数据保持不变，四个 worker 健康。备份为 `%LOCALAPPDATA%/SpiderFly/Backups/before-concise-logs-20260908-125756`，详情见下方验证记录中的“一句话维护日志”追加部分。

文档同步：使用说明补齐 AI 创建、自动修复、日志、回退、预算和迁移步骤，移除“不会自动改代码或调用 Agent”等旧描述；AGENTS 明确直接修复的既有授权、队列衔接、隔离验证与简洁页面要求。相关入口名称与 AI 专门指南同步。本次仅更新文档，不改变代码或正式运行资料，无需重启或构建。

用户要求错误直接修正、内部衔接排队，只在日志说明修改；同时要求 AI 助手及其他页面清爽。普通脚本不再因缺少业务验收声明而停留在候选阶段：检查最小改动、在受限环境复现同类错误、验证修复后运行成功后自动启用；已有验收仍冻结执行。就绪修复在当前运行结束后优先验证，同任务同源且输入相同的待运行记录自动衔接新版本。名称、说明及计划编辑不阻止发布，真正的源码或输入更新不会被旧修复覆盖。

执行日志新增修复结论及可展开的修改记录。AI 助手默认显示最新草稿，长回复、历史、用量及维护版本收起，模型默认回复不超过三句话；模型预算等说明改为按需展开，其余页面冗余文案已精简。

隔离全套 394 项检查，391 通过、3 项 Windows 符号链接权限跳过，包含真实 WSL 故障复现与修复运行；前端构建、pip check、管理员及成员页面核对通过。正式服务已备份重启，四个 worker 健康。按用户要求将 AI_Test 执行 #13 的既有候选 V2 重新验证并自动启用：`3 + name` 改为 `name`，原 TypeError 复现、修复后通过，没有追加模型调用。原失败记录保持原样，日志显示维修结论。

备份为 `%LOCALAPPDATA%/SpiderFly/Backups/before-direct-repair-20260908-111410`。核对仅任务 3 和对应应用的代码路径、版本及更新时间按预期改变，其余 tasks/rpa_apps/users/executions 原有内容未变；SQLite 完整性正常，线上页面资源与构建逐字节一致。代码和构建尚未提交或推送。证据见 [直接修复与页面精简验证](docs/直接修复与页面精简验证_2026-09-08.md)。

## 默认自动维护与结果通知（2026-09-08，当日较早阶段）

用户明确“不用提供维护开关，只需要维护完让用户知道”。新执行失败后默认进入维护，保存原代码/模板引用、错误及原验收，生成一个修复候选，在 WSL Ubuntu 的 readonly-v1 环境试跑；通过冻结的独立验收且原业务仅生成产物时，自动切换版本。修复后正式执行也使用同一受限环境。没有验收、不兼容依赖或未通过试跑时保留候选和原因，不自动替换。正常成功任务不调用模型；同错去重及滚动预算约束费用。

右上角新增“维护结果”，任务归属人和超级管理员分别保留未读通知；任务 AI 助手下方查看维护记录、源码版本和回退。全局默认滚动 24 小时 2 小时 / 200,000 tokens，单任务 45 分钟。按内容去重源码和模板，版本不重复构建环境。公开网页快照、上传模板和新生成 CSV/JSON/XLSX 是当前受限支持范围，登录浏览器、Windows GUI、原公共 wheel 等依赖暂不能自动试跑。

隔离最新全套 388 项检查，385 通过、3 项 Windows 符号链接权限跳过，包含真实 WSL 文件/网络/子进程隔离与超时检查；pip check 与前端构建通过。真实 DeepSeek 自动修复一个故意出错的合成脚本并启用 V2，第二次执行成功，CSV 两行实际值与原要求一致；模型 1 次，输入 1,010 / 输出 270 tokens。浏览器验证通知、已读、维护记录、回退、预算保存和普通成员权限，未使用正式业务任务测试。部署及证据见 [自动维护验证](docs/自动维护验证_2026-09-08.md)。

正式服务已通过原自启任务重载，新后台与维护接口已生效。备份为 `%LOCALAPPDATA%/SpiderFly/Backups/before-maintenance-20260908-105119`；四个 worker 健康，tasks/rpa_apps/users/executions 原有内容摘要与备份一致，SQLite 完整性正常。没有回填历史失败记录或运行真实业务任务；源码和构建尚未提交或推送 Git。

## DeepSeek 开发助手首版（2026-09-08，历史阶段）

用户已同意接入 DeepSeek，并允许根据采集及表格处理需要扩展工具。Python 仍是正式任务入口。新增任务中心“AI 创建”、每任务“AI 助手”和管理中心“AI 模型”：按任务持久保存对话、工具事件、每轮配置/实际 Token 用量和去重草稿版本。提供知识检索、公开页面读取、正式任务及最新草稿读取、Python 静态检查、草稿保存五类工具。管理员使用助手，超级管理员维护模型和密钥；Windows 密钥由服务账号 DPAPI 加密，不写入 Git 或回显。

草稿可下载并带入原创建表单，默认手动验证、通知关闭；新任务关联原对话，已有任务的修改另建独立测试任务。当前没有 AI 自动运行、浏览器接管、正式版本切换、自动修复或全局夜间恢复预算。对话全文会保存，但模型只自动读取最初需求与最近 20 条消息，结构化参数及长对话摘要仍待实现。完整使用说明及用户认可的后续方向见 [AI 任务助手](docs/AI任务助手.md)，状态见 OPT-017。

隔离全套 370 项检查，367 通过、3 项 Windows 符号链接权限跳过；pip check、前端构建通过。真实 DeepSeek 读取公开测试站点、保存两版草稿；经审阅的 V2 使用原创建与串行运行流程成功采集 5 本书，实际 CSV 字段及业务回执核对通过。模型共调用 5 次，返回 46,783 输入及 4,811 输出 tokens。页面验证覆盖草稿带入、对话关联、下载、预算保存、连接测试及普通成员权限，未使用正式任务或业务数据测试。

正式服务在所有队列空闲时备份完整 data、.env、旧页面和 HEAD 源码到本机 `%LOCALAPPDATA%/SpiderFly/Backups/before-ai-20260908-0959`，随后通过既有自启任务加载新后端。调度、队列、环境和 AI 四个 worker 健康；原 tasks、rpa_apps、users、executions 内容与备份一致，数据库完整性正常。源码及构建文件尚未提交或推送 Git。证据和未验证范围见 [本轮验证记录](docs/AI接入验证_2026-09-08.md)。

## 运行记录筛选与分页（2026-09-07）

运行记录新增独立的 `/api/executions/history` 查询，按任务名称、状态、发起人和北京时间提交日期范围在数据库中过滤，并返回总数、页码和每页 10 条记录；原 `/api/executions` 继续为工作台、队列和时间表提供近期实时快照。页面增加查询、重置、刷新、上一页和下一页，起止日期均包含当天；筛选和翻页不再受近期 100 条快照限制。

新增 4 项接口检查，覆盖 23 条记录的三页结果、运行及排队记录置顶、四项组合筛选、北京时间日期边界和无效条件；隔离全套 354 项检查通过 351 项，3 项因 Windows 符号链接权限跳过。隔离浏览器实测第一页/第二页各 10 条、23 条共 3 页、四项组合筛选得到 6 条、重置恢复全部，浏览器无错误。正式数据库和任务未用于测试，也未运行任务。

## 任务按归属人筛选（2026-09-07）

任务中心筛选栏新增“归属人”，选项从当前任务数据按固定创建者编号去重生成，支持全部成员和缺少创建者的“系统迁移”；当前登录人的选项追加“（我）”。筛选可与任务名称、启用状态、触发方式组合，清空筛选会恢复“全部归属人”。管理员与普通成员均使用前端已取得的公开任务字段，不新增成员列表权限或后端接口。

正式前端构建通过；隔离页面使用系统管理员和普通成员两个归属验证下拉选项、单人筛选、筛选计数、重置及普通成员“（我）”标记，浏览器无错误。未运行任务，未修改正式数据库。

## 任务列表显示归属人（2026-09-07）

任务列表在“最近状态”和“操作”之间新增“归属人”，直接显示后端已有的 `created_by_name`。归属由 `tasks.created_by` 在创建时确定，编辑、启停和运行不改变归属；历史迁移任务缺少创建者时显示“系统迁移”。运行记录的“发起人”继续表示单次执行的提交者，与任务归属分开。

隔离构建和浏览器验证使用两个不同创建者的合成任务，归属分别显示普通成员和系统管理员；表头顺序、600px 窄屏表格滚动、无页面错误均通过。正式 `frontend/dist` 已重新构建，运行中的服务无需重启即可提供新资源；未修改正式任务、成员或执行记录，未运行任务。

## 上传脚本存放规则与本次 GitHub 交付（2026-09-07）

上传的 Python 入口默认保存在 `data/apps/<应用ID>/<原文件名>.py`，根目录可由 `SPIDERFLY_APPS_DIR` 覆盖；数据库保存绝对路径并由运行器读取。`data/`、真实 `.env`、任务脚本、环境、执行文件、日志和截图属于本机运行资料，不纳入公开仓库。本次同步更新 README、使用说明和 AGENTS 维护入口，明确脚本位置、禁止绕过页面直接替换、备份与迁移范围。

本次 GitHub 提交范围还包括自上次提交后已经完成并验证的群 Webhook、登录提示、成员管理和超级管理员权限、密码规则、强制停止、运行队列、失败通知附图及对应前后端、测试、发布页面和维护记录。正式运行数据和凭据不提交；推送代码不等于重启正式服务，失败截图功能仍需在任务空闲时重启服务后才会加载。

## 失败通知附图（2026-09-07）

创建和编辑任务的“最终通知”新增默认关闭的 `failure_screenshot`（失败附带截图）。数据库增量迁移使既有任务保持关闭；失败通知关闭时前端置灰截图选项并保留选择，后端同时检查两个开关。任务失败或业务失败时在公共工作区清理前截取宿主机前台窗口；超时在终止进程前截图。成功和取消不截图；仅收尾清理失败时尝试截取收尾窗口。截图保存在本次 artifacts 下的唯一 `failure-*.jpg` 文件，可沿用“本次文件”下载。当前按任务设置触发，未实现自动定位任务窗口或按异常类型选择截图。

错误日志始终保留，长通知摘要保留末尾异常信息。个人与群 Webhook 的失败消息都可包含图片；群附图额外需要应用凭证及图片上传权限。截图或上传失败仍发送错误原因和提示，不重发最终消息、不切换收件人。未配置通知时已成功保存的截图仍可在本次文件下载。

Python 3.12 隔离副本安装后端依赖与 0.1.4 wheel，pip check 通过；全套 350 项检查中 347 通过、3 项 Windows 符号链接权限不足跳过。新增检查覆盖创建/编辑持久化与旧库默认、普通失败/超时/业务失败的截图时机、关闭开关、成功不截图、截图和上传失败回退、群/个人附图和长报错摘要。前端 npm ci / npm run build 通过，隔离浏览器验证管理员创建表单、管理员和普通成员编辑保存及回显、置灰/恢复与 600px 窄屏。截图采集使用合成图片替身，飞书发送使用模拟接口；未验证真实桌面捕获与飞书实际送达。

失败截图部分已完成源码和随仓库构建文件，未为该功能重启正式服务、运行真实任务或发送真实附图通知。验证副本位于本机临时目录 `spiderfly-screenshot-check-243dbca5c7894abbb70048a9081b937a`，保留测试日志和页面截图用于复核。


## 运行队列宽屏布局修正（2026-09-07）

队列面板补齐与标签栏及其他页面一致的 1180px 最大宽度和水平居中。隔离页面在 1920px 宽屏复现队列宽 1584px、标签栏宽 1180px；修复后二者左右边缘一致。390px 窄屏同样对齐且无横向溢出。前端构建通过，线上资源逐字节一致，服务健康，无后端重启或业务任务操作。

## 运行队列与查看入口（2026-09-07）

修复任务列表“进行中”按钮被禁用、运行记录排除运行中记录导致无法重新打开日志的问题。任务中心改为独立“查看运行”入口，优先打开本任务正在运行的记录，其次打开最早排队记录，保留禁止重复提交的规则。运行中心最左新增“运行队列”并默认选中，显示运行任务和按顺序排列的等待任务、实时数量与排队名次，可打开详情停止或取消；任务时间表和全部运行记录作为其他标签保留。

隔离页面复现旧按钮 disabled；修复后验证普通成员可查看且没有停止按钮、超级管理员从队列停止任务、一个运行加两个排队的排序、取消前一项后后一项名次更新、空队列及已取消状态。隔离检查 339 项（336 通过、3 项 Windows 符号链接权限跳过），最新前端构建通过。本次仅更新网页资源，无后端改动或服务重启；不操作正式业务任务。

## 强制停止运行（2026-09-07）

新增管理员/超级管理员专用 `POST /api/executions/{id}/stop`，202 表示请求已接受，运行详情提供 `stop_requested`。执行器通过事件接收请求，仅终止对应 Python 进程树；准备阶段收到请求则不启动进程。不会通过取消队列 worker 来停止单次运行，进程退出并完成清理后才记录 `cancelled` / `FORCE_STOPPED` 并继续队列。重复请求幂等，排队/已结束/无当前执行器的记录拒绝停止；审计记录操作人，最终通知遵循失败开关。前端提供确认弹窗和正在停止状态。

隔离全套检查 339 项：336 通过、3 项因 Windows 符号链接权限不足跳过，前端构建通过。新增 6 项检查覆盖 HTTP 权限/幂等/审计、准备阶段停止、结束竞态、未确认退出时保持等待，以及真实 Windows 父子进程终止、日志保留和后续队列继续。应用内浏览器完成模拟卡住脚本的运行、确认停止、正在停止、已取消及原因展示；普通成员无停止按钮，所有隔离任务已结束，通知关闭。正式服务在队列和环境构建空闲时备份至 `data/backups/before-force-stop/spiderfly.db` 后通过本机自启任务重启，9356 健康检查和三个 worker 正常，新前端资源与验证构建逐字节一致；账号、任务和运行记录与备份一致。未运行或停止真实业务任务，未提交/推送 Git。

## 指定账号密码更新（2026-09-07）

按用户明确要求更新本机三个既有账号的密码，撤销旧会话并保留角色和其他资料。超级管理员初始密码调整为 `admin`；登录及当前密码字段接受现有短密码，新建成员、成员重置和主动设置的新密码仍至少 6 个字符，首次登录不强制改密。实际业务账号凭据不写入版本库。隔离全套检查 333 项：330 通过、3 项 Windows 符号链接权限不足跳过；验证五位初始密码可登录及后续正常改密。正式库已备份至 `data/backups/before-requested-password-reset/spiderfly.db` 后更新，逐个核验新密码哈希、会话撤销与角色保留；本机自启任务重启后局域网 9356 健康检查正常。前端无变化，未运行真实任务或推送 Git。

## 超级管理员权限（2026-09-07）

按最新要求，原始 id=1 的 `admin（系统管理员）` 升级为唯一 `super_admin`，前端标签为“超级管理员”。只有超级管理员可创建、编辑、重置密码和删除成员；管理员可以查看团队成员列表；其他管理员保留任务、系统信息与审计权限，但禁止自行修改密码，前后端均限制；成员管理对其他管理员仅显示只读列表。普通成员原有权限不变。角色下拉只允许为其他成员分配管理员或普通成员，唯一索引及自身保护防止添加第二个超级管理员或删除、停用、降权现有超级管理员。迁移保留所有密码和其他成员角色。密码仍至少六位、新账号默认 `123321`、无需首次改密；本节取代下方历史中的管理员成员管理规则。

本次隔离检查 333 项：330 通过、3 项 Windows 符号链接权限不足跳过；前端构建通过。隔离浏览器验证管理员只读成员列表，无创建/编辑/删除或改密入口；超级管理员标签、成员管理按钮及自身改密入口正常。正式服务已确认空闲并备份至 `data/backups/before-super-admin/spiderfly.db`，通过本机自启任务重启上线，9356 健康检查及三个 worker 正常。只升级原始 admin，密码哈希、其他成员角色和状态均与备份一致；线上构建内容核对一致。原始 admin 的旧会话因角色变更失效，需要重新登录；其他成员刷新页面生效。未运行真实任务，未提交或推送 Git。

## 密码规则调整（2026-09-07）

按用户最新要求，密码最低 6 个字符，新建成员与首次初始化管理员默认密码为 `123321`。管理员重置的是正式登录密码，不再要求首次改密；既有成员仅解除首次改密标记，保留密码哈希。前后端取消首次改密拦截，保留用户主动修改密码入口；重置后仍撤销旧会话。此节取代下方历史记录中的临时密码和首次强制改密规则。

隔离全套检查 329 项，326 通过、3 项因 Windows 符号链接权限不足跳过；前端构建通过。隔离浏览器验证管理员和普通成员均用六位初始密码直接进入工作台，成员表单显示正式重置密码与六位要求。已在队列和环境构建空闲时备份至 `data/backups/before-password-policy/spiderfly.db`，通过本机自启任务重启并上线；9356 健康检查和三个 worker 正常，线上资源与构建一致。逐项核对所有既有账号、角色和密码哈希保持不变，旧首次改密标记已全部清零。未运行真实业务任务，未推送 GitHub。

## 成员管理（2026-09-07）

按用户最终要求只保留管理员与普通成员两种角色，管理员拥有最高权限。新增成员 PATCH/DELETE 接口及编辑、密码重置、启停和删除确认界面。用户名规范化并防止重复；管理员不能自删、自停用或自降权，自己的密码沿用改密入口。角色、账号、密码或启用状态变更撤销目标旧会话；重置密码强制首次改密。删除采用停用并隐藏账号，保留历史外键和用户名。数据库只新增 `users.deleted_at`、`users.version`，不改现有角色和密码。更新使用事务与版本检查，自助改密重新核验数据库当前密码，防止覆盖管理员刚完成的重置。

隔离全套 `python -X utf8 -m unittest discover -s tests -v` 共 325 项：322 通过，3 项因 Windows 符号链接权限不足跳过。新增 11 项真实 ASGI/数据库检查覆盖权限、重置后登录、删除、会话失效、自保护和并发版本冲突；原中文改密检查改为验证实际数据库结果。前端构建通过，隔离浏览器验证管理员编辑保存、删除确认及自身保护、普通成员无管理入口；密码重置和删除执行由隔离接口测试验证。

已确认正式队列及环境构建空闲，备份数据库至本机 `data/backups/before-member-management/spiderfly.db`，更新前端构建并通过本机自启任务重启服务。局域网 9356 健康检查正常，三个 worker 均运行，新前端资源 HTTP 内容与构建文件一致。迁移后逐项核对现有成员账号、名称、密码哈希、角色、启用与首次改密状态，与备份一致；新增字段已生效。正式浏览器已验证成员管理页面显示三名原有成员、两种角色及编辑/删除入口；未修改真实成员资料，未运行业务任务，未提交或推送 Git。

## 登录失败提示（2026-09-07）

修复登录前看不到错误通知的问题：Toast 原本位于只有登录后才渲染的 `app-shell` 内，现移到顶层，连接检查、登录页和已登录页面均可显示。角色权限、密码校验与账号数据未改；普通成员仍可登录。隔离浏览器已复现错误密码返回 401 却没有提示，修复后错误原因可见，正确密码可进入普通成员首次改密页面。前端构建成功并更新本机 `frontend/dist`，已通过局域网 HTTP 核对新资源内容；仅页面变更，无需重启调度器，未推送 GitHub。

## 群 Webhook 通知（2026-09-07）

新增本机配置 `FEISHU_WEBHOOK_URL`，优先使用群自定义机器人发送最终结果；不填写时保留应用机器人个人通知。任务通知开关、错误摘要和人工处理链接沿用现有规则。Webhook 网络错误隐藏完整地址，失败不重试、不回退个人通知；支持可选的 `FEISHU_WEBHOOK_SECRET` 签名校验。真实地址与签名密钥只保存到被 Git 忽略的 `.env`。

本次基于 `0d69061` 的未提交修改已在 Python 3.12.6 隔离副本中执行 `python -X utf8 -m unittest discover -s tests -v`：314 项中 311 项通过，3 项因 Windows 符号链接权限不足跳过；使用指令 wheel 0.1.4，`pip check` 通过。已按用户授权向目标 Webhook 发送明确标记的文字与带签名错误摘要测试，均获 `code=0`。本机服务在确认队列和环境构建空闲、备份数据库后重启，监听 9356，健康接口的三个 worker 均运行，首页 HTTP 200。未运行真实业务任务，前端源码与构建产物未改，未提交或推送 Git。隔离原始日志位置保存在本机 `data/setup/webhook-verification-root.txt`。

SpiderFly 用统一队列调度本机 Python 任务，并保存每次运行的结果与通知状态。

## 当前调度规则（2026-09-06）

用户明确要求手动运行不影响计划运行。已调整为每日/每周到点产生独立计划执行，允许同一任务的多次计划记录与已有手动记录共同排队；仍由单 worker 按入队顺序执行。手动重复提交保护保持原样，停用仍取消全部待执行记录。

数据库启动迁移替换原“每任务仅一条活跃执行”的索引，重启不再合并计划记录；任务摘要在取消、完成或恢复时优先显示尚存的运行/排队状态。公共指令包、前端源码及构建结果不变。测试及正式服务加载情况见本次维护记录；提交与推送尚未执行。

工作台“今日运行”的统计边界已改为北京时间 00:00 至次日 00:00，避免午夜到早上 8 点之间混入前一天记录。该调整不改变任务调度或执行时间。

## 维护入口（2026-09-05）

AI 接手先读 [AGENTS.md](AGENTS.md)，测试与调试按 [开发与验证](docs/开发与验证.md) 准备独立副本。README 与使用说明已统一 Python 3.12 基线、指令包安装前置条件、全局超时配置和保留绝对路径的迁移限制；操作手册中的阶段性验收叙述已精简，历史证据继续保留在下文及检查记录中。

本次用户授权完善文档并推送 GitHub，范围为 SpiderFly；相邻 task_scheduler 使用独立 Gitee 远端，不在本次提交范围。公共包仍为 0.1.4，六条指令与运行代码未变，正式服务未因本次文档维护重启。提交、推送及验证结果以本次实际记录为准，不以历史段落中的“本轮”判断当前状态。

新开发指南已直接提取并在隔离副本中运行，292 项测试中 289 项通过、3 项因 Windows 符号链接权限不足跳过；文档链接与命令语法核对通过。此次没有重新进行页面或真实业务验收，详细结果记录在维护约定的 2026-09-05 条目中。

## 维护文档入口

- [维护入口](AGENTS.md)：仓库边界、接手顺序、修改约束和交付检查。
- [开发与验证](docs/开发与验证.md)：隔离副本、Python 与 wheel 安装、测试命令和页面验收方法。
- [项目优化台账](docs/项目优化台账.md)：优化状态、代码依据、建议方案和验收方式。
- [指令化演进设计](docs/指令化演进设计.md)：本轮认可的方向，以及尚待验证的设计建议。
- [维护约定与决策记录](docs/维护约定与决策记录.md)：如何随改动更新文档，避免把讨论写成已实现功能。
- [项目检查记录](docs/项目检查记录_2026-09-03.md)：七处修复、验证证据、跳过项和服务加载状态。
- [指令开发起步](docs/指令开发起步.md)：已实现的独立指令定义、调用、演示与限制。
- [指令目录](docs/指令目录.md)：按用途查找已有指令，查看参数、返回值和可复制调用示例。
- [平台指令示例](docs/平台指令示例.md)：上传单文件示例、声明指令包依赖和找到结果文件。
- [平台筛选示例](docs/平台筛选示例.md)：上传 Excel，保留待处理行，再下载输入和结果。
- [平台均值示例](docs/平台均值示例.md)：通过项目指令逐行计算待处理金额平均数，保留原表并追加结果列。

## 既有功能与验证记录（截至 2026-09-03）

以下段落保留各阶段事实，其中的服务状态、测试数量和“本轮”均指当时的检查。后续维护以文首当前入口、实际代码和新的验证记录为准。

当前节奏以用户最新决定为准：现阶段测试例子已经够用，暂停增加指令和专用功能，后续按实际需要再加。用户随后要求从上到下检查项目，本轮已完成检查及七处缺陷修复，详见[项目检查记录](docs/项目检查记录_2026-09-03.md)。新增 11 项回归检查；全套 292 项中 289 项通过，3 项因 Windows 符号链接权限不足跳过。已验证隔离页面上传、创建环境、运行、日志及下载；正式服务未重启，后端修复需在服务空闲时重启后加载。

此前按一件一件的节奏完成了 OPT-001-A 至 N 及 OPT-003-A，包括指令基础、Excel 读写、单列筛选、完整示例、最小安装包、平台任务接入、上传 Excel 处理、指令目录、运行文件下载、平台筛选示例、均值指令与保留原表测试及通用 Python 任务入口。公共测试包仍为 0.1.4，六条指令不变；台账里的候选项没有自动获得实施授权，不继续执行历史记录中的“下一件”建议。

维护交接：完整检查的证据目录与三项跳过测试见检查记录；复测准备见[维护约定](docs/维护约定与决策记录.md#复测与交接)。当前是本地代码修复完成、隔离验证完成，正式服务加载尚未执行。修改 flows/ 不会自动替换已上传任务的脚本；按现有平台能力，应另建不同名称的任务验证新入口。后文为各阶段历史记录，其中的版本和“下一件”不覆盖本段当前状态。

用户随后授权提交并更新 GitHub。本次提交范围为已完成的公共指令、独立流程、平台文件下载、检查修复与维护文档；为满足示例任务的固定版本安装，明确纳入原五个 wheel。真实 .env、data、共享工作区、环境与测试材料仍排除。GitHub 更新不代表正式服务已重启，也不代表已发布到 PyPI；此前“未提交或发布”的段落描述当时状态。

用户补充：每个功能先做简单实用的最小范围，等项目稳定后再逐步增加能力。第七件让姓名任务读取创建时上传的 .xlsx，保存本次输入并另存结果；缺上传不回退虚构数据。第八件在日志弹窗新增“本次文件”，执行结束后可下载 artifacts 内的普通文件，失败时保存的输入同样可取。有文件不等于业务成功；权限沿用共享运行记录，已完成首次改密的普通成员也可访问。

第八件后端 48 项检查中 47 项通过、1 项因本机符号链接创建权限不足跳过；前端构建、隔离页面四种记录状态及窄屏验证完成。浏览器实际触发下载，另通过 HTTP 下载三份 Excel 核对与存档一致，结果姓名正确。未重启真实服务、运行真实任务或发送通知；部署需包含更新的后端及 frontend/dist 并在空闲时重启。

第九件新增简明指令目录，当时包含三条已有指令的用途、参数、返回值、常见错误及五个可复制代码块。直接提取文档中的代码，在仓库外用已安装的 0.1.0 包运行，五个例子全部通过；Excel 独立读回、预期返回和同名文件保护核对通过。该件只调整文档。

第十件新增 `table.filter_equals`，四个输入为完整列名、数据行、筛选列和值，返回全部列名、匹配行及行数。只做单列相等条件：文字精确匹配，数字和文字、布尔值分开，整数与小数可以同值匹配；缺列报错，无匹配为空表。新包为 0.1.1，旧包 0.1.0 保留，原三条指令和姓名流程不变。安装版 13 项筛选及 49 项原指令/流程检查共 62 项通过；目录 7 段和随包说明 2 段代码全部实际运行，Excel 筛选结果及原件保护核对通过。当时未新增平台筛选任务、升级真实任务或启动真实服务。

第十一件新增单文件入口 `examples/instruction_excel_filter_task.py`，复用现有 0.1.1 包读取首张表、保留“状态”完全等于“待处理”的行、另存新表，保存本次输入及业务回执。没有匹配行是成功空表；缺列是失败，保留已复制的输入。11 项入口测试在独立安装环境通过；隔离平台通过带登录校验的上传、入队及下载接口实际验证四次运行：正常、无匹配、缺列、正常重跑，7 次文件下载与存档一致，原上传文件和旧结果未变化。当时未更改指令包、平台页面或真实服务。

下列 Language 描述当前产品的术语和操作方式；未来演进记录在上述文档中，实际交付后再同步调整本文件、README 和使用说明。后续工作以用户最新指示和实际代码为依据。

用户随后提供 `ccc.xlsx`，要求测试项目指令，不要求先提供真实业务资料。已确认按每行分别求平均：“5,2”是两个数，得到 3.5；5.6 是单个数，得到 5.6。原表右侧新增“待处理平均数”，只给待处理行填写，已完成行留空，原数据及其他工作表保留。此前用外部表格工具生成的修改版没有调用项目指令，不能计入 SpiderFly 指令或平台测试。

此前均值校正已完成：代码新增 `math.average`，并为 `excel.write` 增加可选 `template_file`，只在原表右侧追加列。当时指令共五条，包版本为 `0.1.2`；`excel.write` 自身版本为 `0.1.1`，其余指令为 `0.1.0`。新流程 `backend/example_flows/excel_pending_average.py` 通过真实注册器调用读取、逐行求平均和模板写入；平台入口为 `examples/instruction_excel_pending_average_task.py`。旧 `0.1.0`、`0.1.1` wheel 与姓名、筛选任务保留，不自动升级。

安装最终 `0.1.2` wheel 的独立环境中，84 项测试全部通过。实际 `ccc.xlsx` 从注册器外部记录到 `excel.read → math.average → math.average → excel.write`，结果为 `3.5`、`5.6`；隔离平台实际上传建环境、连续运行两次成功，普通成员 GET/HEAD 下载的四份文件与存档一致，原件和其他工作表保留。真实服务未改动，用户明确这是测试，不计入 OPT-011 真实业务验收；同一测试已完成，无需再次索要样表或确认是否重做。安装包摘要与详细记录见[指令开发起步](docs/指令开发起步.md)。

随后用户继续用 ccc.xlsx 测试“待处理总金额 − 已完成总金额”，要求使用 Python 流程。已新增 `examples/instruction_excel_amount_difference.py` 和 [Python 金额差额示例](docs/Python金额差额示例.md)，复用 `excel.read` 及两次 `table.filter_equals`；汇总和相减用 Python Decimal，未新增求和/减法指令，指令仍五条、包仍 0.1.2。默认状态与金额列可通过命令行参数更换。安装版实际运行原表得到 12.6 − 12.3 = 0.3，外部调用记录核对读取 3 行、两组分别 2/1 行，原件摘要不变；6 项针对性测试通过。当时只增加普通 Python 流程及说明，未新增平台任务或重建安装包。

用户确认测试完毕并进入下一环节后，完成 OPT-001-L：新增 `spiderfly_instructions.task` 通用入口，业务函数接收 TaskContext 并返回 TaskResult；公共代码留存完整输入、准备输出目录、报告成功或失败。金额差额实现迁入 `example_flows.excel_amount_difference`，命令行与新平台入口复用同一实现；当前包版本 0.1.3，五条指令不变，旧包保留。8 项入口测试及 6 项金额流程测试共 14 项通过；隔离平台创建三个任务，四次执行中三次成功、非法金额一次按预期失败，六份文件下载核对通过。真实服务未改。使用方法见[Python 任务模板](docs/Python任务模板.md)。这一阶段提出的专用业务参数页面建议已被用户纠正并撤回：用户提供的金额和均值仅是检验复用能力的例子。

用户进一步明确：公共指令包目前先用于测试可以保留；同意先落实“新增业务流程不牵动公共包”。已完成 OPT-001-M，新增 flows/ 独立目录、模板、两个单文件验证流程和说明，全部只导入公共指令及运行辅助。现有 0.1.3 测试包及包内旧示例、旧入口保留兼容，公共包、平台代码、打包配置不变。本地与平台模式禁用 example_flows 等业务模块导入后，六项针对性检查通过；隔离平台运行两个新流程及旧任务前后回归，共四次成功，八份下载一致，40 个既有文件摘要保持不变。修改默认规则仅改流程文件，本地与平台模式同步生效。新增流程从 [flows/README](flows/README.md) 开始，不再把测试例子当作产品方向。随后用户同意继续，文件列表指令的完成情况见下一段。

已完成 OPT-001-N：新增 `file.list`，输入文件夹与可选文件名通配符，返回当前一层普通文件的排序后绝对路径和数量；空目录成功，路径或读取错误明确失败。新增独立流程 `flows/list_files.py`，本地参数调用或通过公共入口保存平台任务清单，目录必须在执行机器上可访问。新增公共能力使测试包升至 0.1.4，原五条指令、通用入口、旧包与平台代码保留原样。安装版 10 项文件指令、3 项独立流程及 23 项基础检查共 36 项通过，另有 6 项原独立流程在新包环境回归通过，累计 42 项；本轮验证平台入口及回执协议，没有启动真实服务或重做网页上传下载验收。详细用法见[文件列表](docs/指令目录.md#获取文件列表)。

## Language

**任务**:
管理员在“管理中心 → 创建任务”一次填写名称、说明、入口 `.py`、依赖、可选 Excel 模板、触发方式和时间、启用状态及成功失败通知后得到的完整运行单元。点击一次即可创建，不需要再编辑补全。每个任务独享自己的程序文件和 `.venv`，不再单独创建、绑定或删除程序。
_Avoid_: 程序库、未绑定程序、先建程序再绑任务、创建后再补设置、机器人、影刀应用、Agent

**任务中心**:
查看、运行、以后修改运行设置、修复失败环境和彻底删除任务的唯一入口。编辑不能替换脚本、依赖或模板，需要更换时使用不同名称新建任务；旧任务可按需要保留，删除前先保存所需记录与结果。
_Avoid_: 程序列表、应用库

**触发规则**:
决定任务何时产生执行记录，包括手动、每日和每周。
_Avoid_: 运行时间

**执行记录**:
任务的一次实际运行，包含来源、状态、日志、耗时、退出码与通知结果。
_Avoid_: 任务、计划
