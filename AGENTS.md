# SpiderFly 维护入口

本文件适用于本仓库。用户当前指示优先于历史计划；先完成已授权的工作，不把优化台账中的候选项当作实施授权。

## 接手顺序

1. 阅读 [README](README.md) 和 [CONTEXT](CONTEXT.md)，确认产品边界与当前状态。
2. 查看 `git status --short`、当前分支和远端，区分已有改动与本次工作。不要覆盖、撤销或顺带提交他人的改动。
3. 按任务阅读 [使用说明](使用说明.md)、[开发与验证](docs/开发与验证.md) 或 [独立流程说明](flows/README.md)。涉及设计时再查 [优化台账](docs/项目优化台账.md) 和 [维护约定](docs/维护约定与决策记录.md)。
4. 对照实际代码核实文档；历史测试结果只证明当时的版本，不代表当前服务已加载对应代码。

## 项目边界

- SpiderFly 在单台 Windows 宿主机上串行执行 Python 任务，成员通过浏览器访问。
- 相邻的 `task_scheduler` 是独立的多机 Agent 调度项目，拥有自己的仓库和部署方式。不要混用其登录、Cron、端口、Agent 或服务器更新说明。
- 当前保留六条公共指令和现有验证例子，已接入共享 DeepSeek 服务；各任务的对话、草稿和维护记录分别保存。Python 仍是任务入口，正常成功的执行不调用模型。当前能力与后续范围见 [AI 任务助手](docs/AI任务助手.md)。
- 业务规则放在 `flows/`；已有公共能力够用时，只修改独立流程，不把样表题目发展成专用产品功能。

## 自动维护与版本

- 用户已明确：任务失败后默认维护，验证通过后直接启用修复并自动重跑一次，完成后告知；不提供单任务维护开关，不为同一次已授权修复再次要求确认。普通脚本不能仅因缺少业务验收声明而停留在候选阶段。
- 无验收声明时，按现有流程检查最小修复、用冻结输入复现同类错误、确认修复后运行通过；有原业务验收时继续执行冻结条件。不得删除调用、校验或吞掉异常来假装修好，也不把运行通过写成未声明的业务规则已验收。
- 维护试跑与任务执行共用串行队列。正在运行的任务先结束，再优先验证已就绪修复；同任务同源、依赖及输入一致的待运行记录自动改用新版本，保留发起人、触发信息及参数并记录衔接。不要把普通排队情况作为“有碰撞”交回用户处理。
- 修复发布、重跑入队和记录关联必须在同一事务完成。优先沿用最早的兼容排队记录，否则新建“修复重跑”执行；原失败记录不改成成功，原计划不额外扣次。重跑完成后才发维护结果通知，日志区分等待、运行、成功、失败和取消，并提供重跑日志入口。服务重启不重复入队，升级不补跑历史修复；用户已停用的任务不自动重跑。
- 名称、说明及计划编辑不阻止修复。真正更换源码、依赖、模板或主动回退后，结束旧版本维护，新版本失败时继续维护；旧候选不能覆盖新内容。代码版本按内容去重，修复保存新文件并切换引用，不覆盖原上传文件或为每版重建环境。
- 维护结论默认用一句“把 A 改为 B，解决 C”说明具体修改及效果，仅验证通过后补充运行结果；版本状态单独展示。执行日志、维护记录和通知采用相同简短结论，“技术详情”展开原始过程及已确认用量；原执行的失败状态及 stdout/stderr 保持历史事实。维护结果持久保存，任务归属人和超级管理员各有未读通知。助手中的入口名称是“维护与版本”，历史版本与回退在其中的“代码版本”展开。
- 相同故障去重、滚动预算及停止约束继续生效。当前默认单任务 24 小时最多 45 分钟，全局 2 小时 / 200,000 tokens；自动重跑受剩余维护预算及 120 秒上限约束，预留与实际耗时均计入预算。一次修复只自动重跑一次，重跑失败不得触发修复重跑循环，保留实际结果并通知。配置由超级管理员在“AI 模型 → 调用与维护预算”维护。
- 修复试跑及启用后的执行使用同一 WSL Ubuntu 环境：普通维护用 `readonly-v1`，声明的公开采集任务用 `collection-v1`，单次运行最多 120 秒；原任务的 Windows 独立环境与此不同。通用登录浏览器、Windows GUI、其他未支持依赖的自动修复仍未实现。具体安装与依赖范围见 [受限运行环境](docs/AI任务助手.md#受限运行环境的准备与范围)。

## AI 采集

- 现有 AI 入口通过 test_collection 对保存的单文件 Python 草稿试跑，必须使用同一串行队列，不能从 AI worker 并发启动业务代码。试跑结果绑定草稿，不能把旧版本通过状态展示在新源码上。
- collection-v1 使用 Scrapling FetcherSession、DynamicSession、受控 GET 和 Playwright，前十行内声明 `# spiderfly-runtime: collection-v1`；新任务入队后使用该环境，修复和重跑不切回 Windows。仅用户提供且写入验收的公开域名可访问，页面中出现的新域名不能自行授权。
- 生成脚本通过内置 spiderfly_collection 接口采集，保持 Python 入口；不增加 Node.js 或 DP 入口，不把受控采集解释为 Scrapling 全接口或稳定风控绕过。安装与限制见 [AI 采集环境](docs/AI采集环境.md)。

- 核心环境安装目录为 spiderfly-collection-v2，脚本标记保持 collection-v1 兼容。原生请求通过可信通道固定 DNS/IP、逐跳验权；不允许生成代码直接联网。crawl 提供每页原子断点与 1–4 并发，平台队列仍串行。
- 正式断点按任务及源码/依赖/验收/域名指纹隔离，草稿另存，维护验证只用临时断点。成功且原结果验收通过才清理，下一次计划重新采集；不能把断点恢复写成自动无限续跑。恢复不复用 Cookie。知识、日志中须明确原生引擎与平台适配层的区别。

### 对话要求如何落到脚本

- 用户通过对话指定采集方式、并发数、字段、筛选、排序和恢复要求；Agent 必须落实到保存的 Python 源码及对应接口参数，不以口头答应代替实现。保存后试跑，说明实际配置和结果；超出能力时说明限制，不悄悄改小目标。
- 并发指单个任务内的 HTTP 取页，crawl 支持 1–4，默认 2。用户未指定时根据页面依赖选择，不能把所有采集都默认解释为并发；只有一个待采链接或需要逐页获取下一页时，可能没有可重叠请求。同一命名会话内串行，不承诺浏览器操作或多个任务并行。
- 对话先保存草稿；用户明确修改既有任务时通过 submit_task_update 验证后启用，不直接修改计划。创建并保存任务后，定时执行沿用已保存脚本及参数，正常运行不重新询问模型。后续对话修改先保存新草稿并验证，再沿用现有任务创建/版本流程。

## 任务需求与版本

完整规则见 [任务需求与版本方案](docs/任务需求与版本方案.md)。版本入口已实现，正式服务加载状态以 CONTEXT 为准。当前仅支持准备好的 readonly-v1 / collection-v1 依赖，未知环境明确失败；不承诺任意 Windows 脚本都可受限验证。

- 分开记录用户要求、代码推断和未说明；无需求说明不阻止正常运行，推断不成为用户确认的业务验收。
- 需求变更与报错修复分层；明确修改不重复确认，修复不放宽原条件，上传冲突只问具体差异。
- AI 修改及人工上传最终使用同一版本验证流程。支持任意历史 PY 下载、原任务更新脚本；编号始终递增，回退后不复用编号，上传原文件不覆盖。
- 启用与回退联动需求、参数、源码和环境；正在运行的保持原快照，待运行记录同步新快照，计划及历史保留。依赖相同复用环境，依赖变化先匹配受限环境、验证再切换；保留历史版本引用的原生环境。
- 上传新版本报错也默认维护，修复另存版本；修不好不启用，新旧版本的实际错误均保留。最终维护失败接入已配置群通知，由用户团队人工处理，保留原计划，不增加自动停用或升级流程；继续执行去重和预算限制。
- 默认只展示一句具体修改与实际结果，详情折叠。当前状态、待实现功能和历史验证必须区分。

## 代码位置

| 路径 | 主要职责 |
| --- | --- |
| `backend/app/main.py`、`security.py` | HTTP 接口、账号与权限 |
| `backend/app/ai_agent.py`、`ai_settings.py`、`ai_tools.py`、`backend/ai_knowledge/` | DeepSeek 对话、模型配置、受限开发工具和知识 |
| `backend/app/task_versions.py`、`maintenance.py`、`maintenance_runtime.py`、`backend/sandbox/` | 失败维护、版本和通知、外部验收、WSL 受限执行 |
| `backend/app/database.py`、`scheduling.py` | 数据、触发规则和持久队列 |
| `backend/app/environments.py`、`instruction_packages.py` | 上传文件、独立 Python 环境和本地 wheel 安装 |
| `backend/app/runner.py`、`host_runtime.py` | 串行执行、资源检查和工作区清理 |
| `backend/app/execution_results.py`、`execution_artifacts.py`、`feishu.py` | 业务回执、文件下载与通知 |
| `backend/spiderfly_instructions/` | 公共指令及任务入口辅助 |
| `flows/` | 可单文件上传的独立业务流程 |
| `examples/`、`backend/example_flows/` | 兼容示例，修改时保留旧入口行为 |
| `frontend/src/`、`frontend/dist/` | 页面源码与随仓库交付的构建结果 |
| `launcher/`、`scripts/`、`start.bat` | Windows 启动器、构建与首次安装 |

## 运行资料位置

- 上传的 Python 入口默认保存在 `data/apps/<应用ID>/<原文件名>.py`；实际根目录由 `SPIDERFLY_APPS_DIR` 决定，未配置时为 `SPIDERFLY_DATA_DIR/apps`。
- 数据库的 `rpa_apps.script_path` 和任务快照保存绝对路径，运行器按记录路径执行。不要把 `flows/` 或 `examples/` 中的源码误认为已经上传到正式任务，也不要直接修改 `data/apps/` 绕过任务创建、审计和环境管理。
- `data/apps/`、`data/envs/`、`data/executions/`、`data/maintenance/`、数据库和日志都是本机运行资料，受 `.gitignore` 排除，不得提交。离线迁移、人工直接改写数据文件或批量清理资料前，先安排空闲、停止服务并备份完整 `data/` 及 `.env` 指向的外部目录。普通只读排查不需要停服；平台正常版本发布与删除操作沿用其锁和审计流程。
- 对话、源码版本、模板快照、维护结果和 DPAPI 加密的模型密钥也属于备份范围。换电脑或服务账号后重新配置密钥并准备对应 WSL 运行环境，不能只复制 Windows `.venv`；不得在源码、日志、文档或回复中回显真实密钥。

## 修改约束

- 使用 UTF-8，保留中文内容与无关代码。只修改当前需求涉及的文件。
- 生产调度保持单进程、单 worker，不用 `--reload` 承担真实任务。调试必须使用隔离副本；单独换端口不能隔离数据或产品级实例锁。
- 配置会读取仓库根目录 `.env`。测试不得导入正式目录的 app，也不得复用真实数据库、公共工作区、通知接收人或业务账号。
- 工作区在任务前后清空；需要下载或长期留存的结果放入本次产物目录。不要把用户资料复制进去做临时测试。
- Excel 的保存与 Excel/浏览器的关闭由业务脚本负责；当前不增加平台自动保存、资源接管或上传脚本包装。生成脚本应按业务要求处理异常及 finally 收尾，只关闭本次创建的资源。
- AI 生成的修复代码只能在隔离运行环境试跑。维护试跑与业务运行共用串行队列；通过验收的修复后续也使用相同受限环境，不转为未经验证的 Windows 直接执行。模型不得修改冻结的业务验收。未确认的模型请求按预留 Token 计入维护限额，不编造实际用量。
- 失败截图属于本次运行产物：只有任务同时开启失败通知和“失败附带截图”时才尝试截取宿主机前台窗口。错误日志始终保存；截图或飞书上传失败不得覆盖原始错误或改变任务结果。
- `tasks.created_by` 是固定任务归属，任务列表展示 `created_by_name`；编辑、启停、运行只更新各自的操作人与审计记录，不得把归属改成后续操作者。缺少创建者的历史迁移任务显示“系统迁移”。
- `/api/executions` 保留为队列、工作台和时间表使用的近期实时快照；运行记录页面使用 `/api/executions/history` 在后端筛选并按每页 10 条分页。不要改成只对前端已载入的 100 条记录切片，否则旧记录会无法查询。
- 修改 `flows/` 不会替换已上传任务。开发验证在隔离副本使用合成任务，不借用真实任务试错；用户明确指定修复真实任务时，按已授权范围使用版本维护流程完成修正、验证和记录，不因“必须另建测试任务”搁置修复。
- 当前公共包版本在 `backend/pyproject.toml` 中维护，旧任务按原声明使用 wheel。不要覆盖旧 wheel 或自动升级旧任务；公共包有改动时按新版本构建并验证安装后的行为。
- 前端源码改变时同步验证并更新 `frontend/dist/`；启动器源码改变时核对构建出的 EXE。纯文档修改不需要重新构建二者。

## 验证与交付

- 纯文档：核对事实、示例、文件链接、Markdown 锚点和差异格式。
- 后端或流程：按 [开发与验证](docs/开发与验证.md) 准备 Python 3.12 隔离环境，安装后端依赖和指定 wheel，再运行相关测试。涉及多个模块或发布运行代码时运行全套检查。
- 页面行为：验证管理员和普通成员的相关操作、错误提示及需要的上传下载行为；自动测试不能替代真实页面验收。
- 测试范围与改动相称。记录执行过的命令、结果及跳过原因，不把文档核对写成业务验收，不为纯文字修改运行真实任务。
- 操作或实现行为改变时同步 README、使用说明和受影响的专门指南；当前状态放 CONTEXT，历史证据放带日期的检查记录，避免重复堆到用户手册。
- 提交前检查暂存差异，只暂存本次文件。真实 `.env`、`data/`、业务文件、日志、账号、环境和缓存不得提交；允许提交的 wheel 以 `.gitignore` 的明确清单为准。
- 用户授权推送后，核对目标远端与分支再正常推送，不强制覆盖远端历史。提交到 GitHub 和重启正式服务是两件事，分别报告结果。

## 表达

使用简明中文，先说结果，再说明证据与限制。复杂关系适合用表格或图示；不堆砌口号、修饰词和重复总结。保留“任务”“触发规则”“执行记录”等现有页面术语。

页面保持清爽：默认呈现当前状态、结果和必要操作，历史草稿、用量、维护版本及预算按需展开。AI 对话回复必须完整显示，长内容由消息区域滚动承载；模型默认用三句话以内说明实际结果和必要下一步。完整错误及修改记录仍须可查看。

## 浏览器探索（2026-09-10）

用户已授权 AI 主动探索目标网站。沿用现有 Agent 循环，通过 ai_browser 的固定工具操作独立 Windows Chromium；不在 Windows 主进程执行模型生成的 Python/JavaScript。浏览器可用于页面搜索、筛选和数据提取；登录/验证由用户在宿主机完成，再显式继续。浏览器实际发现的公开站点和资源域名可进入同对话的 fetch_page/test_collection；这是对前文仅手工 URL 范围的补充。生成业务脚本仍走原串行队列，探索会话不等于 WSL 定时脚本具备登录态。浏览器结果、观察和等待状态按对话隔离；关闭或重启后不能声称旧会话仍可用。

原生采集补充：AI 优先使用 ai_scraping 的 Fetcher/无头 StealthyFetcher 和快照提取工具，仅在需要交互时打开可见浏览器。旧观察摘要化以控制上下文；实际未达到数量不得声称完成。原生探索成功不代表 WSL 定时脚本已经支持同样引擎或登录态。

仅探索方法时，以自主观察、工具选择、失败调整和证据复核验收，少量样本即可，不强制采满数量或保存脚本。仅解释已有结果时可引用历史证据，不强制重新采集；区分观察与原因假设。生成、修改或定时运行任务时才沿用草稿及试跑要求。当前完整网络响应仅在会话内保留，重启后不能宣称原始证据已恢复。
