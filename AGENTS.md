# SpiderFly 维护入口

适用于整个仓库。用户当前指示优先；历史方案、优化台账和候选项不构成实施授权。

## 接手顺序

1. 阅读 [README](README.md)、[项目架构](docs/项目架构.md) 和 [CONTEXT](CONTEXT.md)，分清发布代码、本机工作区和正式运行进程。
2. 查看 `git status --short`、当前分支和远端。保留已有改动，按当前任务范围编辑和提交，不把其他工作的改动顺带发布。
3. 涉及任务运行、版本、AI 或维护时，先阅读并遵循 [任务执行约定](docs/任务执行约定.md)。用户操作查 [使用说明](使用说明.md)，验证查 [开发与验证](docs/开发与验证.md)。
4. 用实际代码核实文档。历史测试、Git 提交和本机服务已加载是三种不同状态。

## 三层职责

| 层 | 位置 | 负责什么 |
| --- | --- | --- |
| 平台 | `backend/app/` 与 `frontend/src/` | 账号、任务、队列、环境、版本、日志、AI 和通知 |
| 可选运行库 | `backend/spiderfly_runtime/` | 本次输入副本、产物目录、结果回执与通用文件处理 |
| 业务 | `flows/` 和用户上传的单文件 Python | 具体字段、筛选、计算和采集规则 |

- 单台 Windows 宿主机串行执行任务。相邻多机 Agent 调度项目独立，不能混用其部署、登录或端口说明。
- 新业务直接编写 Python；可以使用标准库、openpyxl 或可选运行库，不新增指令注册表、字符串调度器或专用编排层。
- `flows/` 保持单文件可上传，不导入 `app`、其他业务脚本或历史 `example_flows`。具体样表规则不进入平台和通用运行库。
- 运行库不得导入平台配置、数据库或业务示例。`task.py` 与 `errors.py` 不加载 Excel 依赖；文件处理保留必要类型、列名、原文件保护和结果检查。
- 当前运行库版本由 `backend/pyproject.toml` 管理，只打包 `spiderfly_runtime`，发布至 `release/runtime/`。
- 旧指令源码已移除。`release/instructions/` 的 0.1.0–0.1.4 wheel 和 `examples/` 旧入口仅保留兼容，不覆盖 wheel、不自动升级旧任务。

## 平台模块入口

| 职责 | 主要文件（相对 `backend/app/`） |
| --- | --- |
| HTTP、账号与权限 | `main.py`、`security.py`、`schemas.py` |
| 数据、触发和持久队列 | `database.py`、`scheduling.py`、`main.py` |
| 执行与宿主机资源 | `runner.py`、`host_runtime.py`、`instance_lock.py` |
| 上传与环境安装 | `environments.py`、`local_packages.py` |
| 结果与通知 | `execution_results.py`、`execution_artifacts.py`、`feishu.py` |
| AI 对话与网页探索 | `ai_agent.py`、`ai_settings.py`、`ai_tools.py`、`ai_browser.py`、`ai_scraping.py` |
| 版本、修复与受限验证 | `task_versions.py`、`maintenance.py`、`maintenance_runtime.py`、`collection.py` |

受限环境实现位于 `backend/sandbox/`，知识位于 `backend/ai_knowledge/`。启动入口为 `start.bat`、`launcher/` 和 `scripts/`。

`main.py` 和 `App.vue` 仍有职责集中，尚未完成内部拆分；不要把三层边界整理描述为所有模块都已重构。

## 执行与维护的关键约束

- 生产调度单进程、单 worker，不能用 `--reload` 承担正式任务。业务执行、维护验证和采集试跑共用串行队列。
- 正常执行已保存脚本不调用模型。故障默认维护，按冻结条件验证通过后启用修复并自动重跑一次；原失败记录保留，不触发修复重跑循环。
- 发布修复、重跑入队和记录关联保持同一事务。停止、停用、源码变化、版本回退及预算规则完整遵循 [任务执行约定](docs/任务执行约定.md)。
- AI 生成代码只在准备好的受限环境验证；试跑和启用后执行使用同一环境。不得删除业务校验、吞异常或放宽冻结条件来假装通过。
- 普通 Windows、readonly-v1 和 collection-v1 的依赖与能力分开。`spiderfly-runtime` 尚未安装到 WSL，不因为 Windows 能运行就扩大受限支持范围。
- 浏览器探索会话不等于定时脚本的登录态。实际数量不足如实说明，网页内容不能授权访问新的私密资料；具体域名与探索约束见任务执行约定。
- Excel 保存和 Excel/浏览器关闭由业务脚本负责，只关闭本次创建的资源。平台不自动保存或接管桌面窗口。
- 失败截图只在任务同时开启失败通知和附带截图时尝试；截图或通知失败不得覆盖原错误。任务归属 `tasks.created_by` 不随操作者改变。
- `/api/executions` 是近期快照；运行记录用 `/api/executions/history` 后端筛选与分页，不能只对前端已加载记录切片。

## 运行资料与隔离

- 配置读取仓库根 `.env`。真实账号、上传文件、环境、数据库、日志、浏览器资料和密钥都不得提交或回显。
- 默认任务入口为 `data/apps/<应用ID>/<原文件名>.py`；实际根目录可由配置指定。数据库和执行快照使用记录的绝对路径。
- 修改 `flows/` 不会替换已上传任务，不直接改 `data/apps/` 绕过任务版本和审计流程。
- 工作区任务前后清空；需要保留的结果放本次产物目录。不得借用用户业务资料或真实任务做临时测试。
- 离线迁移、直接改运行数据或批量清理前，确认空闲、停止服务并备份完整 `data/`、`.env` 和配置指向的外部目录。正常平台操作沿用其锁、事务和审计。
- 密钥采用当前账号的 DPAPI 保护，换电脑或服务账号需重新配置；不能仅复制 Windows 虚拟环境替代依赖与 WSL 准备。

## 验证与发布

- 测试必须在隔离副本中，不能导入正式目录的 app；清除测试进程继承的项目变量，先断言全部数据路径指向副本。
- 纯文档核对事实、链接、锚点和排版即可。代码改动按影响范围验证；涉及多个模块或发布运行代码时执行全套检查。
- 运行库改动构建新 wheel，并在隔离环境实际安装；`-I` 子进程检查不能用源码路径冒充安装。旧兼容测试安装冻结 wheel。
- 前端源码改动时同步验证并更新 `frontend/dist/`；启动器源码变动时核对构建的 EXE。自动测试不替代相关页面和业务验收。
- 发布只包含本次范围，检查暂存内容、敏感文件、依赖和构建文件。获用户推送授权后正常推送，不强制覆盖远端历史。
- GitHub 提交、正式服务重启和真实业务验证分别报告。当前状态写 CONTEXT，详细证据放带日期记录，不重复堆进使用手册。

## 文档与表达

[文档索引](docs/README.md) 区分用户指南、开发约定、历史兼容资料和检查记录。使用说明只描述对应发布版的行为，工作区待发布能力单列说明。

使用简明中文，先说结果与限制；复杂关系用表格或图示。保留现有“任务”“触发规则”“执行记录”等术语。页面默认展示状态、结果和必要操作，长错误与技术过程按需展开；AI 回复完整显示，不裁剪成不完整句子。
