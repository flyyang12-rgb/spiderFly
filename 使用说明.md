# SpiderFly 使用说明（大白话版）

## 这是干什么的？

SpiderFly 是一个大家共用的 Python 任务面板。

Python 文件只在一台常开的电脑上运行。其他伙伴不用安装 Python，也不用远程控制这台电脑，只要打开浏览器就能创建计划、提交运行和查看记录。

先记住七件事：

1. 所有任务都在共享电脑上运行。
2. 同一时间只运行一个任务，其他任务会自动排队。
3. 每个任务都有自己的独立运行环境，互相不会乱装依赖。
4. SpiderFly 直接运行 `.py`，不需要给每个任务再写 `.bat`。
5. 每个人使用自己的账号，不要共用管理员账号。
6. 每次 Python 真正开始后最多运行 10 分钟；排队时间不算在里面。
7. 所有任务只共用一个“公共工作文件夹”，系统会在运行前和运行后自动清空。

日常使用就是这条路线：

```text
管理员一次填完并创建任务 → 等待 Python 就绪 → 点击运行或等待定时 → 查看运行记录
```

当前版本还没有接入 Agent。它会把成功、失败、错误原因和人工处理入口记清楚，但不会自己改代码或自动修故障。

## 一、共享电脑第一次怎么准备

### 1. 启动 SpiderFly

日常只双击项目根目录里的：

```text
SpiderFly.exe
```

看到 SpiderFly 启动成功后，就可以打开：

```text
http://127.0.0.1:8000
```

它没有黑色命令窗口。SpiderFly 已经运行时，再双击只会打开网页，不会再启动第二个调度器。

管理员第一次准备好项目后，可以运行一次：

```text
scripts\install_autostart.ps1
```

之后固定的 RPA Windows 账号一登录桌面，SpiderFly 就会在后台自动启动，不用每天点任何批处理。它必须运行在这个已登录的交互账号下，因为 Excel、浏览器和影刀不能放进没有桌面的 Windows 服务。

`start.bat` 只保留给管理员首次准备或维修依赖，普通伙伴不需要使用。上传后的程序也不需要自己的 `.bat`、`.cmd` 或额外启动器，SpiderFly 会直接使用它的独立 Python 环境运行入口 `.py`。

如果启动失败，管理员查看：

```text
data\logs\spiderfly.log
```

重启时会发生这些事：

- 还在排队的任务会保留，服务起来后继续排队执行。
- 当时正在运行的任务不会从中间接着跑，需要重新提交；正常停服务会记为“已取消”，意外中断会在下次启动时记为“失败”。
- 当时正在构建的 Python 环境会回到“等待构建”，启动后重新处理。
- 停机期间错过的每日或每周计划，启动后最多补入一次，不会把每个错过时刻都补跑一遍。

### 2. 开启局域网访问

只在一台共享 RPA 电脑上部署并运行 SpiderFly。其他伙伴不需要复制项目、安装 Python 或创建 `.venv`，只需用浏览器访问共享电脑并登录自己的账号。

如果公司统一关闭或接管了 Windows 防火墙，请先让 IT 按公司规范启用防火墙，并只允许可信内网访问 TCP 8000。网页现在能打开，不等于防火墙已经安全配置好。

第一次给伙伴使用前，在共享电脑上双击：

```text
开启局域网访问.bat
```

Windows 会弹出管理员确认。确认当前连接的是可信的公司网络、家庭网络或自己的热点，然后输入 `Y`。

### 3. 告诉伙伴访问地址

在共享电脑上查看 IPv4 地址，伙伴访问的地址通常类似：

```text
http://192.168.1.50:8000
```

伙伴必须和共享电脑处于同一个可信局域网，可以是 Wi-Fi、网线或热点。电脑重新联网后，这个数字地址可能会变化；如果打不开，请在共享电脑上重新查看 IPv4 地址。

异地访问建议使用 Tailscale 之类的团队私网，不要把 8000 端口直接暴露到公网。

## 二、账号怎么用

管理员账号是：

```text
admin
```

第一次启动时，系统会生成随机临时密码，保存在共享电脑的：

```text
data\首次登录信息.txt
```

第一次登录后必须设置新密码。修改成功后，这个临时密码文件会自动删除；以后使用管理员自己设置的新密码。不要把临时密码文件发给其他人。

管理员登录以后，进入“管理中心 → 成员管理”，给每位伙伴创建一个“普通成员”账号。

- **普通成员**：可以修改现有任务、手动运行、查看队列和运行记录。
- **管理员**：除了上面的操作，还可以创建任务、管理成员、在任务中心修复运行环境或彻底删除任务，以及查看操作审计。

## 三、怎么创建任务

这一步由管理员操作。

1. 打开“管理中心 → 创建任务”。
2. 填写任务名称和说明。
3. 选择一个 `.py` 文件。
4. 脚本用了第三方包时，选择 `requirements.txt`；没有文件也可以手填。
5. 如果程序要拿固定 Excel 模板加工，再选择模板；没有就留空。
6. 选择手动、每日或每周触发；每日或每周任务再填写执行时间。
7. 选择是否立即启用，并勾选成功通知和失败通知。
8. 点击一次“创建任务”。系统会保存上面的全部内容，同时创建任务并准备它自己的独立 `.venv`。
9. 等待任务显示“Python 就绪”后即可运行。

创建后不需要再去编辑补内容。以后要改变运行设置时，才到“任务中心”编辑；查看、运行、修复失败环境和彻底删除也都在“任务中心”操作。编辑时不能替换脚本、依赖或模板，需要更换就删除旧任务后重新创建。

依赖示例：

```text
requests==2.32.5
pandas==2.3.2
openpyxl==3.1.5
DrissionPage==4.1.1.4
```

只有脚本完全使用 Python 自带库时，依赖才可以留空。SpiderFly **不会根据 `import` 自动猜包，也不会自动下载未声明的包**；例如脚本写了 `import DrissionPage`，就必须在依赖中填写 `DrissionPage==4.1.1.4`。建议把已经验证过的版本号一起写上，避免以后下载到不兼容的新版本。

最终以页面依赖框中显示的内容为准。SpiderFly 会做安全检查，再保存成该任务自己的 `requirements.txt`，并在独立 `.venv` 中自动下载安装。

每个任务会单独创建一个 `.venv`。例如 A 任务用旧版 pandas，B 任务用新版 pandas，两边不会互相覆盖。

这里其实有两层环境：项目根目录的 `.venv` 是 SpiderFly 平台自己用的；每个任务的环境放在 `data/envs/`。它们不是一回事。

任务的 Python 状态大白话解释：

- **准备中**：系统已经收到，正在排队创建独立 Python 环境或下载依赖。
- **Python 就绪**：入口脚本、Python 解释器和依赖已经准备好，可以直接运行。
- **准备失败**：环境没有准备成功，任务这一行会显示简短原因；管理员处理后点击“修复环境”。

“Python 就绪”不代表浏览器、Office、驱动和登录账号也准备好了。这些仍要在共享电脑上单独安装或登录。

管理员点击“修复环境”时，SpiderFly 会先在新目录里创建和检查环境，通过后才切换，不会直接把正在使用的环境改坏。切换成功后，系统会清理已经不再使用的旧环境；正在排队或运行的任务所使用的环境会受到保护。

当前网页在“准备中”或“准备失败”时不会放行新的手动运行，请等状态重新变成“Python 就绪”。已经排队、已经保存了旧环境快照的任务不受新目录构建影响。

目前一个任务可以上传“一个入口 `.py` + 一个可选 Excel 模板”。如果脚本还依赖其他本地 `.py` 文件，需要先整理成一个可以独立运行的入口文件。电脑上本来就有、而且每台 RPA 电脑路径都一致的文件（例如 `C:\公司资料\参数.xlsx`）仍可由脚本直接读取，不需要重复上传。

模板原件由 SpiderFly 保存在程序内部。每次运行前，系统会把它复制到公共工作文件夹；脚本加工的是副本，不会改坏原件。删除任务后，它自己的模板和 Python 环境会一起清理。

依赖框只填写正常的 PyPI 包名和版本，不要填写下载网址、Git 地址、命令参数或本地磁盘路径。

## 四、以后怎么修改运行设置

1. 打开“任务中心”。
2. 找到要调整的任务，点击编辑按钮。
3. 修改触发方式、执行时间、启用状态或成功失败通知。
4. 保存修改。

创建任务时这些内容已经一次填完，这里的编辑只用于以后变更，不是创建后的必做步骤。脚本、依赖和模板不能在这里替换。

目前只有三种触发方式：

- **手动触发**：需要有人点击“运行”才会开始。
- **每日执行**：每天在设置的时间自动排队。
- **每周执行**：每周在选中的星期和时间自动排队。

设置的时间都是北京时间。

最长运行时间已经统一固定为 **10 分钟**，小白不用填写秒数。它从 Python 真正开始时计算，前面有其他任务或宿主机繁忙时的排队时间不计算。超过 10 分钟后，SpiderFly 会结束这次 Python 进程树并记录“超时”。

### 创建和删除都是一次完成

- 每次创建任务，都会同时保存它自己的程序文件、依赖、可选模板和独立 `.venv`。
- 名称、说明、触发方式、时间、启用状态和成功失败通知也会在这一次创建中一起保存，不需要二次补全。
- 需要增加任务时，回到“管理中心 → 创建任务”再创建一次。
- 后续编辑只用于改变运行设置；脚本、依赖或模板要更换时，删除旧任务后重新创建。
- 没有单独的程序列表、程序绑定或程序删除步骤。

点击“删除任务”会同时删除：任务、运行记录和日志、Python 脚本、Excel 模板、依赖清单以及该任务的全部 `.venv`。正在运行时不能删除；排队中的任务可以直接删除。

## 五、怎么手动运行

1. 打开“任务中心”。
2. 找到要运行的任务。
3. 点击“运行”。
4. 系统会把任务放进工作台的“当前队列”。

如果前面已有任务，新的任务会在后面等待，不会同时抢电脑。

## 六、怎么看任务排到哪里了

打开工作台的“当前队列”，可以看到正在运行和等待的任务。

打开“运行中心 → 任务时间表”，可以切换“今日 / 本周”，查看每日任务和每周任务会在什么时候运行。手动任务没有固定时间，所以不放进时间表。

- “运行中”表示这个任务正在共享电脑上执行。
- “排队中”表示还在等待。
- 如果共享电脑上还有 Excel 进程，或 SpiderFly 专用浏览器端口 9123 被上一次任务占用，第一项任务会保持“排队中”，并显示具体原因。你用 Chrome 打开管理页面不会再挡住自己发起的任务。
- 排队任务会显示前后顺序。
- 尚未开始的任务可以取消。

正在运行的任务目前不能从网页强行停止，避免浏览器、桌面程序和文件停在半完成状态。只有还在“排队中”的任务可以取消。

## 七、怎么看运行结果和日志

打开“运行中心 → 运行记录”，点击一条记录就能看到：

- 哪个任务运行了；
- 谁提交的；
- 什么时间开始和结束；
- 运行了多久；
- Python 打印的内容；
- 错误日志；
- 进程退出码；
- 脚本返回的业务结果和错误编码；
- 是否建议重试；
- 人工处理链接或人工处理编码；
- 飞书通知是否发送。

状态大白话解释：

- **成功**：Python 正常结束，或者脚本明确告诉 SpiderFly 业务成功。
- **失败**：Python 报错退出，或者脚本虽然正常结束，但明确告诉 SpiderFly 业务失败。
- **超时**：超过任务设置的最长运行时间，SpiderFly 已请求结束该进程。
- **需要人工介入**：登录过期、验证码、权限不足等情况，需要人接手；最终执行状态会记为失败，但详情里会单独标明“需要人工介入”。
- **已取消**：任务在开始前被普通成员取消，或服务停止时中断。

普通旧脚本不用改。SpiderFly 仍按最简单的规则判断：退出码 `0` 是成功，非 `0` 是失败。

### 想让脚本把结果说得更明白

SpiderFly 每次运行都会准备一个独立资料文件夹，并把下面这些位置告诉脚本：

```text
SPIDERFLY_EXECUTION_DIR      本次运行的总目录
SPIDERFLY_RESULT_FILE        脚本写最终结果的位置
SPIDERFLY_ARTIFACT_DIR       导出文件和业务产物
SPIDERFLY_DOWNLOAD_DIR       下载文件
SPIDERFLY_SCREENSHOT_DIR     截图
SPIDERFLY_TMP_DIR            临时文件
SPIDERFLY_WORK_DIR           所有任务共用、会自动清空的公共工作文件夹
SPIDERFLY_TEMPLATE_FILE      本次已复制好的 Excel 模板（没有模板时不提供）
```

### 公共工作文件夹怎么用

默认位置是 SpiderFly 项目里的：

```text
共享工作区
```

如果公司所有 RPA 电脑已经约定了同一个 C 盘路径，可以在 `.env` 中改成例如：

```text
SPIDERFLY_WORK_DIR=C:\RPA公共工作区
```

脚本优先读取 `SPIDERFLY_WORK_DIR`，这样以后换电脑只改一处设置，不用逐个修改 Python。运行前系统会确认 Excel 已关闭、SpiderFly 专用浏览器端口 9123 空闲，并清空公共文件夹；运行结束后，无论成功、失败还是超时，都会再次尝试清空。普通管理 Chrome 可以保持打开。

注意：公共文件夹里的输出会在 Python 结束后清掉。要发飞书的 Excel，应在脚本结束前完成发送或同步；需要留档的文件请复制到 `SPIDERFLY_ARTIFACT_DIR`。当前 SpiderFly 自带的飞书通知只发送任务状态和错误信息，不会自动猜测哪个 Excel 应该发送。

例如登录过期、需要人工接手时，脚本可以向 `SPIDERFLY_RESULT_FILE` 写入：

```json
{
  "schema_version": 1,
  "outcome": "manual_required",
  "code": "LOGIN_EXPIRED",
  "message": "登录状态失效，请重新登录",
  "retryable": false,
  "manual_action_url": "https://example.com/login",
  "manual_code": "ACCOUNT_RELOGIN"
}
```

这样运行详情和飞书失败通知里都会带上人工链接或处理编码。链接只允许普通的 HTTP/HTTPS 地址，不要把账号密码直接写进链接。

`outcome` 只支持三种值：`success`、`failure` 和 `manual_required`。其中 `manual_required` 至少要填写人工处理链接或人工处理编码，否则会被当成无效结果。

如果 Python 已经超时、崩溃或非正常退出，脚本不能靠结果文件把它改成成功；真实技术错误永远优先。

结果 JSON 写坏时会记录为 `RESULT_INVALID`。系统只负责保存、展示和通知，不会自动打开链接、自动重试、修改代码或调用 Agent。

### 飞书通知怎么看

- **已发送**：消息已经交给飞书。
- **未配置**：还没填写飞书应用或收件人，不影响任务运行。
- **已关闭**：这个任务没有勾选对应的成功或失败通知。
- **发送失败**：任务结果已经保存，只是飞书消息没发出去；点进记录可看通知错误。

SpiderFly 不会抓取电脑当前前台窗口并自动外发，避免把无关窗口内容误发到飞书。脚本自己的截图应保存到本次运行的截图目录。

## 八、DrissionPage 和影刀怎么分工

不需要二选一，推荐这样分：

- **DrissionPage**：网页元素定位、点击、填写、读取网页数据、下载和网页截图。
- **影刀**：桌面软件、Office、系统窗口、影刀控制台，以及必须人工配合的流程。

SpiderFly 已提供一份可复制的模板：

```text
examples/drissionpage_managed_template.py
```

使用模板时，在任务依赖中加入：

```text
DrissionPage~=4.1
```

模板会使用 SpiderFly 专用端口 9123 和一次性独立浏览器目录，主动使用本次任务自己的下载和截图目录，并在结束时只关闭自己创建的浏览器。你可以继续在自己的 Chrome 中查看 SpiderFly 管理页面。

如果要接管影刀或人工已经打开、已经登录的浏览器，必须使用明确的浏览器调试地址和“只连接现有浏览器”方式。此时只关闭本任务自己新建的标签页，不能关闭整个浏览器。

注意：SpiderFly 只负责运行 Python。Chrome/Edge 浏览器本体、账号登录、证书、插件和驱动仍要由共享电脑准备。

影刀项目如果依赖 `xbot`、`xbot_visual` 等影刀内部模块，不能直接当成普通单文件 `.py` 上传。需要保留影刀控制台，或先把真正能独立运行的网页部分改写为标准 Python。

SpiderFly 只是提供下载和截图目录，脚本必须主动读取这些环境变量并配置给 DrissionPage，不会自动替你接管浏览器设置。

## 九、多人同时编辑会不会覆盖？

不会静默覆盖。

如果两个人同时打开同一个任务，第一个人保存成功后，第二个人再保存时会收到“任务已被其他伙伴修改”的提示。刷新后再修改即可。

## 十、常见问题

### 伙伴打不开页面

依次检查：

1. 共享电脑是否开机；
2. SpiderFly 是否已经启动；
3. 两台电脑是否处于同一个可信局域网（Wi-Fi、网线或热点）；
4. 是否运行过 `开启局域网访问.bat`；
5. 共享电脑的 IPv4 地址是否发生变化。

### 任务一直不能运行

先看任务的 Python 状态：

- “准备中”：再等一会儿。
- “准备失败”：通常是包名或版本写错、网络或 PyPI 不可用、包与基础 Python 不兼容，或者安装包缺少编译条件。

先看任务上的简短错误。如果只是临时断网，可以恢复网络后点击“修复运行环境”。当前页面不能直接修改已上传任务的依赖清单；如果包名或版本写错，就在任务中心删除旧任务，再到管理中心重新创建。浏览器、Office 没安装通常不会造成环境准备失败，而会在实际运行脚本时失败。

### Python 在自己电脑能跑，在共享电脑不能跑

任务实际运行在共享电脑上。浏览器、Office、驱动、文件和登录账号等条件，也必须在共享电脑上准备好。

### 会点击桌面或浏览器的脚本不工作

共享电脑需要：

- 保持 Windows 用户登录；
- 不要锁屏；
- 关闭自动睡眠和休眠；
- 运行期间不要切换用户或抢占桌面。

稳定的网页元素定位、下载和截图可以逐步改用 DrissionPage；桌面软件、Office、系统窗口和必须人工配合的流程仍可继续使用影刀。两者不用强行二选一。

### 任务失败了，应该先看哪里

按这个顺序看：

1. 运行记录里的“脚本结果”和错误编码；
2. 错误输出 `stderr`；
3. 标准输出 `stdout`；
4. 是否有人工处理链接或编码；
5. 仍看不懂时，把运行编号和错误编码交给开发人员。

### 飞书没收到消息

先看运行记录里的“飞书”状态。任务成功或失败与飞书是否发送是两件事；即使通知失败，完整运行结果仍保存在 SpiderFly。

### 重启以后原来的记录还在吗

在。任务、队列、日志、脚本和账号都保存在 `data` 文件夹。服务重启不会主动清空。

### 忘记管理员密码

当前页面没有“忘记密码”或管理员代重置入口，修改密码必须先知道当前密码。如果管理员密码真的忘了，请联系维护人员在共享电脑上处理；不要删除 `spiderfly.db`，否则任务、账号和运行记录也会一起丢失。

## 十一、日常使用建议

- 每位伙伴使用自己的账号。
- 不要重复启动多个 SpiderFly。
- 不要让共享电脑睡眠或断网。
- 上传来源不明的 Python 文件前先检查代码。
- 定期备份整个 `data` 文件夹。
- 不要在正在执行桌面任务时锁屏、切换用户或抢占鼠标键盘。
- 不要在任务脚本里临时执行 `pip install`；依赖在创建任务时统一填写。

最后再记一次日常路线：

```text
管理中心创建任务 → 任务中心运行或设置时间 → 工作台看当前队列 / 运行中心看时间表与记录
```
