# SpiderFly 使用说明

SpiderFly 让团队通过浏览器共用一台 Windows 电脑上的 Python 任务。管理员准备脚本和依赖，成员运行任务、查看日志并下载结果；同一时间执行一个任务，其余任务排队。

[首次安装](#三第一次运行-startbat) · [创建任务](#六创建任务) · [运行与结果](#八运行排队和结果) · [备份恢复](#十二备份和换电脑恢复) · [文档索引](docs/README.md)

## 一、下载项目

从 [GitHub](https://github.com/flyyang12-rgb/spiderFly) 克隆仓库，或使用 Code → Download ZIP，解压到固定目录后运行。

仓库包含源码、构建好的前端、启动器、示例和本地 wheel。账号、任务、业务文件、虚拟环境及真实配置保存在宿主机，不属于 GitHub 发布资料。

## 二、准备共享电脑

- Windows 10/11、64 位 Python 3.12，以及可访问的 Python 包源。
- 任务所需的 Office、浏览器、驱动或业务客户端另行安装；这些软件不由 Python 依赖清单提供。
- 普通部署不需要 Node.js；修改前端才需要。
- 桌面自动化需要合适的 Windows 登录会话。成员浏览器里的文件路径不等于宿主机可访问的路径。

AI 自动维护和公开采集还需要对应 WSL 环境，详见 [AI 任务助手](docs/AI任务助手.md) 和 [AI 采集环境](docs/AI采集环境.md)。

## 三、第一次运行 `start.bat`

```powershell
git clone https://github.com/flyyang12-rgb/spiderFly.git
cd spiderFly
.\start.bat
```

首次启动创建平台虚拟环境、安装后端依赖并启动网页。默认本机地址为 `http://127.0.0.1:8000`；首次登录信息在宿主机 `data/首次登录信息.txt` 中。登录后修改初始密码，并为团队创建各自账号。

`.env` 用于本机端口、目录和通知等配置，参考 `.env.example`。真实 `.env` 不上传 GitHub；无需把管理员的本机配置发给每位成员。

## 四、日常启动

以后双击 `SpiderFly.exe`。首次依赖安装与日常启动是两件事，排查时先看启动状态和 `data/logs`。具体启动、自启和局域网配置见 [部署指南](docs/部署与操作详解.md)。

## 五、团队访问与账号

成员通过宿主机的局域网地址访问，不能在自己的电脑上使用宿主机的 `127.0.0.1`。按 [局域网访问](docs/部署与操作详解.md#局域网访问) 配置端口和 Windows 防火墙。

超级管理员管理账号与平台设置；管理员准备和维护任务；普通成员使用工作台、任务中心和运行中心。页面和接口按账号权限限制操作，任务归属不会随运行操作者变化。

## 六、创建任务

管理员在“管理中心 → 创建任务”填写名称、说明，上传一个 UTF-8 `.py` 入口，填写依赖；有输入表格时上传 Excel 模板，再设置触发方式和通知。

平台为任务准备独立 Python 环境。等待“Python 就绪”后再运行；“准备失败”时先打开安装日志，核对 Python、包源、版本和本地 wheel。

### 依赖怎么填

脚本实际用了什么第三方包，就声明什么。纯标准库脚本可留空，不需要额外的指令集。

| 脚本类型 | 示例依赖 |
| --- | --- |
| 直接使用 openpyxl | `openpyxl==3.1.5` |
| 仓库当前 `flows/` 示例 | `spiderfly-runtime==0.1.0` |
| 原有 `examples/instruction_*.py` | 按该文件原说明固定 `spiderfly-instructions` 版本 |

新运行库从 `release/runtime/` 安装；旧指令包从 `release/instructions/` 安装。缺少所声明 wheel 时直接报错，不从公网查找同名包。网页依赖框填写包名和版本，不填本机 wheel 路径。

任务环境彼此独立。更新仓库不会自动更换已上传脚本、任务依赖或旧 wheel。

### 跑通订单筛选

1. 新建 `.xlsx`，表头为 `订单号`、`状态`、`金额`。
2. 输入三行：`00123 / 待处理 / 0`、`00124 / 已完成 / 19.5`、`00125 / 待处理 / 42.5`。订单号按文本填写。
3. 上传 [order_filter.py](flows/order_filter.py)，依赖填写 `spiderfly-runtime==0.1.0`，并上传该表格。
4. 选择手动触发，运行后下载 `流程文件/输出/结果.xlsx`，应保留 `00123` 和 `00125`；金额零值不丢失。

没有匹配行时仍输出表头。原文件不覆盖，本次输入副本位于 `流程文件/输入.xlsx`。这只是业务示例，筛选条件在脚本顶部修改，不是平台专门的订单功能。

## 七、手动、每日和每周任务

手动任务由成员点击运行；每日和每周任务按保存的触发规则入队。所有任务共用一条持久队列，计划到点表示进入队列，不保证立即开始执行。

在任务中心编辑触发规则、启用或停用任务。启停、编辑和运行保留各自的操作记录；不要通过改数据库调整队列或计划。

## 八、运行、排队和结果

在任务中心点击运行，在运行中心查看队列和运行记录。正在执行的任务先完成，其余任务等待；环境准备、宿主机条件和优先维护验证也可能影响开始时间。

运行记录保存 stdout、stderr、状态、开始结束时间和本次文件。查询旧记录使用页面筛选和分页。退出码为零不一定满足业务目标，还需检查脚本输出、回执和真实结果。

### 下载本次文件

脚本将需要下载的文件写入 `SPIDERFLY_ARTIFACT_DIR`。使用运行库时写入 `context.output_dir`，完成后从“本次文件”下载。公共工作区会清理，不能作为长期结果目录。

### 失败后自动修复

失败后默认进入自动维护。支持的任务在受限环境复现、修复和验证，通过后启用修复版本并自动重跑一次。原失败记录保留；重跑失败不会无限触发修复循环。

查看执行日志、维护记录和维护结果通知，区分“验证通过”“已启用”和“重跑成功”。未知依赖、Windows GUI 或需要登录的软件不保证能自动修复；具体范围见 [AI 任务助手](docs/AI任务助手.md)。

### 强制停止正在运行的任务

有权限的管理员可从执行日志发起强制停止；排队记录可取消。停止以实际进程退出和工作区清理完成为准，不要反复点击或手动结束无关进程。

### 失败通知附带截图

只有同时开启失败通知和附带截图才尝试截图。截图来自宿主机的执行现场，不是成员电脑；截图或通知失败不会替代原始错误日志。

## 九、输入文件与业务代码

平台上传的模板通过 `SPIDERFLY_TEMPLATE_FILE` 提供给脚本。可选运行库负责留存本次副本与回执，业务脚本负责具体列、筛选、计算和结果验收。

Excel 保存与工作簿、浏览器关闭由脚本负责。原文件不覆盖，只关闭本次创建的资源；不要把 openpyxl 的文件读写理解成桌面 Excel 窗口控制。

新业务从 [flows/template.py](flows/template.py) 开始。平台、运行库和业务边界见 [项目架构](docs/项目架构.md)，接口见 [运行库说明](backend/RUNTIME.md)。

## 十、浏览器、影刀与 AI

DrissionPage、Office 和其他桌面依赖需要在宿主机准备。依赖影刀内部运行时的程序不能直接视为普通 Python 脚本上传。

### 使用 DeepSeek 创建任务

超级管理员配置 AI 模型和密钥，管理员通过 AI 创建入口描述目标、输入、字段和结果要求。生成草稿后先核对代码与依赖，再创建手动任务验证结果。语法检查通过不代表任务已经试运行。

正常执行已保存脚本不调用模型。AI 探索可以提取公开数据并下载 CSV；登录、验证和探索会话不自动变成定时脚本的登录状态。

### AI 采集试跑

公开采集使用已准备的 collection-v1 环境与单文件 Python。试跑走同一串行队列，结果绑定草稿版本；修改源码后不能沿用旧草稿的通过状态。

`spiderfly-runtime` 当前用于普通 Windows Python 任务，尚未安装到 WSL 受限环境。受限脚本继续使用其明确支持的依赖，详见 [采集环境](docs/AI采集环境.md)。

## 十一、删除任务与版本管理

删除任务会影响其脚本、环境、模板和运行记录，先下载需要保留的结果，并核对确认框范围。不要直接删除运行中的资料目录。

更新脚本、查看历史 PY、验证和回退使用平台版本入口，具体行为见 [任务需求与版本](docs/任务需求与版本方案.md)。回退不会重写历史执行，修改仓库示例也不会自动替换正式任务。

## 十二、备份和换电脑恢复

备份完整 `data/`、`.env`，以及配置指向的外部任务目录、环境和工作区；对话、源码版本、模板、维护结果和模型密钥也在备份范围内。GitHub 仓库不是业务数据备份。

### 修改代码后怎么生效

提交 GitHub 与更新正式服务分开。升级前确认任务空闲、停止服务并备份，更新源码后通过原启动方式重启。前端源码变化需要对应构建产物；不能只修改源码就宣称页面或进程已更新。

### 换电脑的推荐顺序

准备 Windows 和 Python → 安装项目与所需桌面软件 → 恢复运行资料和配置 → 重新配置当前账号的密钥 → 准备任务依赖及 WSL → 验证后恢复计划。不能仅复制旧 `.venv` 就认定新电脑可运行。

## 十三、常见问题

| 现象 | 优先检查 |
| --- | --- |
| 成员打不开网页 | 宿主机是否启动、局域网 IP、端口和防火墙 |
| Python 准备失败 | 安装日志、Python 版本、包源和本地 wheel |
| 本机脚本能跑，宿主机不能 | 文件路径、依赖、桌面软件、登录状态与权限 |
| 任务一直排队 | 正在运行的任务、维护验证、环境和宿主机状态 |
| 运行成功但没有文件 | 是否把产物写入本次目录，是否真正保存 |
| 飞书没有通知 | 任务通知选项、平台配置与通知日志 |

涉及本机未发布的 AI 对话、候选确认或界面改动时，先看 [CONTEXT](CONTEXT.md)；不要把本机行为直接当作当前 GitHub 发布版能力。
