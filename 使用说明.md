# SpiderFly 使用说明

AI 助手也可只探索方法：用自然语言说明目标并要求少量样本验证，在“处理记录与用量”查看它的访问、工具错误和调整。无需以采满某个数量或生成脚本作为每次探索的目标；后续可以要求复核已有证据。

## 使用 DeepSeek 创建任务

1. 超级管理员在“管理中心 → AI 模型”填写 DeepSeek 密钥并“保存配置”；留空保留原密钥，页面不会回显。预算在“调用与维护预算”中展开设置。
2. 管理员点击“任务中心 → AI 创建”，写明目标网址、筛选范围、输出字段或表格处理规则；已有任务从行内“AI 助手”继续对话。
3. 查看生成的代码，点击“使用草稿”带入创建表单；已有任务的对话显示“更新本任务”，验证通过后启用。需要表格时补充上传模板，再创建手动任务并运行，检查实际结果。

“语法通过 · 未试运行”只代表草稿语法检查通过。对话、处理记录和草稿会保存；AI 回复完整显示，内容较长时在消息区域滚动查看。默认只显示最新草稿，历史草稿和用量按需展开。

上传脚本执行失败后默认自动维护，验证通过后直接启用修复，无需维护开关或再次确认。执行日志会说明改了什么，右上角“维护结果”保留通知。操作见[失败后自动修复](#失败后自动修复)，能力和环境准备见 [AI 任务助手](docs/AI任务助手.md)。

## 用途与入口

SpiderFly 是装在一台共享 Windows 电脑上的 Python 任务中心。

管理员把脚本创建成任务，伙伴们用浏览器点击运行或查看结果。所有任务都在共享电脑上执行，同一时间只运行一个，其余任务自动排队。

日常路线只有这一条：

```text
GitHub 下载项目 → 共享电脑首次运行 start.bat → 以后双击 SpiderFly.exe → 浏览器里创建和运行任务
```

不需要给每个 Python 任务再写 `.bat`，SpiderFly 会直接运行任务的入口 `.py`。

| 你要做什么 | 从哪里开始 |
| --- | --- |
| 安装并运行已有任务 | 按本文的首次安装、创建任务和运行记录步骤操作 |
| 用 AI 创建或维护任务 | 本文开头及[失败后自动修复](#失败后自动修复) |
| 体验上传、运行、下载结果 | [平台筛选示例](docs/平台筛选示例.md) |
| 编写自己的单文件流程 | [独立流程目录](flows/README.md) 和 [指令目录](docs/指令目录.md) |
| 维护平台或让 AI 接手 | [AGENTS.md](AGENTS.md) 和 [开发与验证](docs/开发与验证.md) |

当前公共测试包为 `0.1.4`，包含六条指令。各示例所需版本见下文“依赖怎么填”；历史测试过程保存在维护记录中。

---

## 一、从 GitHub 下载

在准备作为宿主机的共享电脑上打开 [SpiderFly GitHub 仓库](https://github.com/flyyang12-rgb/spiderFly)：

1. 点击 **Code → Download ZIP**。
2. 解压到一个以后不会随便移动的位置，例如 `D:\SpiderFly`。
3. 不要直接放在压缩包里运行，也不建议放进会自动同步的个人网盘目录。

### GitHub 仓库里有什么

- SpiderFly 后端和前端源码；
- 已经构建好的管理页面；
- `SpiderFly.exe`；
- 首次准备使用的 `start.bat`；
- 局域网配置脚本和说明文档；
- `.env.example` 配置示例。

### GitHub 仓库里故意不放什么

- `.env`：里面可能有飞书密钥和本机设置；
- `data`：里面是账号、任务、脚本、模板、运行记录和日志；
- 平台自己的 `.venv`；
- 每个任务自己的虚拟环境；
- `frontend/node_modules`；
- 公司的 Excel、账号密码和其他业务资料。

因此要记住：**GitHub 放的是可公开的程序发布资料，不是这台电脑的业务备份。**

---

## 二、新电脑还需要准备什么

### 必须准备

1. **Windows 10 或 Windows 11**。
2. **64 位 Python 3.12**，安装时勾选 **Add Python to PATH**。公共指令包要求 3.12 以上，当前完整验证使用 3.12.10。
3. 共享电脑可以访问 Python 依赖下载源；创建任务环境时需要联网下载依赖。

安装后打开 PowerShell，运行 `python --version`，应显示准备使用的 3.12 版本。电脑上有多个 Python 时，还可运行 `python -c "import sys; print(sys.executable)"` 确认路径。`start.bat` 首次创建平台环境时使用这个 `python`；安装新的系统 Python 不会自动升级已经存在的平台 `.venv`。

任务默认跟随平台的基础 Python。需要另选任务解释器时，在 `.env` 设置 `SPIDERFLY_BASE_PYTHON` 的完整路径；使用公共指令的任务仍需 3.12 以上。已有任务环境不会因此自动重建。

### 根据任务准备

如果脚本会使用这些软件，也必须把它们装在共享电脑上：

- Excel、WPS 或其他 Office 软件；
- Chrome 或 Edge；
- 影刀客户端；
- 数据库客户端、证书、浏览器插件、OCR 或公司驱动；
- 脚本需要登录的网站账号。

Python 的 `requirements.txt` 只能安装 Python 包，不能替你安装 Excel、浏览器、影刀或系统驱动。

### 使用 AI 和自动修复前准备

AI 对话需要超级管理员配置 DeepSeek 密钥；自动修复试跑还需要宿主机准备 WSL Ubuntu 和 `readonly-v1` 运行环境，步骤见 [AI 任务助手](docs/AI任务助手.md)。修复版以后也在此环境运行，每次最多 120 秒。当前支持范围不包括登录浏览器、Windows GUI 和任意第三方依赖，环境未准备好或依赖不兼容时会记录原因。

### Node.js 要不要装

**正常下载和运行不需要 Node.js。** 仓库已经带有构建好的管理页面。

只有开发人员要修改网页源码并重新构建页面时，才需要安装 Node.js 和 npm。普通管理员和 RPA 伙伴不用装。

### 共享电脑的使用状态

涉及浏览器、Excel或桌面点击的任务运行时，共享电脑应当：

- 保持 Windows 用户已经登录；
- 不要自动睡眠或休眠；
- 尽量不要锁屏、切换用户或远程抢占桌面；
- 保持网络稳定。

伙伴自己的电脑只需要浏览器，不需要安装 SpiderFly、Python 或任务依赖。

---

## 三、第一次运行 `start.bat`

新电脑第一次准备时，在项目根目录双击：

```text
start.bat
```

当前 `SpiderFly.exe` 没有商业代码签名，Windows 可能提示“未知发布者”。先确认文件来自本项目的正式 GitHub 仓库；公司正式分发时建议由 IT 从源码复核、重新构建并签名，不要对来源不明的 EXE 直接放行。

它会自动完成：

1. 创建 SpiderFly 平台自己的 `.venv`；
2. 安装后端依赖；
3. 使用仓库自带的管理页面；
4. 创建本机的 `data` 文件夹和数据库；
5. 启动 SpiderFly。

第一次需要下载 Python 包，稍等几分钟是正常的。窗口显示失败时先不要反复双击，查看窗口最后几行提示。

成功后，本机打开：

```text
http://127.0.0.1:8000
```

第一次生成的超级管理员账号是：

```text
admin
```

超级管理员初始密码为 `admin`，登录信息保存在：

```text
data\首次登录信息.txt
```

登录后可直接使用，不再要求首次改密。可通过右上角“修改密码”自行设置至少 6 个字符的新密码；修改成功后，首次登录信息文件会自动删除。不要把这个文件、数据库或管理员密码上传到 GitHub，也不要发到群里。

普通成员也可以正常登录，角色只决定登录后的操作权限。输入错误账号或密码时，登录页会显示“登录失败：用户名或密码不正确”；看到此提示应核对创建账号时设置的密码，不需要改成管理员。

### `.env` 什么时候需要

没有特殊设置时可以先不创建 `.env`，默认配置可以启动。

需要设置飞书通知、自定义公共文件夹或端口时：

1. 复制 `.env.example`；
2. 把副本改名为 `.env`；
3. 只在共享电脑本机填写真实配置。

`.env` 不会随 GitHub 下载，也绝不能提交到 GitHub。

---

## 四、以后只双击 `SpiderFly.exe`

首次准备完成后，平时只双击：

```text
SpiderFly.exe
```

它会在后台启动服务并打开网页，不会一直显示黑色命令窗口。

- 已经运行时再次双击，只会打开网页，不会再启动第二套调度器。
- `start.bat` 只用于新电脑首次准备或平台依赖维修；开发人员修改网页后应在 `frontend` 中单独重新构建。
- 每个任务也不需要自己的 `.bat` 或 `.cmd`。

启动失败时，管理员查看：

```text
data\logs\spiderfly.log
```

如果要随 Windows 登录自动启动，可由管理员运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\install_autostart.ps1
```

以上命令在项目根目录运行。取消登录自启时，用同样方式运行 `scripts\uninstall_autostart.ps1`。它只取消以后登录时启动，不会停止已经运行的服务。

桌面自动化必须运行在已经登录的 Windows 账号下，不要把 Excel、浏览器和影刀任务改成没有桌面的 Windows 服务。

---

## 五、让同一局域网的伙伴访问

SpiderFly 只安装在共享电脑上。其他伙伴通过浏览器访问它。

第一次开放局域网访问时，在共享电脑双击：

```text
开启局域网访问.bat
```

Windows 会请求管理员权限。只有当前连接的是可信公司网络、家庭网络或自己的热点时才输入 `Y`。

然后在共享电脑运行 `ipconfig`，找到当前网卡的 IPv4 地址。伙伴访问的地址类似：

```text
http://192.168.1.50:8000
```

注意：

- 伙伴和共享电脑必须在同一个可信局域网；
- 共享电脑重新联网后，IPv4 地址可能变化；
- 如果公司统一管理防火墙，请让 IT 按公司规范只开放内网 TCP 8000；
- 不要把 8000 端口直接暴露到公网；
- 异地访问请使用公司 VPN 或 Tailscale 这类受控私网。

超级管理员登录后，在“管理中心 → 成员管理”给每位伙伴创建独立账号。不要多人共用管理员账号。

### 编辑、重置密码和删除成员

系统有三种角色：初始 `admin（系统管理员）` 是唯一的**超级管理员**，可管理成员和任务；**管理员**可管理任务，可查看团队成员列表，但不能创建、编辑、删除成员或自行修改密码；**普通成员**可登录、运行任务和查看结果。超级管理员角色不能通过成员创建或编辑分配给其他账号。

在成员卡片点击“编辑”，可修改登录账号、显示名称、角色和启用状态。“重置密码”留空表示密码不变；需要重置时填写至少 6 个字符，并再次确认。系统不会显示原密码，重置后成员用新密码登录即可，无需再次修改。新建成员默认初始密码为 `123321`，也可以在创建时指定其他至少 6 个字符的密码。改账号、重置密码、调整角色或启停账号后，该成员的旧会话失效，需要重新登录。

点击“删除”后须确认。账号立即禁止登录并从列表移除，历史任务、执行记录和审计记录保留，原账号名继续保留不能重新注册。若只是临时停止使用，建议在编辑中选择“停用”，以后可以重新启用。

超级管理员不能删除、停用或给自己降权，自己的密码通过右上角“修改密码”调整；其他管理员如需改密，请联系超级管理员重置。在多个窗口同时编辑时，如提示“成员信息已更新”，请关闭编辑窗口、点击“刷新成员”后重新编辑。

---

## 六、创建任务：一次填完

创建任务由管理员操作：

1. 打开“管理中心 → 创建任务”。
2. 填写任务名称和说明。
3. 选择入口 `.py`。
4. 脚本用了第三方包时，上传 `requirements.txt` 或在依赖框手填。
5. 脚本要加工固定 Excel 模板时，选择模板；没有就留空。
6. 选择手动、每日或每周触发。
7. 每日或每周任务填写北京时间。
8. 选择是否启用，以及成功或失败时是否通知。
9. 点击一次“创建任务”。

系统会同时保存脚本、依赖、模板和运行设置，并为这个任务创建独立 Python 环境。等任务显示 **Python 就绪** 后再运行。

### 依赖怎么填

SpiderFly 不会根据 `import` 自动猜第三方包。脚本用了什么，就要明确填写什么，例如：

```text
requests==2.32.5
pandas==2.3.2
openpyxl==3.1.5
DrissionPage==4.1.1.4
```

只有完全使用 Python 自带库时才可以留空。建议写已经验证过的版本号。

每个任务的环境彼此独立，A 任务和 B 任务可以使用不同版本的 pandas，不会互相覆盖。

使用项目公共指令时，可按下面的指南固定版本。安装文件已经随仓库放在 `release/instructions/`；不要把本地 wheel 路径填进网页依赖框。

| 入口或示例 | 按该指南填写的依赖 |
| --- | --- |
| [姓名合并](docs/平台指令示例.md) | `spiderfly-instructions==0.1.0` |
| [单列筛选](docs/平台筛选示例.md) | `spiderfly-instructions==0.1.1` |
| [平台均值](docs/平台均值示例.md) | `spiderfly-instructions==0.1.2` |
| [通用任务模板](docs/Python任务模板.md)、独立金额差额和均值流程 | `spiderfly-instructions==0.1.3`；独立流程也已验证可用 `0.1.4` |
| [获取文件列表](flows/README.md#获取文件清单) | `spiderfly-instructions==0.1.4` |

仓库出现新版不会自动升级已创建任务。提示缺少 wheel 时，由管理员补齐所声明版本的原发布文件；修改依赖或脚本可通过原任务“更新脚本”验证；当前受限环境不支持的依赖需另行准备受支持方案。

### Python 状态是什么意思

- **准备中**：正在创建独立环境或下载依赖；
- **Python 就绪**：脚本和依赖已经准备好；
- **准备失败**：依赖名、版本、网络或 Python 兼容性有问题，管理员查看原因后处理。

“Python 就绪”只代表 Python 部分准备好了，不代表 Excel、浏览器、登录账号和证书已经准备好。

创建以后不用再二次补内容。后续编辑只改名称、说明、每日/每周时间、启用状态和通知设置。要更换脚本、依赖或模板，请使用不同名称新建任务，原任务可按需要保留；删除旧任务前，先保存需要的记录和结果。

---

## 七、手动、每日和每周任务

任务只有三种触发方式：

- **手动触发**：伙伴点击“运行”时才排队；
- **每日执行**：每天在设置时间自动排队；
- **每周执行**：每周在选中的星期和时间自动排队。

设置时间都是北京时间。

手动运行不影响计划执行。例如 8:58 手动运行的任务到 9:00 仍未结束，9:00 的计划执行会照常入队，等前面的执行结束后再跑，不会跳过或中断手动运行。同一任务可以保留多次到点产生的计划记录，按入队顺序串行执行，服务重启后仍保留尚未开始的计划记录。

同一任务已有排队或运行记录时，手动“运行”按钮仍会禁用，防止重复点击。停用任务仍会取消该任务所有尚未开始的排队记录，已经运行的执行继续完成。

打开“运行中心 → 任务时间表”：

- “今日”按北京时间显示当天的每日和每周任务，系统的今日运行统计也使用北京时间自然日；
- “本周”按周一到周日显示一周计划；
- 手动任务没有固定时间，所以不放进时间表。

原 Windows 运行环境的最长运行时间默认 **10 分钟**，从 Python 真正开始运行时计算，排队等待不计时。管理员可在 `.env` 修改 `SPIDERFLY_TASK_TIMEOUT_SECONDS`，单位为秒，支持 60 至 86400 秒，需要在空闲时重启服务后生效。已启用的自动修复版使用受限环境，每次最多 **120 秒**。

当前网页部分位置仍固定显示“10 分钟”，没有逐任务修改入口。宿主机使用了不同配置时，以该配置和实际执行日志为准。

---

## 八、手动运行、排队和日志

### 手动运行

1. 打开“任务中心”。
2. 找到任务。
3. 点击“运行”。

任务列表的“归属人”是最初创建该任务的成员。可以使用任务列表上方的“归属人”筛选查看指定成员的任务，当前账号会标记为“姓名（我）”；其他成员后来编辑、启停或运行任务，都不会改变归属人。老版本迁移且没有创建者记录的任务显示“系统迁移”。运行记录里的“发起人”只表示谁提交了那一次运行。

同一时间只会运行一个任务。前面有任务时，后面的任务自动排队，不会同时抢占共享电脑。

工作台的“当前队列”和“运行中心 → 运行队列”显示正在运行和等待中的任务。还没开始的任务可以取消；管理员需要停止正在运行的任务时，从执行日志使用“强制停止”，具体见[强制停止正在运行的任务](#强制停止正在运行的任务)。

在任务中心点击“已启用”可以停用任务，停止后续调度，并取消该任务尚未开始的排队记录。已经运行的任务会继续执行。即使任务的 Python 环境损坏，也可以先用这个开关停用，再处理环境问题。

### 为什么一直排队

SpiderFly 在开始任务前会检查：

- 上一个任务是否结束；
- 已就绪的自动修复是否正在试跑；
- Excel 进程是否已经关闭；
- SpiderFly 专用浏览器端口 9123 是否空闲；
- 公共工作文件夹是否可以清理。

普通伙伴用 Chrome 打开管理页面不会占用 9123，也不会挡住自己提交任务。

### 查看结果

打开“运行中心 → 运行记录”，点击记录可以看到：

运行记录每页显示 10 条。可以按任务名称、运行状态、发起人、开始日期和结束日期组合查询；日期按北京时间的提交日期计算，并包含所选的开始日和结束日。点击“重置”恢复全部记录，使用底部的“上一页”“下一页”翻页。

- 成功、失败、超时或取消；
- 谁发起了任务；
- 开始和结束时间；
- 耗时和退出码；
- `stdout` 和 `stderr`；
- 自动修复结论、版本和可展开的“技术详情”（有维护记录时）；
- 业务错误编码；
- 是否需要人工介入；
- 人工处理链接或编码；
- 本次保存的文件、大小和下载按钮；
- 飞书通知结果。

普通脚本没有业务回执时，退出码为 `0` 记为成功，非 `0` 记为失败；有业务回执时还要检查回执。已启用修复版如有冻结验收条件，每次运行还会执行对应检查。执行失败后可进入自动维护，原失败记录不会改成成功；修复结果在同一日志顶部单独显示。

### 失败后自动修复

脚本运行失败后，系统根据原代码、参数和错误生成修复版本，在受限环境验证，通过后自动启用并重跑一次。普通脚本不必先写验收声明：系统检查最小修复、复现同类错误并确认修复后运行通过；已有业务验收条件继续检查。

查看结果：打开“运行中心 → 运行记录”，进入原失败记录的日志。顶部显示“等待自动重跑”“正在自动重跑”或“自动重跑成功/失败”等实际状态，并用一句“把 A 改为 B，解决 C；自动重跑成功”说明修改及效果。点击“查看重跑日志”查看这次执行的输出和文件；展开“技术详情”可看完整过程。重跑结束后，右上角“维护结果”保存通知，离线时产生的结果下次登录仍可查看；通知由任务归属人和超级管理员分别标记已读。

例如 `3 + name` 导致整数与字符串相加的 TypeError，修复并实际重跑成功后显示：“把 3 + name 改为 name，解决类型错误；自动重跑成功。”原失败记录及错误输出保留，重跑另有执行结果。升级前只有试跑的旧记录显示“试跑通过”，不会补跑历史业务。

系统自动处理排队衔接：正在运行的任务先结束，已就绪修复优先验证；同任务源码、依赖和输入一致且尚未开始的记录改用修复版，原参数与发起人保留。改名称、说明或计划不会阻止修复；如果代码、依赖、模板已换新或用户主动回退，旧版本维护结束，新版本失败时再继续维护。

每次修复只自动重跑一次：已有兼容排队记录时沿用最早一条，否则新建“修复重跑”记录，不额外扣减原定时计划次数。重跑失败会记录原因并通知，不继续循环修复和重跑；之后独立发起的运行仍按原维护规则处理。任务已停用或剩余维护预算用完时不自动重跑，日志说明原因。依赖不兼容或试跑未通过时保留原版本。强制停止的执行不会触发代码修复，相同故障受去重和预算限制。

### 查看版本、回退与维护预算

管理员打开任务“AI 助手 → 维护与版本”，可查看简短结论；“技术详情”展开过程及用量，“代码版本”展开代码和“使用中”的版本，或点击“回退至 V…”使用本任务原始或已验证版本。回退需等本任务没有排队或运行记录，且依赖一致、版本文件完整。重新上传同名 `.py` 会创建任务，不会升级已有任务。

源码和模板按内容去重保存，修复保留原文件并新增版本，不会为 10–20 个版本重复创建同样数量的 Python 环境；通过创建表单另建的测试任务仍各有独立环境。

超级管理员在“AI 模型 → 调用与维护预算”调整限额。自动维护默认按滚动 24 小时计算：单任务最多 45 分钟，全局 2 小时及 200,000 tokens。修改后点击“保存维护预算”；日常对话预算用“保存配置”保存，两类预算分别计算。需要中止当前维护时，可在对应记录中点击“停止本次维护”，这不是永久关闭自动维护。

### 下载本次文件

在运行记录的日志弹窗中找到“本次文件”，点击目标文件旁边的“下载”，再到浏览器的下载列表查看进度。列表用相对路径区分不同文件夹中的同名文件，并显示文件大小。

- 成功、失败、超时或取消后，可以下载已经保存的正式文件；排队和运行中须等本次执行结束。
- 失败记录也可能保留输入存档，方便下载排查。有文件不代表处理成功，仍要看运行状态和业务结果。
- 记录没有保存文件时显示“暂无文件”；文件后来被删除或移动时，下载会给出相应提示。
- 完成首次改密并已登录的普通成员，也可以下载共享运行记录中的文件。
- 删除任务会一起删除这些资料，重要输入和结果应先下载保存。

这里展示的是脚本专门保存到本次执行产物目录的正式文件。临时文件、其他运行目录及业务回执不会自动出现在下载列表中；文件较多或部分文件读取失败时，页面会提示。

旧安装看不到下载入口时，由管理员按“修改代码后怎么生效”更新后端与 `frontend/dist`，伙伴随后刷新网页。

---

## 九、Excel 模板和公共工作文件夹

一个任务目前支持：

```text
一个入口 .py + 一个可选 Excel 模板
```

上传的 `.py` 默认保存在 SpiderFly 项目目录下：

```text
data\apps\应用ID\原文件名.py
```

例如应用 ID 是 `1`、上传文件名是 `1.py`，原文件位置就是 `data\apps\1\1.py`。应用 ID 是系统分配的数字；数据库保存当前使用版本的完整路径。自动修复代码保存在该应用的 `versions` 目录，通过验证后自动切换引用，原上传文件保留。管理员可用 `.env` 的 `SPIDERFLY_APPS_DIR` 修改程序根目录。不要直接编辑这里的文件；手工开发的新脚本通过“更新脚本”保存新版本，运行故障由自动维护流程处理。

`data\apps` 属于本机任务资料，不会提交到 GitHub。备份、换电脑或移动项目时，要连同完整 `data` 目录一起处理；如果 `.env` 指向外部目录，也要一并备份。

模板原件保存在任务资料里。使用原 Windows 环境时，SpiderFly 在运行前复制一份到公共工作文件夹；自动修复版使用受限环境中的只读模板副本，结果写入本次产物目录。

默认公共工作文件夹是：

```text
共享工作区
```

也可以在 `.env` 中改成公司统一路径：

```text
SPIDERFLY_WORK_DIR=C:\RPA公共工作区
```

SpiderFly 会在运行前和运行后清空公共工作文件夹。要发送飞书的 Excel，应由脚本在结束前完成发送或同步；需要留档和网页下载的文件，应由脚本放入运行变量 `SPIDERFLY_ARTIFACT_DIR` 指定的本次产物目录。

电脑上已经存在、而且每台 RPA 电脑路径都一致的文件，脚本仍可以直接按固定路径读取。

---

## 十、DrissionPage 和影刀怎么分工

两者可以继续一起使用：

- **DrissionPage**：网页定位、点击、读取、下载和网页截图；
- **影刀**：桌面软件、Office、系统窗口、影刀控制台和需要人工配合的流程。

SpiderFly 提供了 DrissionPage 示例：

```text
examples\drissionpage_managed_template.py
```

使用时记得在任务依赖中填写：

```text
DrissionPage~=4.1
```

影刀项目如果依赖 `xbot`、`xbot_visual` 等影刀内部模块，不能直接当成普通单文件 Python 上传，应继续由影刀控制台运行，或先把能独立运行的部分改成标准 Python。

---

## 十一、删除任务

管理员在“任务中心”点击删除任务时，会一起删除：

- 任务设置；
- 运行记录和日志；
- 每次运行保存的输入存档和结果文件；
- Python 脚本；
- Excel 模板；
- 依赖清单；
- 该任务的全部独立环境。

该任务的 AI 对话、草稿、代码版本和维护资料也会随任务删除。

删除前，请先从运行记录的“本次文件”下载需要保留的资料。删除后不能恢复。正在运行或自动维护中不能删除；普通排队中的任务会先从队列移除再删除。

---

## 十二、备份和换电脑恢复

### 平时备份什么

最重要的是：

```text
data
.env
```

`data` 里有账号、任务、上传脚本、模板、计划、运行记录，也包含 AI 对话、草稿、代码版本、维护日志、输入快照、通知及加密的模型密钥；`.env` 里有本机设置和可能存在的飞书密钥。

这些是默认位置。若 `.env` 配置了其他数据、任务或环境目录，也要备份对应位置，记录其完整路径、Python 安装路径和所用代码版本。临时公共工作区不会自动成为长期备份；需要保留的文件应提前存档。

备份前先停止 SpiderFly：如果是 `start.bat` 启动的，在对应窗口按 `Ctrl+C` 并等待退出；如果是 EXE 启动的，在任务管理器中确认文件位置后结束本项目的 `SpiderFly.exe`。然后把资料复制到公司允许的受控备份位置。不要发到群里，不要上传到公开网盘，也不要提交到 GitHub。

停止前先安排服务空闲，等待运行、排队任务及 AI 处理、维护结束，避开下一次定时触发。停止后确认服务进程已经退出、原服务地址不再响应，再复制资料。仅关闭网页不会停止服务；若关闭启动器后服务仍响应，需要检查本项目的服务进程，不能把其他任务使用的 Python 进程一并结束。

下面这些不用备份，可以在新电脑重新生成：

```text
.venv
frontend\node_modules
```

### 修改代码后怎么生效

管理员确认服务空闲后，按上面的步骤停止并备份，再更新对应文件，最后重新启动 SpiderFly。改后端代码需要重启服务；改前端页面需要同时更新构建好的 `frontend/dist`，伙伴随后刷新网页。不要用测试副本的 data 或 .env 覆盖真实资料。

重启后先查看登录、任务状态与运行记录，再运行一个合成数据的手动测试任务。代码提交到 GitHub 不表示宿主机已经更新；管理员应记录本机实际更新版本和时间。之前各次修复的验证范围见 [项目检查记录](docs/项目检查记录_2026-09-03.md)。纯文档更新不需要重启服务。

如果改的是某个流程文件，服务重启不会把它替换进已创建任务。应在任务中心保留旧任务，再用不同名称新建任务并上传修改后的脚本进行验证。

### 换电脑的推荐顺序

当前恢复方式要求保留原绝对路径，例如旧电脑的 `D:\SpiderFly\data\apps`，新电脑也放到该位置。任务、模板、环境及部分历史记录保存了绝对路径，**仅修改 `.env` 不会迁移这些记录**。

1. 记录原先启用的任务，在旧电脑逐个停用，等待正在运行的任务结束；停用会取消尚未开始的排队记录。计划迁移时先做这一步，避免恢复后自动触发业务。
2. 停止旧服务并确认地址不再响应，备份 `data`、本机 `.env`（如果有）和自定义目录，记录原路径与代码版本。
3. 在新电脑准备同一代码版本，恢复到与原来相同的盘符及目录。先恢复成功，再单独安排版本升级，便于排查问题。
4. 安装 Python 3.12，以及任务需要的 Excel、浏览器、影刀、证书和驱动；如果设置了 `SPIDERFLY_BASE_PYTHON`，确认它指向新电脑真实存在的解释器。
5. 把备份还原到各自原路径，不复制旧电脑平台根目录的 `.venv`。关闭旧电脑的登录自启，迁移完成前保持旧服务停止。
6. 首次运行 `start.bat`，让平台环境重新准备，然后登录检查成员、任务和历史记录。
7. 检查任务的 Python 环境。原脚本路径正确且页面显示准备失败时，由管理员在任务中心点击“修复环境”，等待 Python 就绪。若仍显示就绪却无法运行、或看不到修复按钮，保持任务停用，由维护人员检查原解释器与环境；不要删除旧任务来试错。环境修复不会更新上传脚本，也不会迁移旧路径。
8. 新建一个不触发真实业务的手动测试任务，核对运行、日志、文件下载和需要的宿主机软件。飞书通知如需测试，先确认接收人与内容。
9. 恢复局域网访问，把新地址告知伙伴；核对时间表和外部业务状态后，再按原清单逐个启用任务。保持旧服务停用，避免两台电脑重复执行。

使用过 AI 的安装，换电脑或更换服务 Windows 账号后需要重新配置 DeepSeek 密钥；原 DPAPI 加密文件不能当作通用明文密钥复制使用。已启用自动修复版的任务还依赖 WSL Ubuntu 的 `readonly-v1` 环境，恢复任务前按 [AI 任务助手](docs/AI任务助手.md)重新准备，并核对版本与日志。

若无法保留旧路径，目前没有一键迁移工具，也不能靠“修复环境”解决。可选择在新目录建立全新实例，重新创建成员和任务并上传原脚本、模板及依赖，旧备份单独保留；新实例不会自动导入历史记录。如果必须连历史一起换路径，需要维护人员先在备份副本中制定并验证迁移方案，再处理正式数据。不要直接手改正式数据库。

旧电脑已经损坏、备份中任务仍启用时，不能直接套用上述计划迁移流程：先在隔离机器或环境中由维护人员准备停用任务的恢复副本，防止启动后恢复排队或触发定时任务。

只重新从 GitHub 下载而没有恢复 `data`，看到的会是一套全新系统：旧账号、任务和记录都不会出现。

---

## 十三、常见问题

### 伙伴打不开网页

按顺序检查：

1. 共享电脑是否开机并已登录；
2. SpiderFly 是否已经启动；
3. 是否运行过“开启局域网访问.bat”；
4. 两台电脑是否在同一可信局域网；
5. 共享电脑 IPv4 地址是否变化；
6. 公司防火墙是否拦截 TCP 8000。

### 任务显示准备失败

常见原因是包名或版本写错、断网、下载源不可用，或者包和 Python 不兼容。先看页面上的简短原因；依赖内容写错时，请使用不同名称创建依赖正确的新任务。旧任务可按需要保留，删除前先保存所需记录和结果。

网络或包源故障排除后，管理员可在任务中心对准备失败的任务点击“修复环境”，按原依赖重新准备。使用公共指令包时还要检查基础 Python 是否至少为 3.12，以及对应版本的 wheel 是否存在。

### 自己电脑能运行，共享电脑不能

任务实际运行在共享电脑。共享电脑也必须具备脚本需要的文件、软件、登录账号、证书和驱动。

### 忘记管理员密码

当前没有自助找回密码或网页管理员重置入口。不要删除 `data\spiderfly.db`，否则账号、任务和运行记录也会丢失。联系维护人员在共享电脑上核实身份、备份并制定恢复办法；不要把数据库或密码发到群里。

### 飞书没收到消息

发送到群时，在本机 `.env` 填写 `FEISHU_WEBHOOK_URL=群自定义机器人的完整Webhook地址`，等待任务空闲后重启。群模式优先于原来的个人收件人设置；清空此项才恢复个人通知。开启签名校验时，还需填写 `FEISHU_WEBHOOK_SECRET=机器人安全设置中的签名密钥`（不是 App Secret）。返回 `19021` 时检查签名密钥和电脑时间；若机器人启用关键词校验，可设置关键词 `SpiderFly`。任务成功、失败通知开关继续生效，群内收到错误摘要，完整日志仍在运行记录中查看。

先看运行记录里的飞书状态。任务结果和通知发送是两件事；即使通知失败，运行结果仍会保存。

管理员检查宿主机 `.env` 中的 `FEISHU_APP_ID`、`FEISHU_APP_SECRET`、`FEISHU_RECEIVER_ID` 是否齐全，接收人类型 `FEISHU_RECEIVER_ID_TYPE` 是否匹配。当前支持 `open_id` 或 `mobile`，不支持填写群聊 ID。修改配置后在空闲时重启服务，并确认该任务开启了对应的成功或失败通知。

网页显示“已配置”只代表必要配置已填写，不代表应用权限、发布状态或接收人可达性已经验证；这些问题由管理飞书应用的人员根据运行记录中的错误排查。不要通过反复重跑真实业务测试通知。

### 强制停止正在运行的任务

运行中心默认打开最左侧的“运行队列”，显示当前运行任务、排队数量及等待顺序；“任务时间表”保留今日与本周计划，“运行记录”包含正在运行、排队和已结束的记录。任务中心的“查看运行”可直接重新打开当前执行日志，不会再次提交任务。管理员与超级管理员可从运行队列或运行记录打开正在运行的任务，点击“强制停止”并确认；排队任务可打开详情取消排队。系统只按该次运行的进程终止 Python 及其子进程，不按进程名称批量结束程序。提交后显示“正在停止…”，重复点击不会重复发出停止指令；等进程退出并完成工作目录清理后，记录变为“已取消”，结果代码为 `FORCE_STOPPED`，原因写明操作人，队列继续运行。已有日志和保存在运行产物目录的文件保留。若未确认进程退出，执行器继续等待，不会提前启动下一项。

普通成员无强制停止权限，排队中的任务仍使用“取消排队”。强制停止不会撤销脚本已经完成的业务操作，未保存的内容可能丢失；如需重试，请确认业务处理进度后重新运行。通知沿用任务的失败通知开关，每次运行最多发送一条最终通知。

### 失败通知附带截图

在创建任务或编辑任务的“最终通知”中，勾选“失败时通知”后，可按需勾选“失败附带截图”（默认关闭）。取消失败通知会使截图选项置灰，重新勾选失败通知后恢复原选择。

- 纯代码、后台读取 Excel 的任务可保持截图关闭；错误原因和日志始终保留。
- 操作浏览器或 Excel 窗口的任务可开启截图；失败或超时时尝试截取宿主机当时的前台窗口，截图可在运行记录的“本次文件”下载。此设置不会自动识别错误类型或定位业务窗口，请确保需要的窗口在前台。
- 截图不可用或上传失败时，仍发送错误原因和相应提示。成功或取消的任务不截图。
- 群里附图需要管理员配置应用 App ID、App Secret、机器人能力和图片上传权限，仅配置群 Webhook 时仍发送文字通知。

## AI 采集试跑

在“AI 创建”中提供网址、条件和输出要求，Agent 可生成并试跑 Python 采集草稿。草稿卡片显示实际状态、结果数量和下载文件；通过后点击“使用草稿”，按原流程创建任务和设置定时。公开网页使用 Scrapling/Playwright 采集环境，登录和验证码需人工处理。安装及范围见 [AI 采集环境](docs/AI采集环境.md)。

采集核心已补充原生 Scrapling 请求和动态页面、匿名会话、并发限速及按任务隔离的断点续采。成功验收后清除断点，下一次定时重新采集；具体接口与安装见 [AI 采集环境](docs/AI采集环境.md)。

### 通过对话指定并发等要求

可以直接说：“采集这个网站，用 4 并发抓详情，按价格排序，失败后下次继续。”AI 应把这些要求写进 Python 草稿并试跑；HTTP 批量采集支持 1–4 并发，crawl 默认 2。同一命名会话、逐页依赖或只有一个待采网址时，实际可能串行；平台上的不同任务仍排队。

创建任务后，定时执行沿用保存的脚本和参数，不用每次重新对话。后续对话生成的新草稿不会自动替换原任务或修改计划。详见 [用对话设置采集方式](docs/AI采集环境.md#用对话设置采集方式)。

## 更新与回退

后续将支持独立查看任务要求、下载任意历史 PY、在原任务上传更新，以及通过 AI 对话修改后验证启用。版本编号持续递增：已有 V3 后回退到 V2，新修改仍为 V4。更新和回退将联动需求、参数、代码和环境，保留原定时计划与历史；正在运行的任务不改变，待执行记录同步切换。

没有需求说明时，从代码整理并标注推断；只针对业务歧义询问。修复不得放宽原要求，修不好保留真实失败，最终结果通过已配置群通知告知团队人工处理，原计划保留。页面默认只显示一句实际结果，详情展开。

版本入口已实现，正式服务加载状态见 CONTEXT。完整规则见 [任务需求与版本方案](docs/任务需求与版本方案.md)。


### 下载、修改并上传原任务

1. 打开任务的 AI 助手，展开“维护与版本 → 代码版本”。
2. 点“下载 PY”，在本地修改；再点“更新脚本”上传，依赖单独填写。
3. 平台验证通过才启用。代码报错会尝试修复并另存版本；修不好保留原版本和错误。
4. 需要恢复时点击历史版本“回退”。已有 V3，即使回退到 V2，再上传仍为 V4。

“当前任务要求”和“更新记录”按需展开。待执行记录随版本切换，正在运行与历史记录保持原样，原定时计划不变。当前支持已准备的只读/采集依赖，未知依赖会明确报告验证失败。

### AI 自己探索网页

可以直接说网站名称、城市、筛选条件和目标数量，AI 会先查找入口，用原生 Scrapling 的 HTTP 或无头引擎读取网页，无须先打开可见浏览器或登录。确实需要交互、登录或验证时，才在运行 SpiderFly 的 Windows 电脑上操作独立浏览器，然后点击 AI 面板的“我已处理，继续”。已提取的数据可直接下载 CSV，结束采集后结果仍保留。安装、会话时限与定时运行的区别见 [AI 浏览器探索](docs/AI浏览器探索.md)。

AI 采集已增加原生 Stealthy 持久无头会话，可连续筛选、滚动，并从实际 JSON 接口提取和核对数据；登录提示不再直接等同于采集失败。公开筛选分批汇总时保留城市和岗位条件、按主键去重，实际数量不足会明确说明。详见 [AI 浏览器探索](docs/AI浏览器探索.md)。

自动维护支持在“AI 模型 → 调用与维护预算”勾选“不限制累计维护预算”，同时取消全局 Token、全局时长及单任务 24 小时累计时长拦截；仍记录实际用量，单次模型调用和试跑超时继续生效。未开启时沿用原限额。
