# 多宿主机阶段 7 发布与双机验收

适用范围：一台 Windows 主控和一台独立 Windows Agent，同一可信局域网。源码完成、同机合成验证和正式部署是三种不同状态；只有下面的现场记录全部完成，才能写“两台真实 Windows 验收通过”。

## 发布前闸门

| 闸门 | 要求 | 未满足时 |
| --- | --- | --- |
| 代码与构建 | 后端、Agent、前端构建和差异检查通过；发布提交明确 | 不部署 |
| 主控空闲 | 本机无 pending/running/stopping；远程无 preparing/running/stopping/uncertain | 不停止、不备份、不升级 |
| Agent 空闲 | 托盘无运行任务，本地 journal 无未回传终态 | 不升级、不恢复身份 |
| 备份 | 主控和 Agent 分别生成带哈希清单的 ZIP，存放在仓库外受控目录 | 不升级 |
| 回退点 | 记录源码提交、前端资源名、主控备份、Agent 程序备份和数据备份 | 不开始现场验收 |
| 网络 | 主控地址为当前可达局域网地址；防火墙仅限可信专用网络 | 不接入 |

正式发布需要用户另行明确授权。未授权时只能在隔离副本执行脚本和合成任务，不得停止 9356 正式服务、覆盖正式 `data/`、修改真实计划或安装正式 Agent。

## 主控发布顺序

```powershell
powershell -ExecutionPolicy Bypass -File scripts\spiderfly-controller.ps1 Status
powershell -ExecutionPolicy Bypass -File scripts\spiderfly-controller.ps1 Stop
powershell -ExecutionPolicy Bypass -File scripts\spiderfly-controller.ps1 Backup `
  -BackupPath D:\SpiderFlyBackups\before-stage7-controller.zip
```

确认备份离开仓库且可读取后，使用已审核的发布提交快进升级；统一 `Upgrade` 命令会再次检查空闲和备份，不自动合并本机改动或重启。更新依赖和前端产物后，用 `Start` 启动，以 `Status`、`Logs` 和 `/health` 核对。不要用 `--reload` 或多 worker。

## Agent 发布顺序

从主控“宿主机”页下载 `SpiderFlyAgent-0.3.0.zip`，在第二台电脑由准备运行任务的同一 Windows 账号操作：

```powershell
.\manage.ps1 Stop
.\manage.ps1 Backup -BackupPath D:\SpiderFlyBackups\before-stage7-agent.zip
.\manage.ps1 Upgrade -PackagePath D:\Downloads\SpiderFlyAgent-0.3.0.zip
.\manage.ps1 Start
.\manage.ps1 Status
.\manage.ps1 Logs
```

新电脑首次接入仍用 `start.ps1 -Server ... -Code ... -Name ...`；注册完成后改用 `manage.ps1`。主控协议 v1 与 Agent 程序版本分别校验，0.2.0 及更早版本会收到 409 升级提示且不能领取任务。升级只替换程序，数据目录、DPAPI 身份和 journal 不变。

## 双机现场矩阵

每项记录开始/结束时间、主控运行号、任务版本、Agent 名称和实际结果。任务用专门合成脚本，通知默认关闭；浏览器/Excel 项只操作测试文件和公开页面。

| # | 场景 | 操作与通过条件 | 状态 |
| --- | --- | --- | --- |
| 1 | 注册审批 | 主控生成接入码；第二台电脑申请；未批准前无任务；批准后原机器身份重连 | 待现场 |
| 2 | 两侧调度门控 | 仅网页或仅托盘开启时保持排队；两侧开启且桌面可交互后才领取 | 待现场 |
| 3 | v1 与冻结目标 | 上传单文件和依赖生成 v1，不预运行；派发后修改任务说明不改变既有运行版本/机器 | 待现场 |
| 4 | Python 成功闭环 | 第二台安装依赖、实时回传 stdout/进度、主控下载结果；同一运行只启动一次 | 待现场 |
| 5 | 浏览器/Excel 可见运行 | 状态条不遮挡测试目标；任务过程可见，结果文件正确 | 待现场 |
| 6 | 停止与紧急接管 | 普通停止保留调度；紧急接管结束进程树后切非调度，桌面立即可用 | 待现场 |
| 7 | Agent 断网恢复 | 运行中断网后主控不误判成功/释放；恢复网络后补传缺失事件且不重复执行 | 待现场 |
| 8 | Agent 关停恢复 | 空闲安全停止/启动恢复原身份；运行中安全停止留下明确终态或状态待确认 | 待现场 |
| 9 | 主控关停恢复 | 空闲停止/启动后宿主机和队列仍在；已 claim 运行不被另一任务越过 | 待现场 |
| 10 | 锁屏/注销/RDP | 无交互桌面不领取；运行中会请求停止；恢复会话后状态明确 | 待现场 |
| 11 | 旧 Agent | 0.2.0 心跳被拒绝、页面/日志提示人工升级、排队运行未下发 | 待现场 |
| 12 | 局域网地址变化 | 原地址失败时 Agent 保留 journal；同一主控且 journal 全部回传后用 `SetServer` 更新并重连；不同主控用新数据目录重新注册 | 待现场 |
| 13 | 失败与候选 | 真实合成失败立即形成管理员提醒和证据；AI 候选未经确认不运行，确认后原宿主机产生新运行 | 待现场 |
| 14 | 候选再次失败 | 下一候选号继续递增，旧版本和运行不覆盖、不复用 | 待现场 |
| 15 | Agent 备份恢复 | 同机同账号、空目录恢复后身份与 journal 可读；换账号恢复被 DPAPI 阻止并改走重新注册 | 待现场 |
| 16 | 主控数据库恢复 | 隔离目标从发布前备份恢复，quick_check 为 ok；登录、版本、产物、宿主机记录存在，合成任务可运行 | 待现场 |

第 12 项不允许在有未回传事件时直接编辑 `config.json`。`SetServer` 只适合同一主控的地址变化，并在修改前拒绝非 `acked` journal；首版没有凭据轮换或跨主控身份迁移。更换主控时保留旧数据目录、用新数据目录注册为新宿主机，再由管理员重绑计划。

## 迁移与回退

现有单机任务和历史数据原样保留。迁移只通过页面给任务绑定计划宿主机并生成新的远程运行，不直接改 SQLite、`data/apps` 或历史运行。首版主控电脑仍保留旧本机执行器，Agent 必须在另一台电脑运行；不要用测试锁绕过整机互斥。

出现发布故障时：停止新版本且不释放 uncertain 租约 → 保存日志和失败现场 → 恢复与数据库备份匹配的源码发布点 → 将现有目录改名保留 → 在空目录恢复主控备份 → quick_check、登录和只读核对 → 合成任务 → 恢复计划。Agent 程序优先用升级生成的旧程序 ZIP 回退；只要 journal 完整就不要回退 Agent 数据。确需恢复 Agent 数据时必须同机同账号、目标目录为空，并确认没有比备份更新的未回传事件。

回退成功不代表失败运行可以删除或重派。原运行事实、事件序号、产物和通知记录保持不变；无法证明进程结束时继续保留状态待确认。

## 验收记录格式

现场完成后复制下表到当日验证记录：

| 字段 | 记录 |
| --- | --- |
| 主控电脑/Windows/账号 |  |
| Agent 电脑/Windows/账号 |  |
| 主控提交与前端资源 |  |
| Agent/协议版本 |  |
| 主控与 Agent 备份位置 |  |
| 合成任务版本与运行号 |  |
| 现场矩阵结果 |  |
| 未通过项和回退结果 |  |
| 是否恢复真实计划 |  |

日志和备份路径可记录受控位置，但不得把密钥、接入码、Cookie、完整日志或真实业务文件写入仓库。
