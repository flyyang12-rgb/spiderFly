# 架构精简验证（2026-09-11）

## 改动

- 移除当前旧指令框架与 `backend/example_flows` 源码。已发布 0.1.0–0.1.4 wheel 保持原样，旧入口与依赖继续兼容。
- 新建 `spiderfly-runtime==0.1.0`，只打包任务回执和通用文件辅助，不打包业务示例、指令注册表或平台代码。
- `flows` 直接调用 Python 函数，新增当前 README 的订单筛选入口；业务规则集中维护。
- `local_packages.py` 统一解析新旧保留包名；显式固定版本、只安装本地 wheel、检查真实模块和发行版本。
- 当前 CONTEXT 压缩为状态入口，原全文归档；旧源码链接指向已发布提交，历史指令资料标注兼容用途。
- 本次涉及的运行库、流程和安装模块 Python 源码从 3326 行降至 2743 行，减少 583 行（不含测试和文档）。

## 验证

使用 Python 3.12 的新建虚拟环境与隔离源码副本，排除正式 `.env` 和运行资料；配置路径断言确认数据库、任务、环境和工作区均在副本内。未启动正式服务或执行真实任务。

1. 在副本构建新 wheel，实际安装新包和旧 0.1.4 wheel；`pip check` 通过。
2. `python -X utf8 -m unittest discover -s tests -v`：500 项，485 通过、15 跳过；无失败。首次发现新测试的错误码断言少写字母，修正断言后完整复跑通过，产品逻辑未因此放宽。
3. 单文件子进程通过 `-I` 从已安装 wheel 加载运行库，明确禁止导入旧指令包、业务包或平台 app。覆盖订单筛选、均值、金额差额、文件列表和模板入口。
4. 验证前导零、金额零值、空筛选、原文件保护、模板追加列、其他工作表保留、错误输入、回执失败及错误结果验证。
5. 验证新包缺失时安装前失败、保留包名不能绕过固定版本、导入失败不发布环境；新包导入不依赖业务示例。单独 task 入口不加载 pydantic/openpyxl 或平台服务。
6. 比较五个旧 wheel：与本轮开始前逐字节一致。按 AST 对比迁移前后文件处理函数与验证器：除名称替换外实现一致。
7. 新 wheel 内全部 Python 文件与当前源码逐字节相同；不包含旧指令包、业务示例或 app。当前入口文档本地链接检查及 `git diff --check` 通过。

跳过条件：未启用或未准备对应 WSL 受限环境、未启用本机 Chromium 集成测试，以及 Windows 缺少创建符号链接权限。

运行库 wheel SHA-256：`47dee100c68b9de012230fe45bcc979e8de1174705c7b4d9b26391cce0532138`。

## 交付边界

正式服务未重启；新安装器需要重启后端后才能被正式进程加载。现有上传脚本、任务环境、数据库、历史版本和旧 wheel 未迁移。未提交、推送或修改前端。

本轮没有拆分 `main.py` / `App.vue`，二者的职责集中问题保留为独立后续重构；本轮完成平台、运行库、业务与兼容发布文件之间的边界调整。

保留原工作区未提交的 AI、运行器、版本和前端修改。原始日志与开始前源码副本位于本机临时目录 `spiderfly-architecture-a4s8ls82`，不提交运行资料。

## GitHub 发布前复核

本次发布只包含架构精简、运行库、对应测试和配套文档。AI 对话、版本确认、时长和前端的其他本机改动不在暂存范围，保留在工作区。

从暂存树导出新的隔离源码副本，重新安装实际发布 wheel，清除继承配置并断言运行数据均在副本内。全套回归 **488 项：473 通过、15 项环境相关跳过**。该数字与上文包含其他工作区改动的 500 项检查分开记录。

AGENTS 已按三层职责和模块入口重写，详细任务规则集中到任务执行约定；使用手册按安装、任务、结果和备份组织，新增文档索引。README 使用 beautify-github-readme 技能更新纯 SVG 标题、样本和架构图。已检查 900px 内容宽度、360px 页面与深色背景，正文无横向溢出；SVG 文本边界、图片引用和文档链接通过检查。

发布 wheel 的源文件与当前 Python 源码逐字节核对。提交文档与构建包不等于正式服务已经加载新代码，本次不重启服务。
