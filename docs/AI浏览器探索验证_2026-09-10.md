# AI 浏览器探索验证（2026-09-10）

## 本次变化

现有 Agent 增加独立 Windows Chromium 工具：搜索入口、打开/观察、元素操作、页签切换、网络元数据、Scrapling 提取、人工等待和关闭。对话保存最后观察、发现域名、累计结果；主键去重和目标上限在平台执行。登录会话仅保存在独立浏览器内存，跨轮可继续，闲置 30 分钟或服务重启后结束。

AI 面板增加浏览器状态、“我已处理，继续”、关闭及 CSV 下载入口。无网页探测证据的拒绝答复会要求模型实际检查一次，仍未检查时明确写为未验证。浏览器发现的站点可用于后续静态读取/WSL 试跑。

## 验证环境与结果

使用源码隔离副本 `spiderfly-browser-check-9dm_pqrl`、独立 Python 3.12 venv，安装后端测试依赖、`requirements-browser.txt` 和指令 wheel 0.1.4。未复制正式 `.env`、数据库、模型密钥、个人浏览器资料或业务账号。

- `SPIDERFLY_TEST_BROWSER=1 SPIDERFLY_TEST_COLLECTION=1 SPIDERFLY_TEST_SANDBOX=1` 下运行 `python -X utf8 -m unittest discover -s tests -v`：最终 445 项，442 通过，3 项 Windows 符号链接权限跳过，98.753 秒。包含实际 Chromium 及 WSL 采集/受限执行检查。
- 最后补充窗口关闭后重开、重启/过期的等待状态收尾后，AI 相关检查 26 项通过；随后包含这些变更的完整检查结果为上条。
- 真正的 Windows 专用事件循环驱动 Chromium 打开公开 `books.toscrape.com`，Scrapling 从当前 DOM 提取并限定为 5 条书籍，生成 CSV。
- 隔离服务的管理员页面实际完成等待状态展示、继续、关闭、下载；关闭后文件保留。普通成员看不到 AI 创建入口，下载接口返回 403。对话所有权、过期、登录会话隔离、主键跨页去重、目标上限与无依据拒绝的纠正有自动检查。
- 页面验证使用合成模型回复；登录会话验证使用合成 Cookie。没有调用真实 DeepSeek，也没有实际登录 BOSS 或采集招聘岗位。
- 前端构建通过；最终构建 `index-CJf7UAg_.js` / `index-B2t55Dim.css` 在隔离页面验证。构建同时保留工作区已有及另一任务正在完成的版本管理界面修改。
- 本次修改文件的差异格式检查通过。原始测试日志和页面截图留在仓库外的隔离副本。

## 正式服务状态

本次在正式服务 venv 中安装了 Playwright 1.62.0 与 Scrapling 0.4.15，`pip check` 通过；已准备同一 Windows 账号下的 Chromium。没有由本任务再次停止或重启服务。

检查期间，另一轮工作于 10:58:41 重启了正式后端。随后只读核验确认：服务使用本项目路径和单 worker，浏览器源码最后修改时间早于进程启动；浏览器三个接口对未登录请求返回 401，三个浏览器表已创建，SQLite 完整性为 ok。`/health` 的 scheduler、queue_worker、environment_worker、ai_worker 均 running，线上 HTML 与构建文件一致。最终完整测试快照与当时磁盘上的后端/前端源码及构建哈希一致。

未修改正式业务任务、发送外部消息、使用真实模型或推送 GitHub。本次接口已加载，不等于 BOSS 业务已验证。

## 已知范围

当前观察以主文档 DOM 为主，没有模型截图或跨 iframe 操作；网络工具只有脱敏元数据。远程成员需要宿主机上的人完成登录。浏览器登录态不自动转入 WSL 定时脚本；登录页面的探索提取成功不能宣称无人值守脚本已通过。详情见 [AI 浏览器探索](AI浏览器探索.md)。

## 原生引擎与真实模型复测（11:19 更新）

此前“没有调用真实模型”的结论仅适用于上一阶段。随后用原始杭州 RPA 100 条需求调用真实 DeepSeek：旧流程搜索并打开 BOSS、操作一次后，因重复携带大型观察内容，在第 3 次模型调用后耗尽上下文预算（实际累计 40,498 tokens）；当时没有取得岗位结果。

直接对同一 BOSS 搜索地址进行原生引擎对比：Fetcher 返回 200 空壳，DynamicFetcher 跳转登录，StealthyFetcher 无头模式、未登录、未启用验证码求解，返回 15 个 a.job-name。接入 ai_scraping 后重复探测同一地址，仍返回 15 个岗位链接，状态 200，资源公网检查拦截数为 0。此结果证明当次列表可匿名读取，不证明每页、所有城市或 100 条目标均已完成。

新增 scrape_search/page/inspect/extract 原生工具，默认 HTTP 到无头引擎，再根据实际需要使用交互浏览器。HTTP 启用 Chrome 指纹和 stealthy_headers，保留 TLS 验证；Windows 中文路径的 CA 加载问题通过复制原证书包到临时路径解决。旧页面观察压缩为摘要，当前结构保留上限。

隔离副本完整检查：451 项，448 通过，3 项 Windows 符号链接权限跳过，99.128 秒；包含原生工具和上下文预算回归。最后调整快照链接脱敏后，原生相关 5 项再次通过。前端构建通过（index-MtVyDff8.js / index-BE3Nx65_.css）。正式依赖升级为 scrapling[fetchers]==0.4.15，pip check 通过。

11:19 在业务、维护、环境和 AI 队列空闲后停止自启任务，备份完整 data、.env 和共享工作区到仓库外的 spiderfly-native-backup-20260910-111905，再启动单 worker 正式服务。健康检查四个 worker 均 running。新原始需求的真实模型复测另行记录，不能以单独引擎成功替代 Agent 成功。

### 11:24 定位修复与预算授权

真实 AI 第二轮已调用原生 StealthyFetcher 读到岗位文字，但返回链接列表仅含导航：原实现先截取 80 个 a[href] 再排除 javascript 链接，数百个筛选菜单挤掉了岗位。现改为收集有效链接后再截断，并从实际 li/article/tr 发现重复记录区域。离线真实快照重新检查可见 15 个 job-name 链接及 li.job-card-box 区域；未写死 BOSS 选择器。

增加包含 100 个无效菜单链接的回归检查；完整检查更新为 452 项，449 通过、3 项符号链接权限跳过，96.350 秒。11:24:26 空闲备份并再次启动服务加载修正。用户授权提高预算后，通过正常管理界面保存每轮 500,000 tokens / 32 次 / 1,800 秒；自动维护预算未改。继续原对话进行真实模型验证。

### 提高预算后的真实 Agent 结果

同一对话第 13 轮在 10 次真实模型调用后完成本轮探索，实际消耗 136,614 tokens（对话累计 206,471）。AI 自行调用原生 StealthyFetcher、观察、提取，保存 15 条以职位链接去重的 RPA 相关候选记录，页面出现 CSV 下载入口。page=2/page=3 探测未取得新记录，实际页面含“登录账号，查看更多好职位”；不能推断登录后必然有 100 条。

检查下载数据发现公司与区域仍为空：首次选择器不准，后来重复主键被跳过，修正选择器不会更新已存结果。现补充同字段/主键再次提取时填补空值，已有非空值不覆盖；结果 sample 返回实际保存数据。跨轮上下文提供已存字段、主键及样本。另补指令：实际需要登录时直接打开并进入等待，不能只提议再询问；未验证不能承诺登录后一定够 100 条。此处 15 条是候选记录，尚未作为 100 条工程师岗位验收。

### 继续按官方会话与接口机制验证

用户拒绝依赖登录后，关闭本次登录等待，继续核对 Scrapling 官方 Fetchers/Stealthy 文档与已安装源码。原生 StealthySession + real_chrome + zh-CN，在匿名同会话捕获实际岗位 POST 接口。原始参数为 page=1/pageSize=15/city=101210100/query=RPA工程师/scene=1。接口报告总数237、hasMore=true；同会话浏览器 fetch 重放第一页返回15，第二页返回0，pageSize=100仍返回15。此证据替代此前仅改网页URL的错误分页测试；不能推断登录一定解决。

通过页面本身的地区筛选，保持杭州，RPA工程师及RPA相关检索并按标题、城市排除实习/外派重庆等条目，已累计29个不同岗位标识。分地区/关键词汇总不是网站默认排序前100。最初地区多选累积的问题已在实验中改为每次先清除上次选择。接口实际 salaryDesc 为空，不能编造薪资，也不能声称 JSON 已补齐未返回的字段。

为使AI自己执行同样流程，增加 scrape_live_open/act（原生 AsyncStealthySession 持久无头上下文）、网络JSON证据和 scrape_json_extract（真实响应、固定字段路径、业务筛选、同站链接模板、去重和空字段补齐）。不执行模型生成的主机代码。原生持久会话最多3个，关闭或过期时清理引擎；临时响应只留最近12项，每项最多2MB，不输出凭据。

隔离完整检查：456项，453通过、3项Windows符号链接权限跳过，113.127秒。包括真实原生上下文连续筛选、Cookie连续性、JSON证据与密码限制，以及合成城市/岗位筛选和同站链接验证。平台原生工具对真实BOSS页面另验证成功读取15条、JSON提取公司和区域；薪资由源站返回空值。尚未作为100条完成验收。

### 2026-09-10：持久会话衔接与结果修正

- `browser_open` 在已有原生会话时沿用同一个上下文，并刷新原生快照，避免混用工具导致匿名会话丢失。
- `scrape_api_page` 仅对实际捕获的只读列表请求调整已有分页字段；保留关键词、城市和筛选条件。`scrape_json_extract` 可从真实 JSON 按业务条件筛选并保存结果。
- `browser_refine_results` 可筛选已保存记录；保留最近一次筛选前结果，可撤销。后续新增仍需声明同样条件。
- 单轮预算可配置上限扩大到 2,000,000 tokens、128 次调用、7200 秒；默认值未变，自动维护预算独立。
- 实测无需登录，按杭州公开行政区及 RPA/影刀关键词取得 34 个候选，剔除产品经理后有 33 个工程相关岗位。这是筛选汇总，不是默认排序前100条。UiPath 的13区追加检查未增加合格结果。工资字段在源 JSON 中为空，不补造。
- 真实AI测试暴露了原生会话切换及初次保存混入实习岗位的问题；本节列出修正机制，不代表已完成100条目标。

### 2026-09-10 12:53 部署核验

- 已将持久筛选规则加载到运行服务。筛选会同时约束后续 DOM/JSON 提取，并保留撤销前记录及规则；相同字段只变顺序时沿用原 CSV 列顺序。
- 完整隔离回归：459项，456通过，3项Windows符号链接权限跳过，102.401秒（native-persistent-filter-full.log）。前端构建通过。
- 实际单轮预算通过管理页面调整为1,000,000 tokens、96次、3600秒，自动维护预算未改。
- 独立Scrapling追加测试：UiPath的13行政区、西湖区38商圈，未增加合格岗位；已验证33条保存至共享工作区/采集验证_2026-09-10。
- AI对话24的第16轮曾累计90条，但混有空结果页推荐岗位，已主动停止，不作成功验收。第17轮已实际调用持久筛选，将结果修正为11条，再从真实搜索JSON继续；最终数量以该轮结束后复核为准。
- 部署前已停止空闲服务并备份完整data/.env/共享工作区，备份目录：%TEMP%/spiderfly-native-backup-20260910-125351。

### 验收方向调整

用户明确要求自主探索过程，数据数量不是目的。因此后续以全新AI对话、仅提供自然语言目标来检验：是否自行选工具、读真实页面和接口、检查样本、依据错误调整。独立脚本所得37条只是对照证据，不能当作AI助手独立获得的结果；历史包含选择器提示的对话也不能替代这一验收。

批量筛选工具已用真实站点验证3区（11→18条），合成页面验证成功项保存、条件漂移拒收、未观察选项拒绝和停止请求。处理记录可显示客观观察与执行结果，不展示内部思考。
