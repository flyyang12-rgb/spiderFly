# 多宿主机进度与通知验证（2026-09-21）

本轮验证工作区中的远程结构化进度、失败站内提醒和飞书摘要通知。测试只使用隔离源码副本、临时数据库、合成任务、模拟 Agent 身份和模拟飞书发送器；未读取正式数据、发送真实飞书消息或运行真实业务。

## 验证结果

- 宿主机专项：55 项通过；连同 worker 接线共 59 项通过。
- 全后端：430 项，412 通过、18 项因 Chromium、WSL、DP 或 Windows 符号链接条件不足而跳过。
- 前端构建通过：`index-C2_3oV2g.js`、`index-BYooWMSp.css`。Node 20.15.0 低于 Vite 建议的 20.19.0，构建保留版本警告。
- `git diff --check` 通过。

专项检查覆盖 stdout 进度行跨 Agent 事件分片、进度持久化与列表/详情读取；失败终态立即可见、普通成员无权读取、管理员已读幂等保存。前端把原维护结果与远程失败合并为“运行提醒”，远程提醒可打开对应宿主机运行详情；普通成员仍只加载其原维护通知。

飞书检查覆盖任务通知开关冻结、失败与超时、错误脱敏、未配置跳过、发送失败留痕、单次尝试、服务重启不重发，以及详情 URL 直达宿主机运行。发送在终态事务提交后由现有串行 worker 处理，不阻塞 Agent 终态回执；发送结果不改变远程运行状态。

## 尚未验证

- 未启动正式服务，正式数据库尚未执行新增列和提醒回执表迁移。
- 未做浏览器页面交互验收或两台真实 Windows 联调。
- 未做真实飞书群或个人收件人送达验收；全局运行看板、日志保留清理和桌面状态条仍未接入。
