# AI 助手

负责把需求变成脚本草稿，帮助探索网页和修改任务。页面入口是 AI 助手。

采集相关问答先参考 [采集知识库](采集知识库.md)，覆盖 Scrapling 原理、平台用法与常见报错，并附版本和来源。

## 操作流程

描述需求 → 助手分析或探索 → 保存脚本草稿 → 在支持的环境试跑 → 创建任务或提交修改候选。

## 关键规则

- 日常执行已保存脚本不调用模型；模型用于对话、生成和故障维护。
- 生成或修改脚本，需要把字段、筛选等要求落实到代码；口头答复不算完成。
- 浏览器探索会话与定时脚本的登录状态不同，探索成功不等于定时运行已经可用。
- 试跑必须走准备好的受限环境和串行队列；不保证任意 Windows 桌面脚本都能自动验证。
- 修改现有任务交给“版本与修复”；当前工作区已有对话切换、删除和标题相关待发布改动。

常见提问：“助手会不会每次运行都重新写脚本？”“网页登录能不能用于定时任务？”

[返回功能地图](README.md)

DP 为可选原生 Python 方案，页面检查与试跑都共用普通任务队列和配置端口，见 [DP 任务](DP任务.md)。

## 对应 Python

[ai_agent.py](../../backend/app/ai_agent.py)、[ai_tools.py](../../backend/app/ai_tools.py)。职责与调用关系见 [知识框架对照](知识框架与代码对照.md)。
