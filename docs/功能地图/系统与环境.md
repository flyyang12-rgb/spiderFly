# 系统与环境

负责准备任务运行条件。页面入口包括创建任务时的环境状态、AI 模型和系统设置；宿主机部署另见使用说明。

## 操作流程

准备宿主机与配置 → 按脚本依赖准备 Python 环境 → 启动服务 → 向任务执行、AI 和通知提供支持。

## 关键规则

- 普通 Windows 任务环境与 AI 受限验证环境分开准备，能力不相同。
- 模型设置负责模型连接与调用预算；通知按配置发送运行或维护结果。
- 数据库、上传脚本、执行产物、配置和密钥属于运行资料，不能当作源码一起覆盖。
- 拉取代码不等于运行服务已经加载新代码；更新代码、重启和业务验证分别确认。
- 备份、换电脑和恢复按 [使用说明](../../使用说明.md) 执行。

常见提问：“为什么环境准备失败？”“更新后需要重启吗？”“通知在哪里配置？”

[返回功能地图](README.md)

## 对应 Python

[config.py](../../backend/app/config.py)、[environments.py](../../backend/app/environments.py)、[host_runtime.py](../../backend/app/host_runtime.py)。职责与调用关系见 [知识框架对照](知识框架与代码对照.md)。
