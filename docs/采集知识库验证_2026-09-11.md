# 采集知识库验证（2026-09-11）

## 本轮内容

新增 13 个主题知识文件；上游来源固定到 Scrapling v0.4.15 的提交 `333fa22b7a5821194ce66b59b11f4b16a6484f02`。参考源码与官方文档自行整理中文说明，区分原生、AI 探索和 collection-v1 接口。

增加独立 ai_knowledge 检索模块与 read_knowledge 工具；采集轮次在首次模型请求前附带本地知识。原有 Markdown 知识继续可检索，零匹配不再返回无关文章，压缩知识时保留来源和章节。

## 验证方式

使用独立源码副本，清除 SPIDERFLY、FEISHU、DEEPSEEK 与 Python 路径环境变量，并断言运行数据路径全部位于副本。Python 3.12，测试不读取正式模型密钥、不连接真实目标网站，不改正式数据库。

| 检查 | 覆盖内容 |
| --- | --- |
| 中文检索 | 翻页、并发续采、Cookie、代理、自适应、CSS、RAG、安装错误的目标主题进入前三结果 |
| 旧知识 | Excel 单元格问题仍优先命中 tables.md |
| 章节 | 完整读取、目录、长章节继续读取、代码块内标题不误切分 |
| 来源与范围 | 返回固定版本、核对日期、适用范围与官方提交链接 |
| 结果边界 | 空问题、无命中、字符预算、目录外路径拒绝 |
| Agent 流程 | 首次模型调用已有本地参考；纯知识说明不强制探站；实际采集拒绝仍需要证据 |
| 上下文 | 压缩后来源、版本和 section 保留，截断明确标记 |
| 示例 | Python 示例语法检查；合成 HTML 用真实 Scrapling Selector 离线解析 |

知识与 Agent 专项执行 `python -X utf8 -m unittest tests.test_ai_knowledge tests.test_ai_agent -v`，39 项通过。全套执行 `python -X utf8 -m unittest discover -s tests -v`，349 项，334 通过、15 跳过。跳过项延续既有独立浏览器、WSL/collection 环境及 Windows 符号链接条件。

## 结论与限制

本轮验证证明知识能够进入模型请求、按主题检索并保留引用信息；Agent 流程使用模拟模型响应，没有评测真实模型的最终回答质量，也未验证真实网站采集。运行环境中的 Scrapling 仍为 0.4.15，未增加依赖或开放原生完整 Spider、代理、MCP 权限。

前端未修改，不涉及重新构建。本轮未提交、推送或重启正式服务。原始资料与日志保留在本机临时目录 `spiderfly-knowledge-7ed727ebeeda4349bf0dfe719b37e19e`。

负责人入口：[采集知识库](功能地图/采集知识库.md)；维护入口：[知识库维护](开发/采集知识库维护.md)。

## DrissionPage 参考资料与索引补充

新增 DrissionPage 4 个专题和 collection_index.md 总索引，记录工具、主题、版本、来源及接入状态。Scrapling 13 篇补齐工具与接入状态元数据。检索识别 DrissionPage / DP / SessionPage，优先对应工具；比较问题附双方边界。Agent 回答规则和工具说明同步更新，上下文压缩保留状态。

最终在独立源码副本执行 `python -X utf8 -m unittest discover -s tests -v`：353 项，338 通过、15 跳过。新增检查覆盖 DP 检索、双方边界、索引链接、元数据，以及首次模型请求中的未接入状态；同时检查压缩后仍保留状态。初次新增模型模拟遗漏用量字段，按模型接口要求补齐后完整回归通过。

原始日志位于本机临时目录 spiderfly-dp-3acbbcda7e41460b8222fd6acb946759/full-tests.log。测试清除继承配置，先断言数据目录均属于副本。文档相对链接与 git diff --check 通过。

没有安装 DrissionPage、增加执行工具或改变受限环境；未调用真实模型、未验证 DrissionPage 浏览器运行效果。未提交、推送或重启正式服务。原有 15 项宿主环境相关跳过仍保留，不能描述为真实浏览器及 WSL 全部验收。

## 用户本地 DrissionPage Markdown 吸收

资料首页标注 4.1.1.2，与此前官网 4.1.1.4 概要分别保留版本。64 篇 Markdown 中导入 59 篇技术文档；原 index 重建，4 篇商店/社群/视频导航排除。没有执行文档中的代码或操作指示。原文图片不进入文字检索，也未进行视觉解析。

新增 reference/ 正文、catalog.md 全目录、manifest.json 哈希清单、ports.md 任务浏览器与端口说明和可重复的导入脚本。保留参数表与示例，去除站点元信息和广告标签，远程连接示例密钥替换为占位符；原目录中的无关通知代码未导入。逐篇验证全部原文件哈希与导入时一致，源目录未修改。

检索支持二至四级标题，保留父级上下文；原问题/API 名优先于别名，英文别名不匹配 SessionPage 等长词内部。支持 DP 紧邻中文的写法。每条原生参考携带版本、原相对路径、原文 SHA-256 和未接入状态；同名 API 仍需按对象和版本补读。端口说明覆盖实际调试地址、existing_only、新开与复用、用户目录、标签页、退出归属及原文 auto_port 多进程描述差异。

隔离副本：本机临时目录 spiderfly-dp-docs-cbb6521b770b4457b1c3e66c942abbeb。清除继承配置并断言数据路径在副本后执行：

- 知识与 Agent 检查：49 项通过。
- 完整回归：359 项，344 通过，15 项环境相关跳过，原始记录 full-tests.log。
- 新增覆盖 14 类中文/API 问题的前三条检索结果、59 篇清单完整性及哈希、版本来源、参数补读、嵌套标题和围栏、导入可重复与源文件不变、占位密钥与混入代码排除。
- 文档相对链接、全部源文件哈希和 git diff --check 通过；reference 文件用 .gitattributes 固定 LF，避免 Windows 检出改变哈希。

验证中修正了 SessionPage 别名扩展挤掉 API 文档、无关查询默认加权影响 Excel 检索及 Windows 换行影响输出哈希的问题。页面广告标签移除，原文教学代码里的广告元素选择器仍作为例子保留，未执行。

没有安装 DrissionPage、调用真实模型、启动浏览器或运行原文示例。以上证明资料进入检索与模型上下文链路，不证明真实模型回答质量或全部原生 API 运行效果。未提交、推送或重启正式服务。
