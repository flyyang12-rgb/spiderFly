# AI 采集接入验证

日期：2026-09-09。验证对象为本轮工作区源码，未提交。隔离副本前缀为 `spiderfly-collection-check-`，未读取正式数据库、密钥或业务任务进行测试。

## 已验证

- `python -X utf8 -m unittest discover -s tests -v`：415 项，412 通过、3 项 Windows 符号链接权限跳过，耗时 71.212 秒。启用 `SPIDERFLY_TEST_SANDBOX=1` 和 `SPIDERFLY_TEST_COLLECTION=1`。
- Scrapling 实际读取公开测试站点，生成两条真实书名 CSV；平台独立检查字段、数量与唯一性。
- Playwright 实际执行 JavaScript，并加载列表页、点击下一页、检查第二页 20 条记录；通过受控通道读取页面及其脚本资源。
- 初始浏览器真实页面崩溃，单独将 collection-v1 临时目录从 16MB 改为 128MB 后原复现通过；保留真实翻页回归测试，旧 readonly-v1 配额不变。
- 采集试跑经串行队列执行，数量不足和无实际请求不标记通过；停止、重启中断、跨对话拒绝、文件下载权限均有检查。
- 带采集运行标记的合成任务按 `schedule` 来源入队，经真实执行器使用相同 collection-v1，产生成功记录和真实输出。
- 浏览器内无法直接连外网，无法读取 Windows 挂载或宿主机用户目录；网络通道拒绝内网、未知域名、凭据 URL 和自定义端口。
- 隔离页面验证：管理员提交需求，经过保存草稿、试跑工具、真实采集，显示通过、实际 2 条、文件与日志；下载内容核对。使用草稿带入原创建表单，提示与试跑状态一致。普通成员无 AI 入口，访问试跑文件 API 返回 403。
- 该页面测试使用模拟模型响应驱动工具调用，实际运行器和网络不是模拟；没有调用真实 DeepSeek 或产生新模型费用。
- 前端依锁文件安装并构建通过，发布资源为 `index-mXIjY24a.js`、`index-EIASrXw0.css`；已同步项目 dist，保留旧资源以兼容仍运行的旧服务和已打开页面。

## 部署状态

已在服务账号的 WSL Ubuntu 准备独立 collection-v1、Scrapling 0.4.15、Playwright 1.62.0、Chromium 及其系统依赖；没有替换原 readonly-v1 依赖目录。

正式队列空闲检查通过，但自动审批拒绝了停止并更新正式服务的组合命令，返回仅为 `blocked by policy`，未给出更具体理由。该命令未执行，未完成本轮正式备份或重启。随后只同步了已验证的前端发布文件并读取健康状态，原四个 worker 仍运行。

**正式后端尚未重启，新采集工具尚未在正式服务生效。**需要按正常维护流程备份并重启服务。未操作正式任务或调用正式模型，未提交和推送本轮源码。原本已有的失败截图时序改动保持在工作区，本轮只在对应执行器位置增加采集环境分派。

## 实际边界

集成 Scrapling Selector 与 Playwright，不是 Scrapling 所有 Fetcher/Spider 功能；仅支持用户指定域名的公开 GET，不支持登录、POST、个人 Cookie、验证码处理或 DP。验收证明文件及声明条件，不自动证明所有业务语义；采集维护验证实时重读网页，不是冻结网页重放。具体安装和上限见 [AI 采集环境](AI采集环境.md)。
