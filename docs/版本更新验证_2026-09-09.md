# 需求与版本更新验证（2026-09-09）

## 交付范围

- 当前任务要求按用户要求、代码推断、未说明保存；Agent 每轮读取当前版本要求，显式变更与代码一起入版。
- 原任务上传 PY、AI 提交、历史 PY 下载，验证通过再切换，回退后编号继续递增。
- 报错上传尝试一次修复并另存版本；原上传不覆盖，失败保留旧版本和日志。
- 版本切换同事务更新待执行快照；运行中、历史和原计划保持不变。旧提交不能覆盖新提交。
- 最终维护失败与修复重跑失败接入已配置群通知；发送失败如实保留、不无限重发。维护失败不自动停用。
- 页面收在“维护与版本”；使用说明、AI 指南、AGENTS 和知识库已同步。

## 自动检查

在隔离副本 backend 运行：

```powershell
$env:SPIDERFLY_TEST_COLLECTION='1'
$env:SPIDERFLY_TEST_SANDBOX='1'
python -X utf8 -m unittest discover -s tests -q
```

434 项，431 通过，3 项因 Windows 符号链接权限跳过，耗时 90.737 秒。覆盖真实 WSL 只读/采集环境及原有后端回归。

最后新增 AI 工具/草稿按钮合并到原任务的测试，单独运行 `python -X utf8 -m unittest tests.test_task_versions -q`：15 项全部通过。未改动其余已验证业务代码。

前端 `npm ci`、`npm run build` 通过，产出 index-BeUDp8j9.js 与 index-CUd6u9JT.css。

## 实际页面验证

隔离 FastAPI、合成用户和任务，localhost:18763，关闭正式调度与产品实例锁启动流程，仅启用隔离版本队列：

- 管理员进入维护与版本，查看代码版本和上传表单。
- Windows CRLF 脚本上传，经真实 WSL 运行与 CSV 验收后显示 V2 使用中。
- 下载 PY 触发浏览器下载事件；回退后显示 V1 使用中、V2 可回退。
- 普通成员页面没有 AI/版本管理入口；接口测试确认上传、下载返回 403。
- 首次实测发现 CRLF 一致性误判，统一入口换行后复测通过，已补回归测试。

## 限制与部署

需求推断仅提取模块说明、常量及验收声明，不声称理解任意业务代码。当前验证支持已准备的 readonly-v1 / collection-v1 依赖；未准备依赖、Windows GUI、登录浏览器不属于自动验证范围。运行通过不等于未声明业务验收通过。

群接口使用模拟发送验证，未向真实群发消息；模型调用也使用合成响应，未消耗真实模型额度。

本轮不操作正式任务或数据库。正式服务未重启、新接口尚未加载；前轮自动审批拒绝正式更新，本轮没有绕过限制。未提交或推送 GitHub。隔离源代码与生产工作区按开始时 SHA256 核对后同步，保留其他未提交改动。


## 提交前复验

用户授权提交 GitHub 后，从实际暂存内容导出隔离副本重新验证，排除独立截图时序改动：435 项，432 通过、3 项 Windows 符号链接权限跳过，耗时 95.476 秒。前端 npm ci / npm run build 通过，按 Git 源码换行生成的交付资源为 index-DttujKoG.js 和 index-9w2GKuq9.css。暂存差异格式检查通过，未包含 .env、运行数据或真实密钥；未重启正式服务。
