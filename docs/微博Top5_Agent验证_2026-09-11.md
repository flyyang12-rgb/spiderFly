# 微博 Top5 真实 Agent 测试（2026-09-11）

结论：取得 5 条实时响应中的话题记录；没有观察到 Agent 主动查询知识库，结果文件与回答也存在字段差异，本次不能作为知识库调用验收通过。

## 测试方式

- 在独立源码副本和合成账号/数据库中直接运行现有 process_turn，使用已配置的 DeepSeek 模型。未启动正式 worker 或修改正式数据。
- 提问原文：“抓微博热搜top5，给出排名、话题和链接。”没有额外提示使用知识工具，没有替换模型响应或采集结果。
- 原有 SYSTEM 和工具定义保持不变；本次使用 deepseek-v4-pro，试验限制为最多 12 次调用、360 秒、200000 tokens。配置和密钥仅按既有配置读取，密钥不复制、不打印。
- 北京时间 2026-09-11 15:11:15 至 15:11:41，共 7 次模型调用；输入 64898、输出 1110 tokens。
- 该句不触发现有采集关键词自动附带逻辑，因此可以直接观察模型是否主动选择知识工具。

## 实际调用顺序

1. scrape_page：请求 weibo.com/ajax/side/hotSearch，HTTP 403。
2. scrape_page：切换 stealth 模式，仍 HTTP 403。
3. scrape_live_open：打开同一接口，仍 HTTP 403。
4. browser_open：进入微博热搜页面，跳转公开未登录页面；捕获 bandUnified 接口的 HTTP 200 响应，data.realtime 有 15 条。
5. scrape_json_extract：首次尝试链接模板因跨站地址限制被拒绝。
6. scrape_json_extract：改为读取 word_scheme，提取并保存前 5 条。
7. 模型输出最终回答。

实际没有 search_knowledge 或 read_knowledge 调用，也没有 DP 执行调用。数据来自现有 Scrapling/浏览器工具。未要求或使用用户登录。

## 响应提取的前五条

| 展示顺序 | 接口 rank | 话题 |
| --- | --- | --- |
| 1 | 0 | iPhone17Pro史上最低价 |
| 2 | 1 | 男子离婚6年后发现自己被去父留子 |
| 3 | 2 | 28岁智障女孩多次生子后离世 |
| 4 | 3 | 库克 华为 |
| 5 | 4 | TF三代艺人活动取消 |

来源：[微博热搜入口](https://weibo.com/hot/search)，页面实际捕获 [bandUnified 接口](https://weibo.com/ajax/side/bandUnified) 的 data.realtime。以上是测试时的快照，不保证之后榜单相同。

## 验收问题

- 知识调用：模型未选择知识工具。只能证明这次任务没有主动查阅，不能据此断言所有问题都不会查。后续需用确实依赖具体文档参数的任务验证，并检查 Agent 指令与行为。
- 字段一致性：提取结果的排名是 0–4；最终回答展示为 1–5，未解释转换。
- 链接一致性：提取的“链接”是 word_scheme 话题文字；最终回答将其拼成 s.weibo.com 搜索 URL，但没有更新保存结果。因此不能把 CSV 描述成已包含同样的链接。
- 链接验证：最终回答拼接的 5 个搜索链接没有逐个访问验证。
- 请求行为：403 后继续换模式访问同一接口，需要单独检查是否符合项目对限流/拒绝响应的处理要求。
- 浏览器生命周期：过程中出现 Playwright pending task / TargetClosedError 日志，最终采集继续完成；测试结束已调用本对话 browser_close。不能以采集成功忽略这些日志。

## 证据与状态

完整 live-result.json、live-run.log 和 微博Top5_原始提取.csv 位于本机临时目录 spiderfly-weibo-61512e796a1d437b968ef6adc71f8685。原始 CSV 按工具实际 sample 导出，没有替 Agent 修正字段。
脚本直接调用 process_turn，未运行外层 worker 的完成状态更新，所以隔离 ai_turns 的 running 字段不是正式任务状态；test_summary 的 completed 仅表示函数正常返回。
没有提交或推送代码、没有重启正式服务。本轮仅测试与记录，未修改知识检索或 Agent 行为。
