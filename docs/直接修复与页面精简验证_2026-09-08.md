# 直接修复与页面精简验证

## 本次行为

用户要求普通错误直接修好，不把排队或设置编辑交回用户协调，只在日志告知修改；并要求精简 AI 助手及其他页面。

- 无验收声明的脚本：最小修复检查、原错误同类复现、候选试跑通过后自动启用。删除原调用/函数/校验或新增捕获异常来掩盖错误的候选不通过；运行验证不等于证明未声明的业务规则。
- 仍在运行的任务先结束；已就绪修复优先试跑，同任务同源同输入的待运行记录更新代码引用，保留参数和发起人。仅名称、说明及计划编辑不会阻止修复。源码、依赖、模板或回退发生真正变化时结束旧版本维护，避免覆盖新内容。
- 执行日志直接显示修复状态、版本和原因，过程可展开；原 stdout/stderr 和失败状态仍表示原次执行。
- AI 默认短回复；最新草稿常显，旧草稿、长回复、用量、维护和预算按需展开。精简工作台、任务、创建、设置及通知文案。

## 隔离验证

沿用临时副本 `spiderfly-agent-check-34e9f7c47b4241528ba46bd475c6a982`，Python 3.12，独立数据库和工作区；源码同步不含正式运行资料。模型请求使用替身，页面验证服务器禁止真实模型调用。正式密钥没有用于本次隔离验证。

执行 `SPIDERFLY_TEST_SANDBOX=1` 下的 `python -X utf8 -m unittest tests.test_maintenance -v`：24 项通过。新增覆盖普通 TypeError 自动发布及下一次受限执行、不同错误不能充当复现、待运行记录自动换版及参数保留、正在运行时延后、用户新代码不被旧候选覆盖、原调用和校验不能删除、真实 WSL TypeError 复现与修复输出。

`python -X utf8 -m unittest discover -s tests -v`：394 项，391 通过，3 项 Windows 符号链接权限跳过，用时 26.414 秒；包含真实 WSL 文件、网络和子进程隔离及超时检查。日志位于隔离副本 `verification-direct-repair.log`。`pip check` 无依赖冲突。

`npm run build` 通过，产物为 `index-CWuepoOX.js` / `index-Mug_uQyp.css`。浏览器实测管理员的日志修复说明、修改记录展开、AI 长回复收起、历史草稿展开、代码查看、带入创建表单、模型预算展开及保存；普通成员可看日志结论，没有管理中心或 AI 管理权限。浏览器错误列表为空。页面测试只使用既有合成任务，没有提交新任务或发送通知。

## 正式部署与用户指定修复

确认所有正式队列空闲后通过原 Windows 自启任务停止服务，完整备份到 `%LOCALAPPDATA%/SpiderFly/Backups/before-direct-repair-20260908-111410`，包含 data、环境、.env、页面、当前源码与 HEAD 基线，目录仅当前账号和 SYSTEM 访问。

按用户明确要求继续维护 `AI_Test`（任务 3，失败执行 13，维护记录 1）。原候选已经存在，本次只恢复它的维护排队、保留原用量并追加审计；没有直接覆盖原上传文件。自动维护流程在 WSL 复现原 TypeError 后试跑已有 V2，通过后自动更新任务版本引用。没有新增模型调用。

实际修改：`return f"你好, {3 + name}!"` 改为 `return f"你好, {name}!"`。维护结果为 `activated`，版本批准标记为 1，运行模式为 `readonly-v1`。保存的 `original.log` 含原 TypeError，`run.log` 无 Traceback，`check.json` 为通过。执行日志对应说明：

> 修正字符串拼接：3 + name → name，解决 TypeError；原错误已复现，修复后运行通过；已启用 V2

服务重启后 scheduler、queue_worker、environment_worker、ai_worker 均运行正常。与备份逐字段核对：tasks 仅任务 3 的 script_path/version/updated_at 改变，rpa_apps 仅应用 3 的 script_path/updated_at 改变；users、executions 全部原样，原失败记录 13 未改写。SQLite 完整性为 ok。线上静态资源与本次构建逐字节一致。

没有为此强行补跑原执行记录，正式修复由自动维护试跑验证；未验证任意 Windows GUI、登录浏览器或不兼容依赖的自动修复。源码与构建尚未提交或推送 Git。

## 追加：一句话维护日志

按用户要求，维护记录、执行日志和站内通知改用同一简短结论：具体改动及效果，验证成功后显示运行通过。成功记录预览去掉重复的验证、版本套话和无信息的结尾；失败和未启用记录保留原原因，不追加成功结论。数据库中的原始说明和事件不改写。

维护记录默认展开最近一次结果，调用次数、已确认 Token 和完整过程收进“技术详情”；运行环境及历史版本收进“代码版本”，代码查看和回退入口保留。模型工具的修复说明要求用一句“把 A 改为 B，解决 C”，不允许在候选提交阶段声称已经试跑成功。

隔离全套 394 项检查，391 通过、3 项 Windows 符号链接权限跳过，用时 27.673 秒，日志为隔离副本 `verification-concise-maintenance.log`。前端构建通过，资源为 `index-CFeyOFRj.js` / `index-CW46h2NB.css`。浏览器使用合成的重复维护日志核对普通成员的简短结论和技术详情展开、管理员通知及维护面板、代码版本和回退入口，控制台无错误；没有调用真实模型或执行正式任务。

正式服务在所有队列空闲时备份至 `%LOCALAPPDATA%/SpiderFly/Backups/before-concise-logs-20260908-125756` 后重启，四个 worker 健康。tasks、rpa_apps、users、executions 以及维护任务、事件、策略、已读记录、代码版本均与备份一致；SQLite 完整性正常。线上前端资源与新构建逐字节一致。使用说明、AGENTS 和 AI 指南已同步新入口。代码尚未提交或推送。
