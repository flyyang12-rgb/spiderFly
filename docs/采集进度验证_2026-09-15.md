# 采集进度验证 · 2026-09-15

## 范围

基于 d3c8334 增加可选 JSON 行协议、输出持久化、执行详情展示和 AI 生成要求。平台状态、业务验收、维护与串行队列规则不变；未修改已上传业务脚本。

## 验证结果

- 隔离副本：`C:/Users/20898/AppData/Local/Temp/spiderfly-progress-e51cc7786e7b42e7be6ccb61b6a6ee98`。只复制源码，清除进程项目变量，断言应用与数据路径全部指向副本；未导入正式目录 app。
- 全套 `unittest discover`：374 项，356 通过、18 按 WSL/真实浏览器环境条件跳过。日志在副本 tests.log。
- 6 项进度专项：中文任意分块、未知总量、事件长度与历史上限、无效/半行/超长行、日志截断后进度持久化、失败不被覆盖、重复数据库迁移、DP 和受限执行在进程退出前传递日志。通道测试使用本地合成子进程，不访问业务网站。
- 前端 `npm run build` 通过；产物 index-mrC6ADQm.js / index-DtkPtrPb.css。构建入口换行统一为 LF，git diff --check 通过。
- agent-browser 检查隔离组件页面：采集中/失败/完成/旧脚本；过程展开、错误页与原因显示；未知总量未显示百分比；运行失败优先于脚本 complete；390px 宽度无横向溢出。无页面错误、无 Vite 错误遮罩。截图在工作区 artifacts/progress-running.png 与 progress-mobile.png。
- 首次回归遇到测试依赖 httpx2 缺失、隔离副本漏复制导入脚本，均在隔离环境补齐；日志截断测试的精简建表夹具更新为包含新列后完成全套回归。未修改正式依赖。

## 本机加载

确认执行、维护、试跑、AI 对话及环境准备均空闲，停止原服务，备份完整 data 与 .env 后重启单 worker。备份位于 ../SpiderFly-backups/before-collection-progress-20260915-085216。服务监听 0.0.0.0:9356，PID 21900，四个后台 worker 均 running，数据库 quick_check 为 ok，新进度字段存在，首页加载新构建。

未执行真实业务任务，未验证外部网站采集成功率。旧脚本需要在正常版本更新中增加进度上报才能显示数量；已上传文件没有自动改写。未提交或推送 GitHub。
