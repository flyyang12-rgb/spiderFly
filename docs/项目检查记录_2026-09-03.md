# SpiderFly 项目检查记录

检查日期：2026-09-03。用户要求保留简单测试例子、暂停扩展功能，再从上到下检查项目。此次检查对象为当前工作区的 SpiderFly，包括原有未提交改动；相邻 task_scheduler 未改动。

## 结论与实际修复

发现并修复七处可复现问题。现有指令调用、任务上传建环境、排队运行、日志与文件下载链路验证通过。公共测试包保持 0.1.4、六条指令不变，未增加新的业务功能。

| 问题出现的场景 | 修复后的行为 | 代码位置 |
| --- | --- | --- |
| 从其他目录启动，并配置相对数据或工作路径，实际目录随启动位置变化 | 相对路径统一基于项目根目录解析，绝对路径与默认值保持原有含义 | [config.py](../backend/app/config.py) `_resolved_path` |
| 脚本一次打印超过读取行长限制的内容，日志消费中断，可能阻塞执行 | 分块读取，并保留跨块中文字符；实测 20 万字中文长行完整保存 | [runner.py](../backend/app/runner.py) `_consume_stream` |
| 修改含中文的密码时，字符串常量时间比较抛出异常 | 按 UTF-8 字节比较，正常改密成功；新旧相同仍给出明确错误 | [security.py](../backend/app/security.py) `change_password` |
| 更新任务显式传入 null，部分字段导致服务或数据库错误 | 所有更新字段明确拒绝 null；省略字段继续表示不修改 | [schemas.py](../backend/app/schemas.py) `TaskPatch` |
| 更新任务接受全空白名称，与创建规则不一致 | 拒绝空白名称，正常名称去除首尾空白 | [schemas.py](../backend/app/schemas.py) `TaskPatch` |
| 停用排队任务时，执行记录已取消而任务仍显示等待 | 同一事务同步任务状态；已经运行的任务保持运行状态 | [main.py](../backend/app/main.py) `update_task` |
| Python 环境损坏或尚未准备好时，无法单独停用任务 | 单独关闭启用开关不依赖环境就绪；其他设置修改仍检查环境 | [main.py](../backend/app/main.py) `update_task` |

新增 [test_project_audit.py](../backend/tests/test_project_audit.py)，共 11 项针对性回归检查。先在旧代码副本复现问题，再验证修复，未通过放宽断言掩盖原问题。

## 检查范围与证据

| 范围 | 实际检查与结果 |
| --- | --- |
| 后端、公共指令、包安装及流程 | 修改前全套 281 项中 278 项通过、3 项跳过；新增回归并修复后共 292 项，289 项通过、3 项跳过，无失败；依赖一致性检查通过 |
| 页面与构建产物 | 从当前源码重新构建前端到隔离目录，构建通过；查看任务日志弹窗，长日志可滚动，结果文件可下载，浏览器控制台无错误 |
| Windows 启动与配置 | 启动脚本和启动器源码检查；启动器编译到临时目录成功；四份 PowerShell 脚本语法检查通过 |
| 创建和执行完整任务 | 在页面上传合成测试脚本，声明并实际安装 0.1.4 公共包，创建环境后点击运行；调度、排队和环境工作进程正常，任务成功并返回 AUDIT_DONE |
| 日志与文件交付 | 保存 200039 个字符的完整 stdout；实际触发页面下载，另用普通成员会话 GET/HEAD 下载两份结果，与本次存档逐字节一致 |
| 登录与权限 | 未登录读取任务或下载文件返回 401；普通成员访问管理接口返回 403，页面没有管理入口；首次登录账号受到改密限制 |
| 更新输入与密码 | 显式 null、全空白名称返回 422；合成账号中文密码更新返回 200，同密码更新返回 400，新密码可重新登录 |
| 停用任务 | 页面点击启用开关可停用；排队取消与状态同步、运行中状态保留、坏环境允许停用由回归检查覆盖 |
| 文档与当前安排 | 更新 README、CONTEXT、使用说明、优化台账和维护记录，取消旧的自动推进暗示，明确暂停新增功能 |

测试采用没有真实 .env 的项目副本，清除继承的项目、通知及 Python 路径设置，使用独立数据库、账号、任务和工作目录。页面测试使用只监听本机的临时服务；收尾已关闭，18761 端口无监听。未更改真实任务、账号或用户桌面 Excel。

两份测试文件为 `流程文件/输出/检查结果.txt` 和 `流程文件/输出/文件清单.json`，由实际公共任务入口和 `file.list` 生成。运行、权限、下载摘要及测试日志保存于工作区仓库外的 `outputs/01a065cf-a33d-7e33-8235-7dffafd69b9b/项目全面检查/`，使用的都是合成测试数据。

### 保存的证据怎么查

以上 outputs 路径相对于包含 spiderFly 的工作区根目录，不在仓库内部。该目录中的材料如下；以后补测应新增记录，保留这些原始结果。

| 文件 | 用来核对什么 |
| --- | --- |
| `baseline-tests.log` | 修改前全套 281 项检查的结果 |
| `reproduced.log` | 新增回归检查在旧代码上复现的问题；包含子测试报错，不能把失败条数直接当作缺陷数 |
| `final-tests.log` | 修复后 292 项检查的逐项结果及三项跳过原因 |
| `api-proof.json` | 隔离执行状态、日志长度、权限响应、两份下载的大小及 SHA-256 |
| `检查结果.txt`、`文件清单.json` | 实际公共任务生成并下载核对的结果 |
| `最终核对.json` | 本轮修改的五个既有代码文件、前端构建一致性、五个 wheel 摘要、原检查时的语法与链接数量 |
| `项目检查记录_2026-09-03.md` | 首次检查收尾时的文档快照，最新补充以仓库中本文为准 |

收尾核对：76 个 Python 文件语法通过；最新 wheel 内 13 个 Python 文件与源码一致；现有 frontend/dist 与本轮重新构建结果逐字节相同。五个历史 wheel 均保持原摘要，最新 0.1.4 的 SHA-256 为 `f8560f402637c0f41faef3a2bed009c30a12d6f0e51c4900575d2c143e6d2494`。原检查时 15 份文档、265 处本地链接通过核对，此数字是当时快照，不包含后来补写的链接。

### 三项跳过测试

以下测试都因 Windows 创建符号链接返回错误 1314 而跳过，具体名称保存在 final-tests.log。恢复具备权限的隔离环境后，运行相应测试模块补测，记录新的实际结果。

| 测试位置 | 尚未通过真实链接验证的行为 |
| --- | --- |
| [test_execution_artifact_files.py](../backend/tests/test_execution_artifact_files.py) `ArtifactFileTests.test_file_and_directory_symlinks_are_excluded` | 产物列表排除文件及目录符号链接 |
| [test_host_runtime.py](../backend/tests/test_host_runtime.py) `WorkDirectoryCleanupTests.test_removes_a_child_symlink_without_deleting_its_external_target` | 清理工作区内链接时不删除外部目标 |
| [test_host_runtime.py](../backend/tests/test_host_runtime.py) `WorkDirectoryValidationTests.test_rejects_a_work_directory_that_is_a_symbolic_link` | 拒绝将符号链接本身作为工作目录 |

## 本轮验证的边界

- 三项跳过来自 Windows 缺少创建符号链接的权限，不能视为已通过真实链接测试。
- 构建及源码检查不等于新电脑首次安装验收；未实际调整防火墙、注册开机自启或运行新编译启动器。
- 通知设置在测试中关闭，未发送飞书通知，也没有验证外部账号和真实业务系统。
- 这些是项目功能检查，不计入 OPT-011 的两项真实业务验收，不表示所有候选优化已完成或所有潜在问题已排除。
- 正式服务未重启，现有进程要在空闲时重启才能加载后端修复；本轮没有提交、发布或升级真实任务。

### 尚未执行的事项

| 事项 | 当前状态与后续处理 |
| --- | --- |
| 正式服务加载修复 | 未执行；管理员安排空闲维护时按[使用说明](../使用说明.md#修改代码后怎么生效)停止、备份、更新并重启，随后核对登录和任务状态 |
| 三项符号链接测试 | 等具备权限的隔离环境后补测，当前不记为通过 |
| 新电脑、自启、防火墙、外部通知及真实业务验收 | 本轮未实测；实际需要部署或接入时，按对应场景另行验证 |
| 扩充指令或专用页面 | 按用户决定暂停；出现实际需求后再选范围，当前不执行旧的“下一件”建议 |

如何准备副本和运行回归检查见[复测与交接](维护约定与决策记录.md#复测与交接)。这些未执行事项不改变本轮已完成的代码修复与隔离验证结论，也不表示正式服务已经更新。

## 当前维护决定

保留现有简单例子与公共测试包，暂停增加指令和专用页面。后续出现实际重复动作或缺口，再决定补哪一件；旧记录中的“复制文件”建议只是未实施候选。当前状态以[项目优化台账](项目优化台账.md)与 DEC-014 为准。
