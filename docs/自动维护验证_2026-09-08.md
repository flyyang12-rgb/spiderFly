# 默认自动维护验证（2026-09-08）

本轮按用户要求取消维护开关，任务失败后默认进入维护，结束后通知。代码及构建文件未提交或推送 Git。

## 已执行的检查

- Python 3.12 隔离副本运行 `python -X utf8 -m unittest discover -s tests`，设置 `SPIDERFLY_TEST_SANDBOX=1`：388 项，385 通过、3 项 Windows 符号链接权限跳过，27.023 秒。
- `pip check`：No broken requirements found。前端 `npm run build` 成功。
- 新增 18 项检查涵盖默认维护、源码去重快照、候选试跑与发布/回退、模型不能降低原验收、缺少验收或业务操作范围时保留候选、依赖不兼容、运行队列变更、停止、重启和不确定用量预留、全局限额、通知归属及独立已读、删除时的维护互斥与清理、CSV/XLSX 验收、失败业务回执及危险产物名。
- 真实 WSL bubblewrap + seccomp 检查：候选看不到 `/mnt/c` 或 DeepSeek 凭据，不能直接联网、fork 或 exec；无限循环按时退出。没有只靠 Python 语法过滤宣称隔离。

## 真实模型与执行链路

使用本机隔离实例 `127.0.0.1:18761`、测试账号及合成文件；未复制或执行正式业务任务。唯一使用的正式配置是已授权 DeepSeek 密钥，经原 Windows 用户解密后仅保留在测试服务内存。

2026-09-08 10:39:38，故意出错的标准库 CSV 脚本首次运行因未定义变量失败。平台自动读取源码、任务说明和真实异常；DeepSeek 提交一个最小修复，将缺失变量改为迭代得到的实际值。受限试跑按修复前保存的条件验收通过，10:39:45 自动启用 V2。随后再次通过正式串行执行入口运行同一测试任务，成功返回 `ACCEPTANCE_PASSED`。

实际 CSV 核对为两条记录：`a=2`、`b=4`，字段 `name,value`，唯一且数值为正。未把单纯进程退出 0 当作验收。修复分析共 1 次 DeepSeek 调用，服务返回输入 1,010 / 输出 270 tokens，共 1,280。未降低原行数或字段条件。

浏览器检查覆盖：普通成员看不到其他人的维护通知和管理员入口；超级管理员看到未读维护结果；“我知道了”消除未读；“查看维护记录”能进入对应任务并展开实际用量和步骤；回退 V1 后界面显示原始版本使用中、运行环境恢复原环境；全局预算显示 7,200 秒和 200,000 tokens，保存反馈正常。页面没有维护开关，浏览器无 error 记录。

## 部署

部署前确认正式执行、环境构建和 AI 队列均为空。完整备份位于本机 `%LOCALAPPDATA%/SpiderFly/Backups/before-maintenance-20260908-105119`，包含 data、一致性 SQLite 备份、`.env`、当前页面、待部署工作区源码以及 Git HEAD 源码；目录仅当前 Windows 用户与 SYSTEM 可访问。工作区源码是已验证的部署候选，不冒充前一个运行进程的源码。之前 AI 首版部署的备份另行保留。

通过现有 `SpiderFly Host Autostart 04C5438E` 自启任务重载本机正式服务，未修改启动器及自启配置。`9356/health` 返回调度、队列、环境和 AI 四个 worker 均 running；新维护通知接口未登录访问返回 401，说明路由存在且认证生效。WSL readonly-v1 运行环境检查正常。

迁移后按原列比对 tasks、rpa_apps、users、executions 的内容摘要，均与部署前一致；SQLite integrity_check 为 ok，新增维护及代码版本表，当前所有队列仍为空。未回填历史失败任务或调用真实业务任务做验证。服务已加载新后端，代码和构建文件尚未提交/推送。

## 验证的边界

本轮真实模型用例为合成标准库 CSV；XLSX 验收及错误回执在单元检查中覆盖。没有用 BOSS 直聘、登录账号、Windows GUI、Scrapling 浏览器或真实工作表做业务测试。受限环境支持范围见 [AI 任务助手](AI任务助手.md)，不能由这个用例推断任意上传脚本都能自动修好。原代码缺少独立验收时仍维护并报告，但保留候选、不会自动启用。

代码版本按内容去重，执行仅引用版本；没有以每次修复创建新 venv。保留正式任务原有 Windows 运行权限；只有受限兼容且经过原验收的修复会切换到同一隔离环境持续执行。维护完成提醒为站内持久通知，未新增飞书、邮件或其他外部发送渠道。
