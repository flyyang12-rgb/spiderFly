# 主要页面与接口拆分验证（2026-09-11）

## 范围与结果

基于 main 的 `0ebd845` 及当时全部本机未提交改动，拆分主要页面、功能操作、HTTP 接口、队列和后台生命周期。保留现有业务行为，未加入之前讨论的调度恢复或日志性能优化。

| 入口 | 拆分前 | 拆分后 | 移出内容 |
| --- | --- | --- | --- |
| backend/app/main.py | 1383 行 | 51 行 | 8 个接口模块、3 个服务模块 |
| frontend/src/App.vue | 1835 行 | 243 行 | 19 个视图与弹窗、分功能状态与操作、公共请求及格式函数 |

实际位置见 [代码对照与模块结构](开发/代码对照与拆分方案.md)。AI、版本和维护的已有独立模块保留，没有把它们的内部实现全部拆细。

## 自动验证

- Windows、Python 3.12；在独立源码副本运行，清除项目及通知环境变量，断言数据库、应用、环境和工作目录全部位于副本。
- 拆分前：`python -X utf8 -m unittest discover -s tests -v`，334 项，319 通过、15 跳过。
- 拆分后：同一命令，335 项，320 通过、15 跳过；最终日志无导入异常。
- 新增队列检查：采集试跑必须在领取普通任务之前被调用，且不能产生错误日志，覆盖目录迁移后的延迟导入路径。
- 跳过项涉及未启用的独立 Chromium、WSL / collection 运行环境，以及 Windows 符号链接权限；不视为这些集成已验收。
- 46 个后端函数移出后，对导入引用和装饰器做归一化，函数体 AST 与基线一致。74 个前端函数体一致，退出登录的计时器清理由同义的 stopPolling 调用承接。
- 拆分前后 OpenAPI 契约一致。多方法下载接口已有的重复 operation ID 警告两边都存在，本轮未改变。
- 隔离前端执行 `npm ci --no-audit --no-fund`、`npm run build` 成功；Vite 8.2.2、Vue 3.5.42，依赖锁文件保持原样。构建产物已同步 frontend/dist。

## 页面验收

使用 agent-browser 访问 `127.0.0.1:18791` 的独立站点，仅使用合成账号、脚本、运行记录与产物。测试站点关闭后台生命周期，不执行真实任务，不启动模型或环境安装。

- 管理员与普通成员登录、退出、刷新；普通成员不显示管理中心。
- 工作台、任务中心、运行队列、时间表、历史记录、创建任务、模型设置、成员、系统设置、审计页面。
- 上传合成脚本，修改任务说明并保存；手动入队、取消排队；查看日志及触发产物下载。
- 编辑合成成员并保存，审计页显示对应修改；打开密码弹窗并通过 Escape 关闭。
- 打开 AI 创建入口；未调用真实模型。版本等业务由后端回归覆盖，不把页面入口验证视为真实模型或 WSL 验证。
- 浏览器错误与控制台检查未发现运行错误；查看工作台、执行详情截图确认主要布局。

## 保留与交付状态

已与拆分前副本核对：AI、执行器、版本、前端 AI 页面、样式及依赖锁文件的原有改动保持不变。App.vue 原有工作区逻辑随本轮迁移保留。

原始日志与截图保留在本机临时验证目录 `spiderfly-modules-26b60429007b4c5fa24c3a6e6077f0d8`，包括 baseline-tests.log、final-tests.log、frontend-build.log、接口契约及截图。

本轮未提交或推送，未重启正式服务。运行资料未迁移，测试站点不代表正式服务已加载新版本。
